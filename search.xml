<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>android command line 安装时的坑</title>
    <url>/2020/03/24/android-command-line-%E5%AE%89%E8%A3%85%E6%97%B6%E7%9A%84%E5%9D%91/</url>
    <content><![CDATA[<p>很久没有搞过 Android 开发了，Android 的命令行安装方式相比之前发生了一些变化，这里针对安装最新的 Android 命令行时遇到的一些问题，记录一下解决方法。</p>
<p><em>问题：下载最新的 commandlinetools 在自己指定的目录安装之后，无法使用</em></p>
<p><strong>原因：最新版本的 commandlinetools 对安装的目录是有要求的</strong></p>
<ol>
<li>Download latest Command line tools from android i.e. commandlinetools-win-6200805_latest.zip</li>
<li>Unzip the downloaded file</li>
<li>Create directory for storing commandline tools somewhere on your disk, with following path included: android&#x2F;cmdline-tools&#x2F;latest Basically when You unzip this Cmd line tools, just rename tools directory to latest and make sure You put this latest folder in android&#x2F;cmdline-tools directory somewhere on your disk</li>
<li>Create ANDROID_HOME environment variable for directory that stores the cmdline tools directory location like: <code>C:\YourLocationWhereYouStoreTheDirectory\android\cmdline-tools\latest</code></li>
<li>Create new entry in Path environment variable as %ANDROID_HOME%\bin</li>
</ol>
]]></content>
  </entry>
  <entry>
    <title>JaCoCo手册</title>
    <url>/2022/01/26/JaCoCo%E6%89%8B%E5%86%8C/</url>
    <content><![CDATA[<h2 id="JaCoCo-简介"><a href="#JaCoCo-简介" class="headerlink" title="JaCoCo 简介"></a>JaCoCo 简介</h2><blockquote>
<p>JaCoCo should provide the standard technology for code coverage analysis in Java VM based environments. The focus is providing a lightweight, flexible and well documented library for integration with various build and development tools.</p>
</blockquote>
<p>JaCoCo 要为基于 Java VM 的环境中的代码提供覆盖率分析标准技术，重点是提供一个轻量、灵活且文档齐全的库，以便与各种构建和开发工具集成。</p>
<h3 id="功能特性"><a href="#功能特性" class="headerlink" title="功能特性"></a>功能特性</h3><ul>
<li>指令（C0）、分支（C1）、行、方法、类型、圈复杂度的覆盖率分析；</li>
<li>基于 Java 字节码，即使没有源码依然可以运行；</li>
<li>支持 on-the-fly 模式，可以通过 javaagent 便捷地集成，也可以通过 API 来自定义类装载器进行集成；</li>
<li>与框架无关，可以对基于 Java VM 的应用程序顺利的集成；</li>
<li>与已所有已发布的 Java 版本兼容；</li>
<li>支持不同的 JVM 语言；</li>
<li>可以生成多种格式的报告（HTML、XML、CSV）；</li>
<li>支持远程协议和 JMX 控制，可以在任何时间点来进行覆盖率数据的存储；</li>
</ul>
<h3 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h3><p>下载完 JaCoCo 之后会得到几个 jar 包，其中 jacocoagent.jar 和 jacococli.jar 是我们比较关注的。</p>
<ul>
<li>jacocoagent.jar 是 JVM 应用程序启动时的代理</li>
<li>jacococli.jar 是生成覆盖率报告、合并 dump 文件的命令行工具</li>
</ul>
<h4 id="jacocoagent"><a href="#jacocoagent" class="headerlink" title="jacocoagent"></a>jacocoagent</h4><p>Java Agent 的原理可以阅读 <code>java.lang.instrument.Instrumentation</code> 这个类，这里先介绍 jacocoagent 的使用方法。</p>
<p>在 Java 应用的启动命令上加入如下的参数：</p>
<p><code>-javaagent:[yourpath/]jacocoagent.jar=[option1]=[value1],[option2]=[value2]</code></p>
<p>即可对启动的 Java 应用程序中指定的类进行代码覆盖率的采集，其中 option 的选项有：</p>
<table>
<thead>
<tr>
<th>option</th>
<th>缺省值</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td>destfile</td>
<td>jacoco.exec</td>
<td>保存 jacoco 采集的数据文件的路径</td>
</tr>
<tr>
<td>append</td>
<td>true</td>
<td>数据文件保存路径的文件存在的情况下，是否追加覆盖率数据</td>
</tr>
<tr>
<td>includes</td>
<td>*</td>
<td>采集哪些类（采集类过多会导致新能下降严重）</td>
</tr>
<tr>
<td>excludes</td>
<td>*</td>
<td>排除哪些类</td>
</tr>
<tr>
<td>inclbootstrapclasses</td>
<td>false</td>
<td>是否需要采集 bootstrap classLoader 加载的类</td>
</tr>
<tr>
<td>inclnolocationclasses</td>
<td>false</td>
<td>是否需要采集没有原位置的类（即程序运行时生成的类）</td>
</tr>
<tr>
<td>sessionid</td>
<td>auto-generated</td>
<td>会话标识符，如果没有配置该参数，jacocoagent 会自动生成</td>
</tr>
<tr>
<td>dumponexit</td>
<td>true</td>
<td>是否在 VM 关闭时写入数据</td>
</tr>
<tr>
<td>output</td>
<td>file</td>
<td>存储数据的方式，有效的选项：file、tcpserver、tcpclient、none</td>
</tr>
<tr>
<td>address</td>
<td>loopback interface</td>
<td>当 output 为 tcpserver 时启用，缺省值为 127.0.0.1</td>
</tr>
<tr>
<td>port</td>
<td>6300</td>
<td>当 output 为 tcpserver 时启用</td>
</tr>
<tr>
<td>classdumpdir</td>
<td>no dumps</td>
<td>保存插桩之后的 class 的路径</td>
</tr>
<tr>
<td>jmx</td>
<td>false</td>
<td>是否开启 JMX 功能</td>
</tr>
</tbody></table>
<h4 id="jacococli"><a href="#jacococli" class="headerlink" title="jacococli"></a>jacococli</h4><p>jacoco 的命令行工具，可以用于链接 jacocoagent 用于采集回文件的存储、对指定的类进行插桩、合并 dump 文件、生成报告等功能，具体使用方法不再一一列举，可以查看<a href="https://www.jacoco.org/jacoco/trunk/doc/cli.html">官方文档</a></p>
]]></content>
  </entry>
  <entry>
    <title>curl-command 手册</title>
    <url>/2019/10/24/curl-command-%E6%89%8B%E5%86%8C/</url>
    <content><![CDATA[<p>curl 是十分常用的命令，通常我们用这个命令来请求 Web 服务器。一些常见的构造 HTTP 请求的客户端（如 Postman）关于项目协作的功能都是商业化版本才有的，团队开发过程中通过维护接口的 curl 命令脚本可以低成本的代替一些商业化软件的功能。</p>
<p>下面介绍一下 curl 命令的常见用法。</p>
<h2 id="基础用法"><a href="#基础用法" class="headerlink" title="基础用法"></a>基础用法</h2><h3 id="请求一个-URL"><a href="#请求一个-URL" class="headerlink" title="请求一个 URL"></a>请求一个 URL</h3><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">curl http://www.baidu.com/</span><br></pre></td></tr></table></figure>

<h3 id="X"><a href="#X" class="headerlink" title="-X"></a>-X</h3><p>使用 -X 参数可以指定 HTTP Method</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">curl -X POST http://www.baidu.com/</span><br></pre></td></tr></table></figure>

<h3 id="H-–header"><a href="#H-–header" class="headerlink" title="-H, –header"></a>-H, –header</h3><p>使用 -H 或者 –header 参数可以添加 HTTP 的请求头</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">curl -H &quot;Content-Type:application/json&quot; http://www.baidu.com/</span><br></pre></td></tr></table></figure>

<h3 id="d-–data"><a href="#d-–data" class="headerlink" title="-d, –data"></a>-d, –data</h3><p>使用 -d 或 –data 可以发送 HTTP 的请求参数，可以是 form 表单，也可以是二进制的 request body</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">发送 form 表单</span></span><br><span class="line">curl -X POST -d &quot;name=jasonfu&amp;age=20&amp;birthday=1990/06/01&quot; &quot;http://localhost:8080/http/post?type=form&quot;</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">也可以使用多个 -d 参数</span></span><br><span class="line">curl -X POST -d &quot;name=jasonfu&quot; -d &quot;age=20&quot; -d &quot;birthday=1990/06/01&quot; &quot;http://localhost:8080/http/post?type=form&quot;</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_">#</span><span class="language-bash"><span class="comment">## 发送 request body</span></span></span><br><span class="line">curl -X POST -H &quot;Content-Type:application/json&quot; -d &quot;&#123;\&quot;name\&quot;:\&quot;jasonfu\&quot;,\&quot;age\&quot;:19,\&quot;birthday\&quot;:12345678911&#125;&quot; &quot;http://localhost:8080/http/post?type=body&quot;</span><br></pre></td></tr></table></figure>

<h3 id="F-–form"><a href="#F-–form" class="headerlink" title="-F, –form"></a>-F, –form</h3><p>使用 -F 或 –form 参数可以上传文件</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_">#</span><span class="language-bash"><span class="comment">## 上传文件</span></span></span><br><span class="line">curl -F &quot;file=@alias.bat&quot; -X POST &quot;http://localhost:8080/http/post?type=upload&quot;</span><br><span class="line"><span class="meta prompt_">#</span><span class="language-bash"><span class="comment">## 上传文件 带参数</span></span></span><br><span class="line">curl --form &quot;file=@alias.bat&quot; --form &quot;id=1&quot; -X POST &quot;http://localhost:8080/http/post?type=upload&quot;</span><br></pre></td></tr></table></figure>

<h2 id="进阶用法"><a href="#进阶用法" class="headerlink" title="进阶用法"></a>进阶用法</h2><h3 id="i-I"><a href="#i-I" class="headerlink" title="-i, -I"></a>-i, -I</h3><p>使用 -i 参数可以输出 HTTP 的相应头</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">curl -i http://www.baidu.com/</span><br></pre></td></tr></table></figure>

<p>使用 -I 参数只输出 HTTP 的相应头</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">curl -I http://www.baidu.com/</span><br></pre></td></tr></table></figure>

<h3 id="e-–referer"><a href="#e-–referer" class="headerlink" title="-e, –referer"></a>-e, –referer</h3><p>使用 -e 或者 –referer 参数可以在 HTTP 请求头中添加 referer 字段，标识从哪里跳转过来的</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">curl --referer http://www.baidu.com/ http://www.qq.com/</span><br></pre></td></tr></table></figure>

<h3 id="A-–user-agent"><a href="#A-–user-agent" class="headerlink" title="-A, –user-agent"></a>-A, –user-agent</h3><p>使用 -A 或者 –user-agent 参数可以指定 HTTP 请求头中的 User-Agent 字段</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">curl --user-agent curl-client http://www.baidu.com/</span><br></pre></td></tr></table></figure>

<h3 id="cookie"><a href="#cookie" class="headerlink" title="cookie"></a>cookie</h3><p>使用 -b 或者 –cookie 可以发送 cookie 给 Web 服务器。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">curl --cookie &quot;user=fuyongde&quot; http://www.baidu.com/</span><br></pre></td></tr></table></figure>

<p>使用 -c 参数可以保存服务端返回的 cookie 到指定的文件</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">curl -c baidu-cookie.txt http://www.baidu.com/</span><br></pre></td></tr></table></figure>

<h3 id="显示通信过程"><a href="#显示通信过程" class="headerlink" title="显示通信过程"></a>显示通信过程</h3><p>使用 -v 或者 –verbose 参数可以展示完整的 HTTP 通信过程</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">curl -v http://www.baidu.com/</span><br></pre></td></tr></table></figure>

<p>使用 –trace 或者 –trace-ascii 参数可以展示更详细的通信过程</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">curl --trace tract.txt http://www.baidu.com/</span><br></pre></td></tr></table></figure>

<h3 id="保存响应"><a href="#保存响应" class="headerlink" title="保存响应"></a>保存响应</h3><p>使用 -o 或者 -O 参数可以将 http 相应保存为文件，区别在于 -o 可以指定文件名，-O 则是使用 URL 中最后一段 path 作为文件名来进行保存，其下载文件的效果等同于 <code>wget</code> 命令。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 将 http://www.baidu.com/ 响应保存至文件 baidu.html 中</span><br><span class="line">curl -o baidu.html http://www.baidu.com/</span><br><span class="line"># 将 http://localhost:8080/ping 响应保存至文件 ping 中</span><br><span class="line">curl -O http://localhost:8080/ping</span><br></pre></td></tr></table></figure>

<h3 id="使用代理"><a href="#使用代理" class="headerlink" title="使用代理"></a>使用代理</h3><p>一些 Web 服务器是仅支持某些特定的 ip 来访问的，这种情况下，我们就需要使用代理来发送我们的 HTTP 请求，使用 -x 参数可以指定代理服务器来发送我们的请求。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">使用 socks5 协议的代理服务器来发送请求</span></span><br><span class="line">curl -x socks5://username:password@proxyhost:8080 http://www.baidu.com/</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">默认情况下使用 http 协议的代理服务器来发送请求</span></span><br><span class="line">curl -x username:password@proxyhost:8080 http://www.baidu.com/</span><br></pre></td></tr></table></figure>

<h3 id="L-–location"><a href="#L-–location" class="headerlink" title="-L, –location"></a>-L, –location</h3><p>对于一些 HTTP 响应码为 302 的情况，可以使用 -L 或者 –location 参数来实现自动跳转的效果</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">curl -L -i http://localhost:8080/http/redirect</span><br></pre></td></tr></table></figure>

<p>其输出如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">HTTP/1.1 302</span><br><span class="line">Location: http://localhost:8080/http/get/jasonfu</span><br><span class="line">Content-Length: 0</span><br><span class="line">Date: Tue, 24 Aug 2021 10:22:10 GMT</span><br><span class="line"></span><br><span class="line">HTTP/1.1 200</span><br><span class="line">Content-Type: application/json</span><br><span class="line">Transfer-Encoding: chunked</span><br><span class="line">Date: Tue, 24 Aug 2021 10:22:10 GMT</span><br><span class="line"></span><br><span class="line">&#123;&quot;name&quot;:&quot;jasonfu&quot;,&quot;age&quot;:18,&quot;birthday&quot;:&quot;2021-08-24T10:22:10.713+00:00&quot;&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到已经实现了自动跳转的效果</p>
<h3 id="–limit-rate"><a href="#–limit-rate" class="headerlink" title="–limit-rate"></a>–limit-rate</h3><p>使用 –limit-rate 参数可以限制访问 HTTP 请求和响应的带宽。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">将带宽限制为每秒 1k 字节</span></span><br><span class="line">curl --limit-rate 1k http://www.baidu.com/</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>手册</category>
      </categories>
      <tags>
        <tag>curl</tag>
        <tag>command</tag>
        <tag>未完待续</tag>
      </tags>
  </entry>
  <entry>
    <title>数据库死锁</title>
    <url>/2019/11/15/database-deadlock/</url>
    <content><![CDATA[<blockquote>
<p>死锁是指两个或者多个事务在同一个资源上相互占用，并请求锁定对方占用的资源，从而导致恶性循环的现象</p>
</blockquote>
<h2 id="示例"><a href="#示例" class="headerlink" title="示例"></a>示例</h2><p>先用一个示例来演示一下数据库死锁的情况。本示例基于 <code>MySQL</code> 的 <code>InnoDB</code> 存储引擎。</p>
<h3 id="表结构"><a href="#表结构" class="headerlink" title="表结构"></a>表结构</h3><p>我们先建一个 wallet 表，表结构如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mysql&gt; DESC wallet;</span><br><span class="line">+---------+---------------------+------+-----+---------+-------+</span><br><span class="line">| Field   | Type                | Null | Key | Default | Extra |</span><br><span class="line">+---------+---------------------+------+-----+---------+-------+</span><br><span class="line">| id      | bigint(20) unsigned | NO   | PRI | NULL    |       |</span><br><span class="line">| user_id | bigint(20) unsigned | NO   | UNI | 0       |       |</span><br><span class="line">| balance | bigint(20)          | NO   |     | 0       |       |</span><br><span class="line">+---------+---------------------+------+-----+---------+-------+</span><br></pre></td></tr></table></figure>

<h3 id="开启事务-A，并修改-id-为-1-的记录"><a href="#开启事务-A，并修改-id-为-1-的记录" class="headerlink" title="开启事务 A，并修改 id 为 1 的记录"></a>开启事务 A，并修改 id 为 1 的记录</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mysql&gt; START TRANSACTION;</span><br><span class="line">Query OK, 0 rows affected (0.00 sec)</span><br><span class="line"></span><br><span class="line">mysql&gt; UPDATE wallet SET balance = balance + 100 WHERE id = 1;</span><br><span class="line">Query OK, 1 row affected (0.00 sec)</span><br></pre></td></tr></table></figure>

<h3 id="开启事务-B，并修改-id-为-2-的记录"><a href="#开启事务-B，并修改-id-为-2-的记录" class="headerlink" title="开启事务 B，并修改 id 为 2 的记录"></a>开启事务 B，并修改 id 为 2 的记录</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mysql&gt; START TRANSACTION;</span><br><span class="line">Query OK, 0 rows affected (0.00 sec)</span><br><span class="line"></span><br><span class="line">mysql&gt; UPDATE wallet SET balance = balance + 100 WHERE id = 2;</span><br><span class="line">Query OK, 1 row affected (0.00 sec)</span><br><span class="line">Rows matched: 1  Changed: 1  Warnings: 0</span><br></pre></td></tr></table></figure>

<h3 id="事务-A-修改-id-为-2-的记录"><a href="#事务-A-修改-id-为-2-的记录" class="headerlink" title="事务 A 修改 id 为 2 的记录"></a>事务 A 修改 id 为 2 的记录</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mysql&gt; UPDATE wallet SET balance = balance + 100 WHERE id = 2;</span><br></pre></td></tr></table></figure>

<p>此时发现该修改并未马上成功，而是陷入的等待状态，原因是事务 A 此时要锁定 id 为 2 的记录，但该记录已经被事务 B 锁定了。</p>
<h3 id="事务-B-修改-id-为-3-的记录"><a href="#事务-B-修改-id-为-3-的记录" class="headerlink" title="事务 B 修改 id 为 3 的记录"></a>事务 B 修改 id 为 3 的记录</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mysql&gt; UPDATE wallet SET balance = balance + 100 WHERE id = 1;</span><br><span class="line">ERROR 1213 (40001): Deadlock found when trying to get lock; try restarting transaction</span><br></pre></td></tr></table></figure>

<p>此时事务 B 报错，错误原因很明确是发生了死锁问题，通过查询操作也可以发现事务 B 的操作已经回滚，于此同时事务 A 对 id 为 2 的记录的修改已经成功，后续的 <code>COMMIT</code> 操作就不展示了。</p>
<h2 id="如何解决死锁问题"><a href="#如何解决死锁问题" class="headerlink" title="如何解决死锁问题"></a>如何解决死锁问题</h2><h3 id="死锁产生的原因"><a href="#死锁产生的原因" class="headerlink" title="死锁产生的原因"></a>死锁产生的原因</h3><p>锁的行为和顺序是和存储引擎相关的，同样的顺序执行语句，有些存储引擎会产生死锁，有的则不会。故死锁的产生有两重原因：有些是因为真正的数据冲突，有些则完全是由于存储引擎的实现方式导致的。</p>
<p>死锁发生以后，只有部分或者完全回滚其中一个事务，才能打破死锁。对于事务性的系统，死锁问题是无法避免的，设计对应的应用程序时必须考虑如何处理死锁。大部分情况只需要重新执行因死锁导致回滚的事务即可。</p>
<h3 id="解决死锁问题"><a href="#解决死锁问题" class="headerlink" title="解决死锁问题"></a>解决死锁问题</h3><p>常见的数据库都实现了个中死锁检测和死锁超时机制。</p>
<ul>
<li><p>死锁检测：比如上述例子中，检测到死锁问题，数据库立马报错，这种解决方式非常有效，否则死锁会导致非常慢的查询。</p>
</li>
<li><p>超时机制：超时机制就是当查询时间达到锁等待超时的设定后放弃锁的请求。</p>
</li>
</ul>
<p><code>InnoDB</code> 目前处理死锁的方式是将最少行级排他锁的事务进行回滚。</p>
<h3 id="InnoDB-显式和隐式锁定"><a href="#InnoDB-显式和隐式锁定" class="headerlink" title="InnoDB 显式和隐式锁定"></a>InnoDB 显式和隐式锁定</h3><p><code>InnoDB</code> 采用的时两端锁定协议（two-phase locking protocol）。在事务执行过程中，随时都可以执行锁定，锁只有在执行 <code>COMMIT</code> 或者 <code>ROLLBACK</code> 的时候才会被释放，并且所有的锁是在同一时刻被释放。<code>InnoDB</code> 会根据隔离级别在需要的的时候自动加锁。</p>
<p><code>InnoDB</code> 也支持通过特定的语句进行显式的锁定，这些语句不属于 <code>SQL</code> 规范。</p>
<ul>
<li><code>SELECT ... LOCK IN SHARE MODE</code></li>
<li><code>SELECT ... FOR UPDATE</code></li>
</ul>
<p><code>MySQL</code> 也支持 <code>LOCK TABLES</code> 和 <code>UNLOCK TABLES</code> 语句，这是在服务器层面实现的，跟存储引擎无关，不能用于代替事务处理。</p>
]]></content>
      <tags>
        <tag>数据库</tag>
        <tag>MySQL</tag>
      </tags>
  </entry>
  <entry>
    <title>Arthas 非常实用的几个命令</title>
    <url>/2020/01/18/arthas-useful-command/</url>
    <content><![CDATA[<p>本文介绍几个非常有用的 Arthas 命令，熟练使用这几个命令，可以让我们在排查线上问题的时候更加轻松。</p>
<p>关于 Arthas 的设计思想，美团技术团队的文章<a href="https://tech.meituan.com/2019/02/28/java-dynamic-trace.html">Java 动态追踪技术探究</a>一文中做了详细的说明，有兴趣可以去阅读一下。</p>
<h2 id="trace"><a href="#trace" class="headerlink" title="trace"></a>trace</h2><p><code>trace</code> 命令见名知意，可以用于跟踪方法内部的调用路径，渲染整个调用链路上所有的性能开销和跟踪调用链路。</p>
<p>例如，我们提供了一个 <code>controller</code>，用来保存一笔订单数据，如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.goku.order.rest;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.goku.order.entity.Order;</span><br><span class="line"><span class="keyword">import</span> lombok.extern.slf4j.Slf4j;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.PostMapping;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.RequestBody;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.RequestMapping;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.RestController;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.*;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> fuyongde</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2020/1/18 15:36</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="meta">@RequestMapping(&quot;/api/orders&quot;)</span></span><br><span class="line"><span class="meta">@Slf4j</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">OrderRestController</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="type">Random</span> <span class="variable">random</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Random</span>();</span><br><span class="line"></span><br><span class="line">    <span class="meta">@PostMapping</span></span><br><span class="line">    <span class="keyword">public</span> Map&lt;String, Object&gt; <span class="title function_">save</span><span class="params">(<span class="meta">@RequestBody</span> Order order)</span> &#123;</span><br><span class="line">        <span class="type">long</span> <span class="variable">id</span> <span class="operator">=</span> random.nextLong();</span><br><span class="line">        order.setId(id);</span><br><span class="line">        <span class="type">Date</span> <span class="variable">now</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Date</span>();</span><br><span class="line">        <span class="keyword">if</span> (Objects.nonNull(order.getFlag()) &amp;&amp; order.getFlag()) &#123;</span><br><span class="line">            log.info(<span class="string">&quot;调用路径1&quot;</span>);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            log.warn(<span class="string">&quot;调用路径2&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        order.setCreateTime(now);</span><br><span class="line">        order.setUpdateTime(now);</span><br><span class="line">        log.info(<span class="string">&quot;request body : &#123;&#125;&quot;</span>, order);</span><br><span class="line">        Map&lt;String, Object&gt; map = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;(<span class="number">16</span>);</span><br><span class="line">        map.put(<span class="string">&quot;order&quot;</span>, order);</span><br><span class="line">        <span class="keyword">return</span> map;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>现在项目在运行中，我们要查看 <code>save</code> 方法的链路，打开 <code>arthas</code> 并选择我们的工程，使用 <code>trace com.goku.order.rest.OrderRestController save</code> 命令并触发一次调用</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">Affect(class-cnt:1 , method-cnt:1) cost in 69 ms.</span><br><span class="line">`---ts=2020-01-18 22:40:56;thread_name=http-nio-8081-exec-1;id=14;is_daemon=true;priority=5;TCCL=org.springframework.boot.web.embedded.tomcat.TomcatEmbeddedWebappClassLoader@4f824872</span><br><span class="line">    `---[3.6313ms] com.goku.order.rest.OrderRestController:save()</span><br><span class="line">        +---[0.0164ms] com.goku.order.entity.Order:setId() #26</span><br><span class="line">        +---[0.0047ms] com.goku.order.entity.Order:getFlag() #28</span><br><span class="line">        +---[0.1695ms] org.slf4j.Logger:warn() #31</span><br><span class="line">        +---[0.0103ms] com.goku.order.entity.Order:setCreateTime() #33</span><br><span class="line">        +---[0.0062ms] com.goku.order.entity.Order:setUpdateTime() #34</span><br><span class="line">        `---[2.6388ms] org.slf4j.Logger:info() #35</span><br></pre></td></tr></table></figure>

<p>从输出上来看，我们可以很清楚看到代码的 26、28、31、33、34、35 行被执行了，同样的，如果 <code>order</code> 对象的 <code>flag</code> 属性如果为 true，则第 31 行的调用会变成第 29 行，如下：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">`---ts=2020-01-18 22:56:00;thread_name=http-nio-8081-exec-4;id=17;is_daemon=true;priority=5;TCCL=org.springframework.boot.web.embedded.tomcat.TomcatEmbeddedWebappClassLoader@4f824872</span><br><span class="line">    `---[2.1072ms] com.goku.order.rest.OrderRestController:save()</span><br><span class="line">        +---[0.0051ms] com.goku.order.entity.Order:setId() #26</span><br><span class="line">        +---[min=0.0023ms,max=0.0023ms,total=0.0046ms,count=2] com.goku.order.entity.Order:getFlag() #28</span><br><span class="line">        +---[0.6911ms] org.slf4j.Logger:info() #29</span><br><span class="line">        +---[0.0186ms] com.goku.order.entity.Order:setCreateTime() #33</span><br><span class="line">        +---[0.0032ms] com.goku.order.entity.Order:setUpdateTime() #34</span><br><span class="line">        `---[0.312ms] org.slf4j.Logger:info() #35</span><br></pre></td></tr></table></figure>

<p>从输出结果也可以清楚的看到代码在执行过程中，哪一行的代码调用耗时最长，这对我们排查线上对应的问题是非常有帮助的。</p>
<h3 id="包含-jdk-的函数"><a href="#包含-jdk-的函数" class="headerlink" title="包含 jdk 的函数"></a>包含 jdk 的函数</h3><p>如果需要将 <code>JDK</code> 的方法执行链路也输出，则使用 <code>--skipJDKMethod false</code> 参数，例如： <code>trace com.goku.order.rest.OrderRestController save --skipJDKMethod false</code>。</p>
<h3 id="根据调用耗时过滤"><a href="#根据调用耗时过滤" class="headerlink" title="根据调用耗时过滤"></a>根据调用耗时过滤</h3><p>如果需要只需要查看调用耗时比较长的，则添加对应的过滤条件，可以更快的帮助我们定位异常问题。比如只查询调用耗时超过 10ms 的方法：<code>trace com.goku.order.rest.OrderRestController save &#39;#cost &gt; 10&#39;</code></p>
]]></content>
      <categories>
        <category>手册</category>
      </categories>
      <tags>
        <tag>Arthas</tag>
      </tags>
  </entry>
  <entry>
    <title>夏令时问题</title>
    <url>/2019/10/01/daylight-saving-time/</url>
    <content><![CDATA[<p>在工作中，遇到一个很有意思的关于时间的问题，这里跟大家分享一下。</p>
<h2 id="现象"><a href="#现象" class="headerlink" title="现象"></a>现象</h2><p>从数据库里取出用户的生日<code>1990-05-22</code>，接口返回时发现返回值变成了<code>1990-05-21</code>，而对于<code>1992-05-22</code>这样的日期却能正确返回。</p>
<p>网上很多对于相关问题的分析都是简单粗暴的认为是时区的问题，却没有讲明白问题出现的根本原因。</p>
<h2 id="排查过程"><a href="#排查过程" class="headerlink" title="排查过程"></a>排查过程</h2><h3 id="问题复现"><a href="#问题复现" class="headerlink" title="问题复现"></a>问题复现</h3><p>为了复现问题现象，我们先快速搭建一个 Spring Boot Application。</p>
<p>先将配置文件中对日期类型的格式化配置为 <code>yyyy-MM-dd</code>，如下所示：</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring.jackson.time-zone</span>=<span class="string">GMT+8</span></span><br><span class="line"><span class="attr">spring.jackson.date-format</span>=<span class="string">yyyy-MM-dd</span></span><br></pre></td></tr></table></figure>

<p>写一个接口，直接将有问题的日期输出，代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">IndexController</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping(&quot;/dst&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> Map&lt;String, Object&gt; <span class="title function_">dst</span><span class="params">(<span class="meta">@RequestParam(&quot;date&quot;)</span> Date date)</span> &#123;</span><br><span class="line">        Map&lt;String, Object&gt; map = Maps.newHashMap();</span><br><span class="line">        System.out.println(<span class="string">&quot;birthday : &quot;</span> + DateFormatUtils.format(date, <span class="string">&quot;yyyy-MM-dd&quot;</span>));</span><br><span class="line">        map.put(<span class="string">&quot;birthday&quot;</span>, date);</span><br><span class="line">        <span class="keyword">return</span> map;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>构造如下请求：</p>
<figure class="highlight http"><table><tr><td class="code"><pre><span class="line">GET http://localhost:8081/dst?date=1990/05/22</span><br></pre></td></tr></table></figure>

<p>发现服务端日志正确输出<code>1990-05-22</code>，而接口返回值却输出：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span><span class="attr">&quot;birthday&quot;</span><span class="punctuation">:</span> <span class="string">&quot;1990-05-21&quot;</span><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>构造如下请求：</p>
<figure class="highlight http"><table><tr><td class="code"><pre><span class="line">GET http://localhost:8081/dst?date=1992/05/22</span><br></pre></td></tr></table></figure>

<p>发现服务端日志正确输出<code>1992-05-22</code>，此时接口的输出与服务端日志一致：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span><span class="attr">&quot;birthday&quot;</span><span class="punctuation">:</span> <span class="string">&quot;1992-05-22&quot;</span><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>至此，我们复现了本文要讲解的问题。</p>
<h3 id="输出对应的时间"><a href="#输出对应的时间" class="headerlink" title="输出对应的时间"></a>输出对应的时间</h3><p>为了更精确的知道这两个日期究竟有什么不同，我们更详细的输出这两个 <code>Date</code> 所对应的具体时间。</p>
<p>修改配置文件为：</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring.jackson.date-format</span>=<span class="string">yyyy-MM-dd HH:mm:ss</span></span><br></pre></td></tr></table></figure>

<p>重复以上请求，发现对于<code>1990-05-22</code>，服务端日志输出<code>1990-05-22 00:00:00</code>，而接口返回值则为<code>1990-05-21 23:00:00</code>，两者相差了一个小时。而对于<code>1992-05-22</code>，服务端日志与接口返回值完全一致。</p>
<h3 id="输出系统的时区"><a href="#输出系统的时区" class="headerlink" title="输出系统的时区"></a>输出系统的时区</h3><p>通过上述的现象基本上可以定位问题出现在 <code>Jackson</code> 对日期的序列化上。</p>
<p>为了排查这个问题，我们在启动 Spring Boot Application 时，先输出当前系统的时区，发现系统时区为 <code>Asia/Shanghai</code>，而我们配置的 <code>Jackson</code> 时区为 <code>GMT+8</code>。</p>
<p>难道 <code>Asia/Shanghai</code> 与 <code>GMT+8</code> 不是同一个时区？</p>
<h3 id="求证"><a href="#求证" class="headerlink" title="求证"></a>求证</h3><p>为了证明我们的猜想是正确的，在启动 Spring Boot Application 时，将系统时区设置为 <code>GMT+8</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">DSTApplication</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        SpringApplication.run(DSTApplication.class, args);</span><br><span class="line">        TimeZone.setDefault(TimeZone.getTimeZone(<span class="string">&quot;GMT+8&quot;</span>));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>重复以上请求，发现对于<code>1990-05-22</code>，服务端日志和接口均正确输出 <code>1990-05-22 00:00:00</code>。</p>
<p>若不设置默认时区为 <code>GMT+8</code>，而是将 <code>Jackson</code> 序列化的时区配置修改为 <code>Asia/Shanghai</code>，接口也能正确输出。</p>
<p>那么 <code>Asia/Shanghai</code> 与 <code>GMT+8</code> 在什么情况下会导致相差一个小时的问题呢？</p>
<h2 id="夏令时"><a href="#夏令时" class="headerlink" title="夏令时"></a>夏令时</h2><blockquote>
<p>夏令时，表示为了节约能源，人为规定时间的意思。也叫夏时制，夏时令（Daylight Saving Time：DST），又称“日光节约时制”和“夏令时间”，在这一制度实行期间所采用的统一时间称为“夏令时间”。一般在天亮早的夏季人为将时间调快一小时，可以使人早起早睡，减少照明量，以充分利用光照资源，从而节约照明用电。各个采纳夏时制的国家具体规定不同。目前全世界有近110个国家每年要实行夏令时。1986年4月，中国中央有关部门发出“在全国范围内实行夏时制的通知”，具体作法是：每年从四月中旬第一个星期日的凌晨2时整（北京时间），将时钟拨快一小时，即将表针由2时拨至3时，夏令时开始；到九月中旬第一个星期日的凌晨2时整（北京夏令时），再将时钟拨回一小时，即将表针由2时拨至1时，夏令时结束。从1986年到1991年的六个年度，除1986年因是实行夏时制的第一年，从5月4日开始到9月14日结束外，其它年份均按规定的时段施行。在夏令时开始和结束前几天，新闻媒体均刊登有关部门的通告。1992年起，夏令时暂停实行。</p>
</blockquote>
<p>以上内容摘抄自<a href="https://baike.baidu.com/item/%E5%A4%8F%E4%BB%A4%E6%97%B6">百度百科</a>，这也就是为什么在上述例子中为什么<code>1992-05-22</code>即使在系统时区与 <code>Jackson</code> 序列化时区不一致的情况下，仍然能够正确返回，而<code>1990-05-22</code>就会出错。</p>
]]></content>
      <tags>
        <tag>夏令时</tag>
      </tags>
  </entry>
  <entry>
    <title>使用 Docker 构建 Spring Boot Application</title>
    <url>/2019/06/25/dockerizing-spring-boot-application/</url>
    <content><![CDATA[<h2 id="Docker-amp-Spring-Boot"><a href="#Docker-amp-Spring-Boot" class="headerlink" title="Docker &amp; Spring Boot"></a>Docker &amp; Spring Boot</h2><p><code>Docker</code> 是一种 <code>Linux</code> 容器的实现，具有进程的轻量级资源隔离的特点，每一个容器对应操作系统中的一个进程，但是它又会有自己的网络空间、文件系统、PID 等。<code>Docker</code> 除了实现 <code>Linux</code> 容器，还使得容器具有“社会化”的属性，用户可以将容器镜像发布在 <code>Docker Hub</code> 与其他开发者共享、协作，类似于 <code>Maven</code>。</p>
<p><code>Spring Boot</code> 简化了 Spring 应用程序的开发过程，遵循约定优先配置的原则提供了各类 <em>开箱即用（out-of-the-box）</em> 的框架配置。另一方面，<code>Spring Boot</code> 还具备将代码直接构建为可执行 jar 包的能力，这个 jar 包是一个可以独立运行的部署单元。基于以上特性，现在普遍认为 <code>Spring Boot</code> 提供了一种快速构造<strong>微服务（Micro-Service）</strong>的能力。</p>
<p><code>Spring Boot</code> 的运行环境非常简单，将 Spring Boot Application 打包为 <code>Docker</code> 镜像，对于自动化部署、运维都非常方便。</p>
<h2 id="Dockerizing-Spring-Boot-Application"><a href="#Dockerizing-Spring-Boot-Application" class="headerlink" title="Dockerizing Spring Boot Application"></a>Dockerizing Spring Boot Application</h2><p>使用 Docker 发布 Spring Boot Application，需要经历简单的几步，即可实现。</p>
<p>如有需要，可参考本文所涉及 <a href="https://github.com/fuyongde/marvel/tree/master/thanos">Demo</a>。</p>
<h3 id="常规操作"><a href="#常规操作" class="headerlink" title="常规操作"></a>常规操作</h3><h4 id="编写-Spring-Boot-Application"><a href="#编写-Spring-Boot-Application" class="headerlink" title="编写 Spring Boot Application"></a>编写 Spring Boot Application</h4><p>本文不是为了讲解如何编写 Spring Boot Application，故建议读者直接采用脚手架快速构建出一个 Spring Boot Application。</p>
<h4 id="编写-Dockerfile"><a href="#编写-Dockerfile" class="headerlink" title="编写 Dockerfile"></a>编写 Dockerfile</h4><p><code>Dockerfile</code> 对于 <code>Docker</code> 的意义如同 <code>POM</code> 之于 <code>Maven</code>。</p>
<p>在 <code>pom.xml</code> 同级目录新建文件 <code>Dockerfile</code>。</p>
<figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 基础运行环境，建议采用 openjdk alpine 作为基础的镜像</span></span><br><span class="line"><span class="keyword">FROM</span> openjdk:<span class="number">8</span>-jdk-alpine</span><br><span class="line"></span><br><span class="line"><span class="comment"># 标记作者</span></span><br><span class="line"><span class="keyword">LABEL</span><span class="language-bash"> maintainer=<span class="string">&quot;fuyongde@foxmail.com&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 挂载 /tmp 目录，因为 Spring Boot 内嵌的 Tomcat 默认使用 /tmp 作为工作目录</span></span><br><span class="line"><span class="keyword">VOLUME</span><span class="language-bash"> [ <span class="string">&quot;/tmp&quot;</span> ]</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 容器对外映射 8080 端口</span></span><br><span class="line"><span class="keyword">EXPOSE</span> <span class="number">8080</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 定义要打包的 jar</span></span><br><span class="line"><span class="keyword">ARG</span> JAR_FILE=target/thanos-<span class="number">0.0</span>.<span class="number">1</span>-SNAPSHOT.jar</span><br><span class="line"></span><br><span class="line"><span class="comment"># 将指定的 jar 添加到目标镜像并重命名</span></span><br><span class="line"><span class="keyword">ADD</span><span class="language-bash"> <span class="variable">$&#123;JAR_FILE&#125;</span> thanos-0.0.1.jar</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用 java -jar 命令运行目标程序</span></span><br><span class="line"><span class="keyword">ENTRYPOINT</span><span class="language-bash"> [ <span class="string">&quot;java&quot;</span>, <span class="string">&quot;-jar&quot;</span>, <span class="string">&quot;/thanos-0.0.1.jar&quot;</span> ]</span></span><br></pre></td></tr></table></figure>

<ul>
<li><code>FROM</code>：指定一个构建镜像的基础源镜像，如果本地没有就会从公共库中拉取，没有指定镜像的标签会使用默认的 latest 标签，如果需要在一个 <code>Dockerfile</code> 中构建多个镜像，该选项可以出现多次。</li>
<li><code>LABEL</code>：为镜像指定标签</li>
<li><code>VOLUME</code>：可实现挂载功能，可以将内地文件夹或者其他容器种得文件夹挂在到这个容器种</li>
<li><code>EXPOSE</code>：暴漏容器运行时的监听端口给外部，但是 EXPOSE 并不会使容器访问主机的端口，如果想使得容器与主机的端口有映射关系，必须在容器启动的时候加上 <code>-p</code> 参数</li>
<li><code>ARG</code>：设置变量</li>
<li><code>ADD</code>：将文件添加到目标镜像中</li>
<li><code>ENTRYPOINT</code>：启动时的默认命令</li>
</ul>
<h4 id="构建-Docker-镜像"><a href="#构建-Docker-镜像" class="headerlink" title="构建 Docker 镜像"></a>构建 Docker 镜像</h4><p>由于 <code>Docker</code> 依赖我们程序编译的结果，故在构建 <code>Docker</code> 镜像之前，需要先构建我们的工程。</p>
<p>如本文所讲例子，需要依次执行</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">采用 Maven 构建 Spring Boot Application</span></span><br><span class="line">mvnw install</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">构建 Docker Image</span></span><br><span class="line">docker build -t thanos .</span><br></pre></td></tr></table></figure>

<p>构建完成之后，可以通过 <code>docker image ls</code> 查看。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">REPOSITORY    TAG       IMAGE ID        CREATED              SIZE</span><br><span class="line">thanos        latest    5f30b11b5088    About an hour ago    122MB</span><br></pre></td></tr></table></figure>

<h4 id="运行-Docker-镜像"><a href="#运行-Docker-镜像" class="headerlink" title="运行 Docker 镜像"></a>运行 Docker 镜像</h4><p>和正常的运行 Docker 镜像的命令一致。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker run -p 8080:8080 thanos</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">也可以采用后台运行的方式运行容器</span></span><br><span class="line">docker run -d -p 8080:8080 thanos</span><br></pre></td></tr></table></figure>

<p>运行之后，通过 <code>docker ps</code> 命令来查看当前运行的容器。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CONTAINER ID   IMAGE    COMMAND                  CREATED             STATUS             PORTS                    NAMES</span><br><span class="line">9f20a76e22fa   thanos   &quot;java -jar /thanos-0…&quot;   About an hour ago   Up About an hour   0.0.0.0:8080-&gt;8080/tcp   xenodochial_noyce</span><br></pre></td></tr></table></figure>

<h4 id="将-Docker-镜像推送到镜像仓库"><a href="#将-Docker-镜像推送到镜像仓库" class="headerlink" title="将 Docker 镜像推送到镜像仓库"></a>将 Docker 镜像推送到镜像仓库</h4><p>由于众所周知的原因，我们访问 <code>docker hub</code> 并不稳定，而且速度比较慢，本文采用阿里云的容器镜像服务。</p>
<ol>
<li>登录到阿里云容器镜像服务</li>
</ol>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">登录</span></span><br><span class="line">docker login --username=fuyongde@foxmail.com registry.cn-hangzhou.aliyuncs.com</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">输入密码，当看到 Login Succeeded 表示登录成功</span></span><br></pre></td></tr></table></figure>

<ol start="2">
<li>为当前镜像打 <code>tag</code></li>
</ol>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker tag thanos registry.cn-hangzhou.aliyuncs.com/fuyongde/thanos:v1</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>推送到容器镜像服务</li>
</ol>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker push registry.cn-hangzhou.aliyuncs.com/fuyongde/thanos:v1</span><br></pre></td></tr></table></figure>

<p>按照上述步骤操作完成之后，登录到阿里云的<code>控制台</code> &gt; <code>容器镜像服务</code> &gt; <code>镜像仓库</code> 即可查看已经推送好的镜像。</p>
<h3 id="dockerfile-maven-plugin"><a href="#dockerfile-maven-plugin" class="headerlink" title="dockerfile-maven-plugin"></a>dockerfile-maven-plugin</h3><p>经过上述的操作我们发现，要将我们的 Spring Boot Application 打包为 Docker 镜像，必须先将我们的工程构建，然后才能执行 Docker 的 <code>build</code>、<code>push</code>。那么能否将工程的构建与 Docker 的操作合二为一呢？</p>
<p>答案是肯定的，这就需要用到 <code>dockerfile-maven-plugin</code>。</p>
<h4 id="引入-dockerfile-maven-plugin"><a href="#引入-dockerfile-maven-plugin" class="headerlink" title="引入 dockerfile-maven-plugin"></a>引入 dockerfile-maven-plugin</h4><p>基于上述工程，在 <code>pom.xml</code> 中添加插件：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">plugin</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.spotify<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>dockerfile-maven-plugin<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.4.10<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">configuration</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">repository</span>&gt;</span>registry.cn-hangzhou.aliyuncs.com/fuyongde/thanos<span class="tag">&lt;/<span class="name">repository</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">tag</span>&gt;</span>$&#123;project.version&#125;<span class="tag">&lt;/<span class="name">tag</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">useMavenSettingsForAuth</span>&gt;</span>true<span class="tag">&lt;/<span class="name">useMavenSettingsForAuth</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">configuration</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">executions</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">execution</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">id</span>&gt;</span>default<span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">phase</span>&gt;</span>install<span class="tag">&lt;/<span class="name">phase</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">goals</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">goal</span>&gt;</span>build<span class="tag">&lt;/<span class="name">goal</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">goal</span>&gt;</span>push<span class="tag">&lt;/<span class="name">goal</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">goals</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">execution</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">executions</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">plugin</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>其中：</p>
<ul>
<li>repository：表示要上传的容器镜像服务，可以是阿里云、Docker Hub 等</li>
<li>tag：镜像打包的 tag</li>
<li>execution：这个标签下的配置表示，在 <code>Maven</code> 的 <code>install</code> 阶段，进行 <code>Docker</code> 的 <code>build</code>、<code>push</code> 操作</li>
<li>useMavenSettingsForAuth：表示使用 <code>Maven</code> 的配置的账号密码来认证 <code>Docker</code> 容器镜像服务，需要在 <code>Maven</code> 的 <code>settings.xml</code> 文件中加入以下配置：</li>
</ul>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">servers</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">server</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">id</span>&gt;</span>registry.cn-hangzhou.aliyuncs.com<span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">username</span>&gt;</span>username<span class="tag">&lt;/<span class="name">username</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">password</span>&gt;</span>password<span class="tag">&lt;/<span class="name">password</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">server</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">servers</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="构建"><a href="#构建" class="headerlink" title="构建"></a>构建</h4><p>执行 <code>mvn install</code> 命令，<code>Docker</code> 镜像就打包并上传好了 ✌。</p>
<h2 id="One-more-thing"><a href="#One-more-thing" class="headerlink" title="One more thing"></a>One more thing</h2><p>你以为上述构建 <code>Docker</code> 镜像方法就非常方便了吗？</p>
<p>Too young!</p>
<p><a href="https://fuyongde.github.io/2019/06/26/jib-make-docker-simple-again/">下一篇</a>，带你了解 <code>Jib</code>，Make docker simple again✨!</p>
]]></content>
      <tags>
        <tag>Docker</tag>
        <tag>Spring Boot</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker 手册</title>
    <url>/2019/11/14/docker-%E6%89%8B%E5%86%8C/</url>
    <content><![CDATA[<h2 id="镜像管理"><a href="#镜像管理" class="headerlink" title="镜像管理"></a>镜像管理</h2><h3 id="查询所需的镜像"><a href="#查询所需的镜像" class="headerlink" title="查询所需的镜像"></a>查询所需的镜像</h3><ul>
<li><p>Usage：</p>
<p><code>docker search [OPTIONS] TERM</code></p>
</li>
<li><p>Options：</p>
<p><code>-f --filter</code> ：根据提供的条件过滤输出</p>
<p><code>--format</code> ：使用 Go 模板进行漂亮的打印搜索</p>
<p><code>--limit</code> ：最大搜索结果数（默认为 25）</p>
<p><code>--no-trunc</code>：不要截断输出</p>
</li>
<li><p>Example：</p>
</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker search mysql</span><br></pre></td></tr></table></figure>

<h3 id="安装镜像"><a href="#安装镜像" class="headerlink" title="安装镜像"></a>安装镜像</h3><ul>
<li><p>Usage：</p>
<p><code>docker pull [OPTIONS] NAME[:TAG|@DIGEST]</code></p>
</li>
<li><p>Options：</p>
<p><code>-a --all-tags</code>：下载存储库中的所有标记镜像</p>
<p><code>--disable-content-trust</code>：跳过图像验证（默认为 true）</p>
</li>
<li><p>Example：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">安装MySQL的5.7.24版本</span></span><br><span class="line">docker pull mysql:5.7.24</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">缺省TAG时默认安装最新版本</span></span><br><span class="line">docker pull mysql</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">安装所有版本</span></span><br><span class="line">docker pull -a mysql</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">安装zookeeper</span></span><br><span class="line">docker pull zookeeper</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="查看镜像信息"><a href="#查看镜像信息" class="headerlink" title="查看镜像信息"></a>查看镜像信息</h3><p>Usage：</p>
<p><code>docker images [OPTIONS] [REPOSITORY[:TAG]]</code></p>
<p>Options:</p>
<p><code>-a, --all</code>：展示全部镜像<br>​ <code>--digests</code>：展示镜像的哈希值<br><code>-f, --filter filter</code>：根据提供的条件过滤输出<br>​ <code>--format string</code>：使用 Go 模板的漂亮打印图像<br>​ <code>--no-trunc</code>：不要截断输出<br><code>-q, --quiet</code>：仅显示数字 ID</p>
<p>Example:</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">查看所有的进行信息</span></span><br><span class="line">docker images -a</span><br></pre></td></tr></table></figure>

<h3 id="删除镜像"><a href="#删除镜像" class="headerlink" title="删除镜像"></a>删除镜像</h3><p>Usage：</p>
<p><code>docker rmi [OPTIONS] IMAGE [IMAGE...]</code></p>
<p>Options:<br><code>-f, --force</code>：强制删除某个镜像<br><code>--no-prune</code> ：不删除未标记的父类</p>
<p>Example：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">删除<span class="built_in">id</span>为cd14cecfdb3a的镜像</span></span><br><span class="line">docker rmi cd14cecfdb3a</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">删除jenkins镜像</span></span><br><span class="line">docker rmi jenkins</span><br></pre></td></tr></table></figure>

<h2 id="容器管理"><a href="#容器管理" class="headerlink" title="容器管理"></a>容器管理</h2><h3 id="查看容器"><a href="#查看容器" class="headerlink" title="查看容器"></a>查看容器</h3><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">查看所有正在运行的容器</span></span><br><span class="line">docker ps</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">查看所有容器</span></span><br><span class="line">docker ps -a</span><br><span class="line">查看所有容器ID</span><br><span class="line">docker ps -a -q</span><br></pre></td></tr></table></figure>

<h3 id="启动容器"><a href="#启动容器" class="headerlink" title="启动容器"></a>启动容器</h3><p><code>docker run IMG</code></p>
<p>Example：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">启动MySQL</span></span><br><span class="line">docker run -p 3306:3306 --name MySQL -e MYSQL_ROOT_PASSWORD=fuyongde mysql:5.7.24</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">启动zookeeper</span></span><br><span class="line">docker run -p 2181:2181 --name zookeeper zookeeper</span><br></pre></td></tr></table></figure>

<h3 id="停止容器"><a href="#停止容器" class="headerlink" title="停止容器"></a>停止容器</h3><p><code>docker stop [containerId]</code>：停止容器</p>
<p>Example:</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">删除<span class="built_in">id</span>为8fcc3dad236a的容器</span></span><br><span class="line">docker rm 8fcc3dad236a</span><br></pre></td></tr></table></figure>

<h3 id="重启容器"><a href="#重启容器" class="headerlink" title="重启容器"></a>重启容器</h3><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">重启<span class="built_in">id</span>为1e4b2a31028d的容器</span></span><br><span class="line">docker restart 1e4b2a31028d</span><br></pre></td></tr></table></figure>

<h3 id="删除容器"><a href="#删除容器" class="headerlink" title="删除容器"></a>删除容器</h3><p><code>docker rm [containerId]</code></p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">删除<span class="built_in">id</span>为1e4b2a31028d的容器</span></span><br><span class="line">docker rm 1e4b2a31028d</span><br></pre></td></tr></table></figure>

<h3 id="重命名容器"><a href="#重命名容器" class="headerlink" title="重命名容器"></a>重命名容器</h3><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">将docker-mysql重命名为mysql</span></span><br><span class="line">docker rename docker-mysql mysql</span><br></pre></td></tr></table></figure>

<h2 id="其他命令"><a href="#其他命令" class="headerlink" title="其他命令"></a>其他命令</h2><h3 id="进入到一个容器"><a href="#进入到一个容器" class="headerlink" title="进入到一个容器"></a>进入到一个容器</h3><p>通常我们使用的 docker 镜像一般是基于 <code>Linux</code>，则可以通过下面的方式进入到 docker 容器内:</p>
<p><code>docker exec -it 063caee1d235 /bin/bash</code></p>
<p>但是对于<code>Alpinelinux</code>，则需：</p>
<p><code>docker exec -it 063caee1d235 sh</code></p>
<p>对于部分镜像默认用户不是 <code>root</code> 的（比如 <code>Jenkins</code> 官方提供的 docker 镜像），但我们又需要使用 <code>root</code> 账户的，可以通过添加 <code>-u root</code> 参数来使用 <code>root</code> 账户登录 docker 容器：</p>
<p><code>docker exec -it -u root a07fe9b55ade bash</code></p>
<h3 id="拷贝文件到一个容器"><a href="#拷贝文件到一个容器" class="headerlink" title="拷贝文件到一个容器"></a>拷贝文件到一个容器</h3><p><code>docker cp C:\Users\fuyon\Documents\repo\github\goku\atomikos-demo\zhifubao-server\src\main\resources\sql\zhifubao.sql 1e4b2a31028d:/tmp</code></p>
<h3 id="从容器拷贝文件到主机"><a href="#从容器拷贝文件到主机" class="headerlink" title="从容器拷贝文件到主机"></a>从容器拷贝文件到主机</h3><p><code>docker cp 1e4b2a31028d:/tmp/zhifubao.sql C:\Users\fuyon\Documents</code></p>
]]></content>
      <categories>
        <category>手册</category>
      </categories>
      <tags>
        <tag>Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>RPC 进阶 - 线程池</title>
    <url>/2019/07/16/edith-with-thread-pool/</url>
    <content><![CDATA[<p>在 <a href="https://fuyongde.github.io/2019/07/11/rpc-introduction/">Rpc Introduction</a> 中简单写了一个 <code>RPC Framework</code>。在本节中，对 <code>Edith</code> 新增采用线程池支持多线程的特性。</p>
<h2 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h2><p>实际开发中，我们的应用程序是不能盲目的开辟线程的，这样会造成资源的极大浪费。若采用线程池，可以减少创建和销毁线程的次数，让每个线程可以多次使用，并且可以根据系统的承受能力，调整线程池中工作线程的数量，方式消耗过多的内存。</p>
<p>如有需要可以阅读 <code>Dubbo</code> 中关于线程池的设计（位于 <code>org.apache.dubbo.common.threadpool</code> 包）。</p>
<h2 id="开发"><a href="#开发" class="headerlink" title="开发"></a>开发</h2><h3 id="依赖"><a href="#依赖" class="headerlink" title="依赖"></a>依赖</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;dependency&gt;</span><br><span class="line">    &lt;groupId&gt;com.google.guava&lt;/groupId&gt;</span><br><span class="line">    &lt;artifactId&gt;guava&lt;/artifactId&gt;</span><br><span class="line">&lt;/dependency&gt;</span><br></pre></td></tr></table></figure>

<h3 id="Code"><a href="#Code" class="headerlink" title="Code"></a>Code</h3><p>本文代码可以从<a href="https://github.com/fuyongde/edith/releases/tag/0.1.1">这里</a>获取。</p>
<h4 id="新增-ThreadUtils-java"><a href="#新增-ThreadUtils-java" class="headerlink" title="新增 ThreadUtils.java"></a>新增 ThreadUtils.java</h4><p>用于创建 <code>ExecutorService</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ThreadUtils</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 用于对线程分配一个名字，方便排查问题</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> format format</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> Edith-1</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> ThreadFactory <span class="title function_">buildThreadFactory</span><span class="params">(String format)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">ThreadFactoryBuilder</span>().setNameFormat(format).build();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> ExecutorService <span class="title function_">newCachedThreadPool</span><span class="params">(<span class="type">int</span> maximumPoolSize)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">ThreadPoolExecutor</span>(THREAD_CORE_POOL_SIZE, maximumPoolSize &gt; <span class="number">0</span> ? maximumPoolSize : THREAD_MAXIMUM_POOL_SIZE,</span><br><span class="line">                Long.MAX_VALUE, TimeUnit.MICROSECONDS, <span class="keyword">new</span> <span class="title class_">SynchronousQueue</span>&lt;&gt;(), buildThreadFactory(THREAD_NAME), <span class="keyword">new</span> <span class="title class_">ThreadPoolExecutor</span>.AbortPolicy());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="修改-RpcFramework-java"><a href="#修改-RpcFramework-java" class="headerlink" title="修改 RpcFramework.java"></a>修改 RpcFramework.java</h4><p>将原代码中 <code>new Thread()</code> 部分替换为线程池。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">ExecutorService</span> <span class="variable">executorService</span> <span class="operator">=</span> ThreadUtils.newCachedThreadPool(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">export</span><span class="params">(<span class="keyword">final</span> Object service, <span class="type">int</span> port)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">    Objects.requireNonNull(service, <span class="string">&quot;Service instance is null!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (port &lt;= PORT_MIN || port &gt; PORT_MAX) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">&quot;Invalid port &quot;</span> + port);</span><br><span class="line">    &#125;</span><br><span class="line">    System.out.println(<span class="string">&quot;Export service &quot;</span> + service.getClass().getName() + <span class="string">&quot; on port &quot;</span> + port);</span><br><span class="line">    <span class="type">ServerSocket</span> <span class="variable">server</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ServerSocket</span>(port);</span><br><span class="line">    <span class="keyword">for</span> (; ; ) &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;application is running&quot;</span>);</span><br><span class="line">        <span class="type">Socket</span> <span class="variable">socket</span> <span class="operator">=</span> server.accept();</span><br><span class="line">        executorService.execute(() -&gt; &#123;</span><br><span class="line">            <span class="comment">// do something ...</span></span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>经过以上修改，我们在例子中的 <code>HelloServiceImpl</code> 打印一下线程名称</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">hello</span><span class="params">(String name)</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> <span class="string">&quot;Hello &quot;</span> + name;</span><br><span class="line">    System.out.println(Thread.currentThread().getName() + <span class="string">&quot; &quot;</span> + result);</span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>启动 <code>provider</code>，然后用 <code>consumer</code> 调用一次，观察 <code>provider</code> 的控制台输出</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Edith-0 Hello Peter Parker</span><br></pre></td></tr></table></figure>

<p>Done.</p>
]]></content>
      <tags>
        <tag>rpc</tag>
      </tags>
  </entry>
  <entry>
    <title>Dubbo 中的负载均衡策略</title>
    <url>/2019/11/16/dubbo-loadbalance/</url>
    <content><![CDATA[<p><code>Dubbo</code> 内置了 4 种负载均衡策略</p>
<ul>
<li><code>RandomLoadBalance</code>：随机负载均衡，随机选择一个服务结点，跟权重相关。该策略是 <code>Dubbo</code> 的<strong>默认</strong>负载均衡策略。</li>
<li><code>RoundRobinLoadBalance</code>：轮询负载均衡，轮询选择一个服务结点，跟权重相关。</li>
<li><code>LeastActiveLoadBalance</code>：最少活跃调用数，相同的活跃数随机选择一个结点，跟权重相关。实现方式是对活跃数值前后计数差。使得慢的结点收到更少的请求，因为越慢的结点前后计数差越大。</li>
<li><code>ConsistentHashLoadBalance</code>：一致性哈希负载均衡，即相同的参数的请求总是落在同一个服务结点上。</li>
</ul>
<p>通过观察类之间依赖图，可以快速帮我们梳理其之间的关系，如下图所示：</p>


<h2 id="实践"><a href="#实践" class="headerlink" title="实践"></a>实践</h2><blockquote>
<p>纸上得来终觉浅，觉知此时要躬行。</p>
</blockquote>
<p><code>Dubbo</code> 中负载均衡的源码可自行通过源码来学习，为了加深印象，这里我以 <code>Dubbo</code> 源码中的思路来实现这四个负载均衡策略。</p>
<h3 id="准备工作"><a href="#准备工作" class="headerlink" title="准备工作"></a>准备工作</h3><h4 id="定义结点接口"><a href="#定义结点接口" class="headerlink" title="定义结点接口"></a>定义结点接口</h4><p>这里我们不实现 <code>Dubbo</code> 中的 <code>Invoker</code>，以一个结点类 <code>Endpoint</code> 代替。</p>
<p>Endpoint.java</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">Endpoint</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * hello方法</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> param 参数</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">hello</span><span class="params">(String param)</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 获取结点名称</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> 结点名称</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    String <span class="title function_">name</span><span class="params">()</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 获取结点权重</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> 结点权重</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="type">int</span> <span class="title function_">weight</span><span class="params">()</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="实现多个结点"><a href="#实现多个结点" class="headerlink" title="实现多个结点"></a>实现多个结点</h4><p>这里我们分别实现 <code>S0</code>, <code>S1</code>, <code>S2</code> 三个结点，代码如下：</p>
<p>S0Endpoint.java</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">S0Endpoint</span> <span class="keyword">implements</span> <span class="title class_">Endpoint</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">hello</span><span class="params">(String param)</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;S0 -&gt; &quot;</span> + param);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">name</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;S0&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">weight</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">3</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>S1Endpoint.java</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">S1Endpoint</span> <span class="keyword">implements</span> <span class="title class_">Endpoint</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">hello</span><span class="params">(String param)</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;S1 -&gt; &quot;</span> + param);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">name</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;S1&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">weight</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">2</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>S2Endpoint.java</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">S2Endpoint</span> <span class="keyword">implements</span> <span class="title class_">Endpoint</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">hello</span><span class="params">(String param)</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;S2 -&gt; &quot;</span> + param);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">name</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;S2&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">weight</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="定义负载均衡的接口以及抽象类"><a href="#定义负载均衡的接口以及抽象类" class="headerlink" title="定义负载均衡的接口以及抽象类"></a>定义负载均衡的接口以及抽象类</h4><p>LoadBalance.java</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">LoadBalance</span> &#123;</span><br><span class="line"></span><br><span class="line">    Endpoint <span class="title function_">select</span><span class="params">(List&lt;Endpoint&gt; endpoints, String param)</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>AbstractLoadBalance.java</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">class</span> <span class="title class_">AbstractLoadBalance</span> <span class="keyword">implements</span> <span class="title class_">LoadBalance</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> Endpoint <span class="title function_">select</span><span class="params">(List&lt;Endpoint&gt; endpoints, String param)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (Objects.isNull(endpoints) || endpoints.size() == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">RuntimeException</span>(<span class="string">&quot;没有可用的结点&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (endpoints.size() == <span class="number">1</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> endpoints.get(<span class="number">0</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> doSelect(endpoints, param);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">abstract</span> Endpoint <span class="title function_">doSelect</span><span class="params">(List&lt;Endpoint&gt; endpoints, String param)</span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> <span class="title function_">getWeight</span><span class="params">(Endpoint endpoint)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> endpoint.weight();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="程序入口"><a href="#程序入口" class="headerlink" title="程序入口"></a>程序入口</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">App</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        List&lt;Endpoint&gt; endpoints = Lists.newArrayList(<span class="keyword">new</span> <span class="title class_">S0Endpoint</span>(), <span class="keyword">new</span> <span class="title class_">S1Endpoint</span>(), <span class="keyword">new</span> <span class="title class_">S2Endpoint</span>());</span><br><span class="line">        <span class="comment">// 需要演示哪种负载均衡策略，替换为对应的实现类即可</span></span><br><span class="line">        <span class="type">LoadBalance</span> <span class="variable">loadBalance</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">RoundRobinLoadBalance</span>();</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">18</span>; i++) &#123;</span><br><span class="line">            <span class="type">Endpoint</span> <span class="variable">endpoint</span> <span class="operator">=</span> loadBalance.select(endpoints, <span class="string">&quot;LoanBance&quot;</span>);</span><br><span class="line">            endpoint.hello(<span class="string">&quot;LoadBance&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="随机负载均衡"><a href="#随机负载均衡" class="headerlink" title="随机负载均衡"></a>随机负载均衡</h3><p><code>Dubbo</code> 中随机负载均衡策略，会受每个服务结点权重的影响。由于随机负载均衡策略的实现比较简单，每一步的作用，可查看注释，这里就不详细讲解。代码如下：</p>
<p>RandomLoadBalance.java</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">RandomLoadBalance</span> <span class="keyword">extends</span> <span class="title class_">AbstractLoadBalance</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> Endpoint <span class="title function_">doSelect</span><span class="params">(List&lt;Endpoint&gt; endpoints, String param)</span> &#123;</span><br><span class="line">        <span class="comment">// 先获取结点总数</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">length</span> <span class="operator">=</span> endpoints.size();</span><br><span class="line">        <span class="comment">// 每个结点权重是否相等</span></span><br><span class="line">        <span class="type">boolean</span> <span class="variable">sameWeight</span> <span class="operator">=</span> <span class="literal">true</span>;</span><br><span class="line">        <span class="comment">// 每个计算器的权重</span></span><br><span class="line">        <span class="type">int</span>[] weights = <span class="keyword">new</span> <span class="title class_">int</span>[length];</span><br><span class="line">        <span class="comment">// 获取第一个计算器的权重，Dubbo 中权重会从 Invoker 中取</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">firstWeight</span> <span class="operator">=</span> getWeight(endpoints.get(<span class="number">0</span>));</span><br><span class="line">        weights[<span class="number">0</span>] = firstWeight;</span><br><span class="line">        <span class="type">int</span> <span class="variable">totalWeight</span> <span class="operator">=</span> firstWeight;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">1</span>; i &lt; length; i++) &#123;</span><br><span class="line">            <span class="comment">// 获取后续每一个结点的权重，用于判断所有结点权重是否相等</span></span><br><span class="line">            <span class="type">int</span> <span class="variable">weight</span> <span class="operator">=</span> getWeight(endpoints.get(i));</span><br><span class="line">            weights[i] = weight;</span><br><span class="line">            totalWeight += weight;</span><br><span class="line">            <span class="keyword">if</span> (sameWeight &amp;&amp; weight != firstWeight) &#123;</span><br><span class="line">                sameWeight = <span class="literal">false</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (totalWeight &gt; <span class="number">0</span> &amp;&amp; !sameWeight) &#123;</span><br><span class="line">            <span class="comment">// 若各个结点权重不相等，则随机一个[0~总权重数]之间的数值</span></span><br><span class="line">            <span class="type">int</span> <span class="variable">offset</span> <span class="operator">=</span> ThreadLocalRandom.current().nextInt(totalWeight);</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; length; i++) &#123;</span><br><span class="line">                offset -= weights[i];</span><br><span class="line">                <span class="keyword">if</span> (offset &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="keyword">return</span> endpoints.get(i);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> endpoints.get(ThreadLocalRandom.current().nextInt(length));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>将程序入口中的负载均衡替换为随机负载均衡的实现类，其输出如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">S0 -&gt; LoadBance</span><br><span class="line">S0 -&gt; LoadBance</span><br><span class="line">S2 -&gt; LoadBance</span><br><span class="line">S0 -&gt; LoadBance</span><br><span class="line">S0 -&gt; LoadBance</span><br><span class="line">S0 -&gt; LoadBance</span><br><span class="line">S2 -&gt; LoadBance</span><br><span class="line">S0 -&gt; LoadBance</span><br><span class="line">S0 -&gt; LoadBance</span><br><span class="line">S1 -&gt; LoadBance</span><br><span class="line">S1 -&gt; LoadBance</span><br><span class="line">S0 -&gt; LoadBance</span><br><span class="line">S1 -&gt; LoadBance</span><br><span class="line">S0 -&gt; LoadBance</span><br><span class="line">S1 -&gt; LoadBance</span><br><span class="line">S0 -&gt; LoadBance</span><br><span class="line">S0 -&gt; LoadBance</span><br><span class="line">S1 -&gt; LoadBance</span><br></pre></td></tr></table></figure>

<p>可发现其调用每个结点的规律大致与所占权重的比例相同。</p>
<h3 id="轮询负载均衡"><a href="#轮询负载均衡" class="headerlink" title="轮询负载均衡"></a>轮询负载均衡</h3><p>轮询负载均衡就是依次调用所有的服务结点，和随机负载均衡一样轮询负载均衡也有权重的概念。轮询负载均衡算法可以让服务调用严格的按照我们设置的权重比例来分配，并且跟调用的次数无关。缺点是慢的结点会累计请求，从而导致整个系统变慢。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">RoundRobinLoadBalance</span> <span class="keyword">extends</span> <span class="title class_">AbstractLoadBalance</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/** 在内存中存储所有的 服务名+方法 **/</span></span><br><span class="line">    <span class="keyword">private</span> ConcurrentMap&lt;String, ConcurrentMap&lt;String, WeightedRoundRobin&gt;&gt; methodWeightMap = <span class="keyword">new</span> <span class="title class_">ConcurrentHashMap</span>&lt;&gt;();</span><br><span class="line">    <span class="comment">/** 保证原子性 **/</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">AtomicBoolean</span> <span class="variable">updateLock</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">AtomicBoolean</span>();</span><br><span class="line">    <span class="comment">/** 默认的回收期，设置为1分钟 **/</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">RECYCLE_PERIOD</span> <span class="operator">=</span> <span class="number">60000</span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> Endpoint <span class="title function_">doSelect</span><span class="params">(List&lt;Endpoint&gt; endpoints, String param)</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;-----------------------------------------------------------------&quot;</span>);</span><br><span class="line">        <span class="comment">// Dubbo 中这个 Key 值是通过解析 Invoker 被调用的服务URL+方法名以及对应的参数而拼装的字符串，这里为了演示，直接写死</span></span><br><span class="line">        <span class="type">String</span> <span class="variable">key</span> <span class="operator">=</span> <span class="string">&quot;com.exp.Endpoint.add&quot;</span>;</span><br><span class="line">        ConcurrentMap&lt;String, WeightedRoundRobin&gt; map = methodWeightMap.get(key);</span><br><span class="line">        <span class="keyword">if</span> (Objects.isNull(map)) &#123;</span><br><span class="line">            <span class="comment">// 若没有该 key 值的权重信息，则说明是第一次调用，创建新的放入methodWeightMap即可</span></span><br><span class="line">            methodWeightMap.putIfAbsent(key, <span class="keyword">new</span> <span class="title class_">ConcurrentHashMap</span>&lt;&gt;());</span><br><span class="line">            map = methodWeightMap.get(key);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 总权重</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">totalWeight</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">        <span class="comment">// 当前最大权重，初始值设为 Long 类型最小值</span></span><br><span class="line">        <span class="type">long</span> <span class="variable">maxCurrent</span> <span class="operator">=</span> Long.MIN_VALUE;</span><br><span class="line">        <span class="comment">// 记录当前时间</span></span><br><span class="line">        <span class="type">long</span> <span class="variable">now</span> <span class="operator">=</span> System.currentTimeMillis();</span><br><span class="line">        <span class="comment">// 被选择的结点</span></span><br><span class="line">        <span class="type">Endpoint</span> <span class="variable">selectedEndpoint</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">        <span class="comment">// 被选择的结点的权重轮询信息</span></span><br><span class="line">        <span class="type">WeightedRoundRobin</span> <span class="variable">selectedWRR</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">        <span class="keyword">for</span> (Endpoint endpoint : endpoints) &#123;</span><br><span class="line">            <span class="type">String</span> <span class="variable">name</span> <span class="operator">=</span> endpoint.name();</span><br><span class="line">            <span class="type">WeightedRoundRobin</span> <span class="variable">weightedRoundRobin</span> <span class="operator">=</span> map.get(name);</span><br><span class="line">            <span class="type">int</span> <span class="variable">weight</span> <span class="operator">=</span> getWeight(endpoint);</span><br><span class="line">            <span class="keyword">if</span> (Objects.isNull(weightedRoundRobin)) &#123;</span><br><span class="line">                weightedRoundRobin = <span class="keyword">new</span> <span class="title class_">WeightedRoundRobin</span>();</span><br><span class="line">                weightedRoundRobin.setWeight(weight);</span><br><span class="line">                map.putIfAbsent(name, weightedRoundRobin);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (weight != weightedRoundRobin.getWeight()) &#123;</span><br><span class="line">                weightedRoundRobin.setWeight(weight);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 对权重进行加权操作（其实就上次内存中该节点的权重 + 该节点的真实权重），计算出该次调用该节点的权重</span></span><br><span class="line">            <span class="type">long</span> <span class="variable">cur</span> <span class="operator">=</span> weightedRoundRobin.increaseCurrent();</span><br><span class="line">            System.out.println(String.format(<span class="string">&quot;节点%s -&gt; 内存中权重：%s，真实权重：%s，该次调用权重：%s，当前最大权重：%s&quot;</span>, name, weightedRoundRobin.current, weight, cur, maxCurrent));</span><br><span class="line">            weightedRoundRobin.setLastUpdate(now);</span><br><span class="line">            <span class="keyword">if</span> (cur &gt; maxCurrent) &#123;</span><br><span class="line">                maxCurrent = cur;</span><br><span class="line">                selectedEndpoint = endpoint;</span><br><span class="line">                selectedWRR = weightedRoundRobin;</span><br><span class="line">            &#125;</span><br><span class="line">            totalWeight += weight;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (!updateLock.get() &amp;&amp; endpoints.size() != map.size()) &#123;</span><br><span class="line">            <span class="comment">// 若 methodWeightMap 中该该服务+方法的节点数据量与内存中的不一致</span></span><br><span class="line">            <span class="keyword">if</span> (updateLock.compareAndSet(<span class="literal">false</span>, <span class="literal">true</span>)) &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    ConcurrentMap&lt;String, WeightedRoundRobin&gt; newMap = <span class="keyword">new</span> <span class="title class_">ConcurrentHashMap</span>&lt;&gt;();</span><br><span class="line">                    newMap.entrySet().removeIf(item -&gt; now - item.getValue().getLastUpdate() &gt; RECYCLE_PERIOD);</span><br><span class="line">                    methodWeightMap.put(key, newMap);</span><br><span class="line">                &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                    updateLock.set(<span class="literal">false</span>);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (Objects.nonNull(selectedEndpoint)) &#123;</span><br><span class="line">            <span class="type">long</span> <span class="variable">tmpWeight</span> <span class="operator">=</span> selectedWRR.current.get();</span><br><span class="line">            selectedWRR.sel(totalWeight);</span><br><span class="line">            System.out.println(String.format(<span class="string">&quot;本地调用选择 %s，同时调整 %s 节点在内存中的权重为 %s - %s = %s&quot;</span>, selectedEndpoint.name(), selectedEndpoint.name(), tmpWeight, totalWeight, selectedWRR.current));</span><br><span class="line">            System.out.println(<span class="string">&quot;此时内存中各节点权重：&quot;</span>);</span><br><span class="line">            map.entrySet().forEach(item-&gt; System.out.println(String.format(<span class="string">&quot;  %s -&gt; 权重：%s&quot;</span>, item.getKey(), item.getValue().current)));</span><br><span class="line"></span><br><span class="line">            System.out.println(<span class="string">&quot;-----------------------------------------------------------------&quot;</span>);</span><br><span class="line">            <span class="keyword">return</span> selectedEndpoint;</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">&quot;-----------------------------------------------------------------&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> endpoints.get(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 该对象用于计算每次调用各个结点的权重信息</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">WeightedRoundRobin</span> &#123;</span><br><span class="line">        <span class="comment">/** 每次调用时结点当次的权重值 **/</span></span><br><span class="line">        <span class="keyword">private</span> <span class="type">AtomicLong</span> <span class="variable">current</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">AtomicLong</span>(<span class="number">0</span>);</span><br><span class="line">        <span class="comment">/** 各个结点的真实权重 **/</span></span><br><span class="line">        <span class="keyword">private</span> <span class="type">int</span> weight;</span><br><span class="line">        <span class="comment">/** 最后一次更新的时间 **/</span></span><br><span class="line">        <span class="keyword">private</span> <span class="type">long</span> lastUpdate;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">getWeight</span><span class="params">()</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> weight;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setWeight</span><span class="params">(<span class="type">int</span> weight)</span> &#123;</span><br><span class="line">            <span class="built_in">this</span>.weight = weight;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> <span class="type">long</span> <span class="title function_">getLastUpdate</span><span class="params">()</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> lastUpdate;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setLastUpdate</span><span class="params">(<span class="type">long</span> lastUpdate)</span> &#123;</span><br><span class="line">            <span class="built_in">this</span>.lastUpdate = lastUpdate;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> <span class="type">long</span> <span class="title function_">increaseCurrent</span><span class="params">()</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> current.addAndGet(weight);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">sel</span><span class="params">(<span class="type">int</span> total)</span> &#123;</span><br><span class="line">            current.addAndGet(-<span class="number">1</span> * total);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> AtomicLong <span class="title function_">getCurrent</span><span class="params">()</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> current;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>为了方便的观察每次调用各个结点的是如何选择的，程序中打印了对应的日志。修改程序入口中的负载均衡策略为轮询负载均衡，观察其输出：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-----------------------------------------------------------------</span><br><span class="line">节点S0 -&gt; 内存中权重：3，真实权重：3，该次调用权重：3，当前最大权重：-9223372036854775808</span><br><span class="line">节点S1 -&gt; 内存中权重：2，真实权重：2，该次调用权重：2，当前最大权重：3</span><br><span class="line">节点S2 -&gt; 内存中权重：1，真实权重：1，该次调用权重：1，当前最大权重：3</span><br><span class="line">本地调用选择 S0，同时调整 S0 节点在内存中的权重为 3 - 6 = -3</span><br><span class="line">此时内存中各节点权重：</span><br><span class="line">  S0 -&gt; 权重：-3</span><br><span class="line">  S1 -&gt; 权重：2</span><br><span class="line">  S2 -&gt; 权重：1</span><br><span class="line">-----------------------------------------------------------------</span><br><span class="line">S0 -&gt; LoadBance</span><br><span class="line">-----------------------------------------------------------------</span><br><span class="line">节点S0 -&gt; 内存中权重：0，真实权重：3，该次调用权重：0，当前最大权重：-9223372036854775808</span><br><span class="line">节点S1 -&gt; 内存中权重：4，真实权重：2，该次调用权重：4，当前最大权重：0</span><br><span class="line">节点S2 -&gt; 内存中权重：2，真实权重：1，该次调用权重：2，当前最大权重：4</span><br><span class="line">本地调用选择 S1，同时调整 S1 节点在内存中的权重为 4 - 6 = -2</span><br><span class="line">此时内存中各节点权重：</span><br><span class="line">  S0 -&gt; 权重：0</span><br><span class="line">  S1 -&gt; 权重：-2</span><br><span class="line">  S2 -&gt; 权重：2</span><br><span class="line">-----------------------------------------------------------------</span><br><span class="line">S1 -&gt; LoadBance</span><br><span class="line">-----------------------------------------------------------------</span><br><span class="line">节点S0 -&gt; 内存中权重：3，真实权重：3，该次调用权重：3，当前最大权重：-9223372036854775808</span><br><span class="line">节点S1 -&gt; 内存中权重：0，真实权重：2，该次调用权重：0，当前最大权重：3</span><br><span class="line">节点S2 -&gt; 内存中权重：3，真实权重：1，该次调用权重：3，当前最大权重：3</span><br><span class="line">本地调用选择 S0，同时调整 S0 节点在内存中的权重为 3 - 6 = -3</span><br><span class="line">此时内存中各节点权重：</span><br><span class="line">  S0 -&gt; 权重：-3</span><br><span class="line">  S1 -&gt; 权重：0</span><br><span class="line">  S2 -&gt; 权重：3</span><br><span class="line">-----------------------------------------------------------------</span><br><span class="line">S0 -&gt; LoadBance</span><br><span class="line">-----------------------------------------------------------------</span><br><span class="line">节点S0 -&gt; 内存中权重：0，真实权重：3，该次调用权重：0，当前最大权重：-9223372036854775808</span><br><span class="line">节点S1 -&gt; 内存中权重：2，真实权重：2，该次调用权重：2，当前最大权重：0</span><br><span class="line">节点S2 -&gt; 内存中权重：4，真实权重：1，该次调用权重：4，当前最大权重：2</span><br><span class="line">本地调用选择 S2，同时调整 S2 节点在内存中的权重为 4 - 6 = -2</span><br><span class="line">此时内存中各节点权重：</span><br><span class="line">  S0 -&gt; 权重：0</span><br><span class="line">  S1 -&gt; 权重：2</span><br><span class="line">  S2 -&gt; 权重：-2</span><br><span class="line">-----------------------------------------------------------------</span><br><span class="line">S2 -&gt; LoadBance</span><br><span class="line">-----------------------------------------------------------------</span><br><span class="line">节点S0 -&gt; 内存中权重：3，真实权重：3，该次调用权重：3，当前最大权重：-9223372036854775808</span><br><span class="line">节点S1 -&gt; 内存中权重：4，真实权重：2，该次调用权重：4，当前最大权重：3</span><br><span class="line">节点S2 -&gt; 内存中权重：-1，真实权重：1，该次调用权重：-1，当前最大权重：4</span><br><span class="line">本地调用选择 S1，同时调整 S1 节点在内存中的权重为 4 - 6 = -2</span><br><span class="line">此时内存中各节点权重：</span><br><span class="line">  S0 -&gt; 权重：3</span><br><span class="line">  S1 -&gt; 权重：-2</span><br><span class="line">  S2 -&gt; 权重：-1</span><br><span class="line">-----------------------------------------------------------------</span><br><span class="line">S1 -&gt; LoadBance</span><br><span class="line">-----------------------------------------------------------------</span><br><span class="line">节点S0 -&gt; 内存中权重：6，真实权重：3，该次调用权重：6，当前最大权重：-9223372036854775808</span><br><span class="line">节点S1 -&gt; 内存中权重：0，真实权重：2，该次调用权重：0，当前最大权重：6</span><br><span class="line">节点S2 -&gt; 内存中权重：0，真实权重：1，该次调用权重：0，当前最大权重：6</span><br><span class="line">本地调用选择 S0，同时调整 S0 节点在内存中的权重为 6 - 6 = 0</span><br><span class="line">此时内存中各节点权重：</span><br><span class="line">  S0 -&gt; 权重：0</span><br><span class="line">  S1 -&gt; 权重：0</span><br><span class="line">  S2 -&gt; 权重：0</span><br><span class="line">-----------------------------------------------------------------</span><br><span class="line">S0 -&gt; LoadBance</span><br><span class="line">...</span><br></pre></td></tr></table></figure>

<p>通过观察可发现，<code>S0</code>、<code>S1</code>、<code>S2</code>结点的选择确实是严格按照其对应的权重比例来进行分配的。</p>
<h3 id="最少活跃调用数负载均衡"><a href="#最少活跃调用数负载均衡" class="headerlink" title="最少活跃调用数负载均衡"></a>最少活跃调用数负载均衡</h3><p>最少活跃调用数，相同活跃数的随机，活跃数指调用前后计数差，使慢的机器收到更少。</p>
<p><code>Dubbo</code> 中计算活跃调用数是通过 Dubbo 的 Filter 机制来实现的。具体可以参考 <code>ActiveLimitFilter</code> 类的源码。本文示例直接采用简单的调用前后的耗时进行累加来表示每个结点的活跃值。</p>
<h4 id="改造-Endpoint-接口"><a href="#改造-Endpoint-接口" class="headerlink" title="改造 Endpoint 接口"></a>改造 Endpoint 接口</h4><p>由于最少活跃调用数需要获取活跃调用数，故 <code>Endpoint</code> 接口新增返回调用活跃数的方法。如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">Endpoint</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * hello方法</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> param 参数</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">hello</span><span class="params">(String param)</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 获取节点名称</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> 节点名称</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    String <span class="title function_">name</span><span class="params">()</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 获取节点权重</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> 节点权重</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="type">int</span> <span class="title function_">weight</span><span class="params">()</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 获取调用活跃数</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> 调用活跃数</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="type">int</span> <span class="title function_">active</span><span class="params">()</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="各个结点实现类实现对活跃调用数的累加"><a href="#各个结点实现类实现对活跃调用数的累加" class="headerlink" title="各个结点实现类实现对活跃调用数的累加"></a>各个结点实现类实现对活跃调用数的累加</h4><p>S0Endpoint.java</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">S0Endpoint</span> <span class="keyword">implements</span> <span class="title class_">Endpoint</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> <span class="variable">activeValue</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">hello</span><span class="params">(String param)</span> &#123;</span><br><span class="line">        <span class="type">long</span> <span class="variable">start</span> <span class="operator">=</span> System.currentTimeMillis();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Thread.sleep(<span class="number">1</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">&quot;S0 -&gt; &quot;</span> + param);</span><br><span class="line">        <span class="type">long</span> <span class="variable">remain</span> <span class="operator">=</span> (System.currentTimeMillis() - start);</span><br><span class="line">        activeValue += remain;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">name</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;S0&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">weight</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">3</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">active</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> activeValue;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>S1Endpoint.java</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">S1Endpoint</span> <span class="keyword">implements</span> <span class="title class_">Endpoint</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> <span class="variable">activeValue</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">hello</span><span class="params">(String param)</span> &#123;</span><br><span class="line">        <span class="type">long</span> <span class="variable">start</span> <span class="operator">=</span> System.currentTimeMillis();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Thread.sleep(<span class="number">1</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">&quot;S1 -&gt; &quot;</span> + param);</span><br><span class="line">        <span class="type">long</span> <span class="variable">remain</span> <span class="operator">=</span> (System.currentTimeMillis() - start);</span><br><span class="line">        activeValue += remain;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">name</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;S1&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">weight</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">2</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">active</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> activeValue;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>S2Endpoint.java</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">S2Endpoint</span> <span class="keyword">implements</span> <span class="title class_">Endpoint</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> <span class="variable">activeValue</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">hello</span><span class="params">(String param)</span> &#123;</span><br><span class="line">        <span class="type">long</span> <span class="variable">start</span> <span class="operator">=</span> System.currentTimeMillis();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Thread.sleep(<span class="number">2</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">&quot;S2 -&gt; &quot;</span> + param);</span><br><span class="line">        <span class="type">long</span> <span class="variable">remain</span> <span class="operator">=</span> (System.currentTimeMillis() - start);</span><br><span class="line">        activeValue += remain;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">name</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;S2&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">weight</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">active</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> activeValue;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="实现最小活跃调用数的负载均衡类"><a href="#实现最小活跃调用数的负载均衡类" class="headerlink" title="实现最小活跃调用数的负载均衡类"></a>实现最小活跃调用数的负载均衡类</h4><p>LeastActiveLoadBalance.java</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">LeastActiveLoadBalance</span> <span class="keyword">extends</span> <span class="title class_">AbstractLoadBalance</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> Endpoint <span class="title function_">doSelect</span><span class="params">(List&lt;Endpoint&gt; endpoints, String param)</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;-----------------------------------------------------------------&quot;</span>);</span><br><span class="line">        <span class="comment">// 总结点数</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">length</span> <span class="operator">=</span> endpoints.size();</span><br><span class="line">        <span class="comment">// 所有结点的最小活动值</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">leastActive</span> <span class="operator">=</span> -<span class="number">1</span>;</span><br><span class="line">        <span class="comment">// 具有最小有效值的结点的数量</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">leastCount</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">        <span class="comment">// 定义一个数组来存储每个结点的最小活跃数</span></span><br><span class="line">        <span class="type">int</span>[] leastIndexes = <span class="keyword">new</span> <span class="title class_">int</span>[length];</span><br><span class="line">        <span class="comment">// 权重数组</span></span><br><span class="line">        <span class="type">int</span>[] weights = <span class="keyword">new</span> <span class="title class_">int</span>[length];</span><br><span class="line">        <span class="comment">// 总权重</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">totalWeight</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">        <span class="comment">// 最不活跃的结点的权重</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">firstWeight</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">        <span class="comment">// 每个最不活跃的结点是否都具有相同的权重</span></span><br><span class="line">        <span class="type">boolean</span> <span class="variable">sameWeight</span> <span class="operator">=</span> <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 过滤掉最不活跃的结点</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; length; i++) &#123;</span><br><span class="line">            <span class="type">Endpoint</span> <span class="variable">endpoint</span> <span class="operator">=</span> endpoints.get(i);</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 获取结点的活跃值，</span></span><br><span class="line">            <span class="type">int</span> <span class="variable">active</span> <span class="operator">=</span> endpoint.active();</span><br><span class="line">            <span class="comment">// Dubbo 中的结点活跃值是在ActiveLimitFilter这个类中计算的，</span></span><br><span class="line">            <span class="comment">// 而本示例由于没有实现Filter机制与Listener机制，直接同步累加对应结点的调用耗时来实现</span></span><br><span class="line">            <span class="comment">// 由于本示例的原因，i=0表示S0结点，i=1表示S1结点，i=2表示S2结点</span></span><br><span class="line">            System.out.println(String.format(<span class="string">&quot;i = %s, 活跃值：%s&quot;</span>, i, active));</span><br><span class="line">            <span class="comment">// 获取结点的权重</span></span><br><span class="line">            <span class="type">int</span> <span class="variable">weight</span> <span class="operator">=</span> getWeight(endpoint);</span><br><span class="line">            weights[i] = weight;</span><br><span class="line">            <span class="comment">// 从下面的逻辑可以看出若一个结点的活跃值越大，则不会添加到备选项中</span></span><br><span class="line">            <span class="comment">// 如果它是第一个，或者结点的活动编号小于当前的最小活跃值</span></span><br><span class="line">            <span class="keyword">if</span> (leastActive == -<span class="number">1</span> || active &lt; leastActive) &#123;</span><br><span class="line">                <span class="comment">// 将最小活跃值赋值给 leastActive</span></span><br><span class="line">                leastActive = active;</span><br><span class="line">                leastCount = <span class="number">1</span>;</span><br><span class="line">                leastIndexes[<span class="number">0</span>] = i;</span><br><span class="line">                System.out.println(String.format(<span class="string">&quot;i = %s，此时由于当前结点活跃值最小或该结点为第一遍历的结点&quot;</span>, i));</span><br><span class="line">                totalWeight = weight;</span><br><span class="line">                firstWeight = weight;</span><br><span class="line">                sameWeight = <span class="literal">true</span>;</span><br><span class="line">            &#125; <span class="keyword">else</span> <span class="keyword">if</span> (active == leastActive) &#123;</span><br><span class="line">                System.out.println(String.format(<span class="string">&quot;i = %s，此时由于当前结点的活跃值等于最小活跃值&quot;</span>, i));</span><br><span class="line">                <span class="comment">// 若当前结点的活跃值与最小活跃值相同</span></span><br><span class="line">                leastIndexes[leastCount++] = i;</span><br><span class="line">                totalWeight += weight;</span><br><span class="line"></span><br><span class="line">                <span class="keyword">if</span> (sameWeight &amp;&amp; i &gt; <span class="number">0</span> &amp;&amp; weight != firstWeight) &#123;</span><br><span class="line">                    sameWeight = <span class="literal">false</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (leastCount == <span class="number">1</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> endpoints.get(leastIndexes[<span class="number">0</span>]);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (!sameWeight &amp;&amp; totalWeight &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="comment">// 这里与随机负载均衡策略相同</span></span><br><span class="line">            <span class="type">int</span> <span class="variable">offsetWeight</span> <span class="operator">=</span> ThreadLocalRandom.current().nextInt(totalWeight);</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; leastCount; i++) &#123;</span><br><span class="line">                <span class="type">int</span> <span class="variable">leastIndex</span> <span class="operator">=</span> leastIndexes[i];</span><br><span class="line">                offsetWeight -= weights[leastIndex];</span><br><span class="line">                <span class="keyword">if</span> (offsetWeight &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                    System.out.println(<span class="string">&quot;-----------------------------------------------------------------&quot;</span>);</span><br><span class="line">                    <span class="keyword">return</span> endpoints.get(leastIndex);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">&quot;-----------------------------------------------------------------&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> endpoints.get(leastIndexes[ThreadLocalRandom.current().nextInt(leastCount)]);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>将程序入口中的负载均衡替换为随机负载均衡的实现类，其输出如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-----------------------------------------------------------------</span><br><span class="line">i = 0, 活跃值：0</span><br><span class="line">i = 0，此时由于当前结点活跃值最小或该结点为第一遍历的结点</span><br><span class="line">i = 1, 活跃值：0</span><br><span class="line">i = 1，此时由于当前结点的活跃值等于最小活跃值</span><br><span class="line">i = 2, 活跃值：0</span><br><span class="line">i = 2，此时由于当前结点的活跃值等于最小活跃值</span><br><span class="line">-----------------------------------------------------------------</span><br><span class="line">S1 -&gt; LoadBance</span><br><span class="line">-----------------------------------------------------------------</span><br><span class="line">i = 0, 活跃值：0</span><br><span class="line">i = 0，此时由于当前结点活跃值最小或该结点为第一遍历的结点</span><br><span class="line">i = 1, 活跃值：1</span><br><span class="line">i = 2, 活跃值：0</span><br><span class="line">i = 2，此时由于当前结点的活跃值等于最小活跃值</span><br><span class="line">-----------------------------------------------------------------</span><br><span class="line">S0 -&gt; LoadBance</span><br><span class="line">-----------------------------------------------------------------</span><br><span class="line">i = 0, 活跃值：2</span><br><span class="line">i = 0，此时由于当前结点活跃值最小或该结点为第一遍历的结点</span><br><span class="line">i = 1, 活跃值：1</span><br><span class="line">i = 1，此时由于当前结点活跃值最小或该结点为第一遍历的结点</span><br><span class="line">i = 2, 活跃值：0</span><br><span class="line">i = 2，此时由于当前结点活跃值最小或该结点为第一遍历的结点</span><br><span class="line">-----------------------------------------------------------------</span><br><span class="line">S2 -&gt; LoadBance</span><br><span class="line">...</span><br></pre></td></tr></table></figure>

<p>通过观察日志可发现，确实实现了最少活跃调用数有限，相同最小活跃数的则随机</p>
<h3 id="一致性哈希负载均衡"><a href="#一致性哈希负载均衡" class="headerlink" title="一致性哈希负载均衡"></a>一致性哈希负载均衡</h3><p>Dubbo 中的一致性哈希负载均衡是唯一一个与权重无关的负载均衡算法。使用一致性 Hash 算法，让相同参数的请求总是发到同一结点，当某一结点崩溃时，原本发往该结点的请求，基于虚拟节点，平摊到其它结点，不会引起剧烈变动。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ConsistentHashLoadBalance</span> <span class="keyword">extends</span> <span class="title class_">AbstractLoadBalance</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> ConcurrentMap&lt;String, ConsistentHashSelector&gt; selectors = <span class="keyword">new</span> <span class="title class_">ConcurrentHashMap</span>&lt;&gt;();</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> Endpoint <span class="title function_">doSelect</span><span class="params">(List&lt;Endpoint&gt; endpoints, String param)</span> &#123;</span><br><span class="line">        <span class="comment">// Dubbo中这个Key值的含义是被调用的服务名+方法名组成的字符串，与轮询随机算法一致</span></span><br><span class="line">        <span class="type">String</span> <span class="variable">key</span> <span class="operator">=</span> <span class="string">&quot;com.exp.Endpoint.add&quot;</span>;</span><br><span class="line">        <span class="comment">// identityHashCode 是为了防止新增结点或部分结点失效的问题</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">identityHashCode</span> <span class="operator">=</span> System.identityHashCode(endpoints);</span><br><span class="line">        System.out.println(String.format(<span class="string">&quot;identityHashCode : %s&quot;</span>, identityHashCode));</span><br><span class="line">        <span class="type">ConsistentHashSelector</span> <span class="variable">selector</span> <span class="operator">=</span> selectors.get(key);</span><br><span class="line">        <span class="keyword">if</span> (selector == <span class="literal">null</span> || selector.identityHashCode != identityHashCode) &#123;</span><br><span class="line">            selectors.put(key, <span class="keyword">new</span> <span class="title class_">ConsistentHashSelector</span>(endpoints, identityHashCode));</span><br><span class="line">            selector = selectors.get(key);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> selector.select(param);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">ConsistentHashSelector</span> &#123;</span><br><span class="line">        <span class="comment">/** 存放结点信息的Map **/</span></span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">final</span> TreeMap&lt;Long, Endpoint&gt; virtualEndpoints;</span><br><span class="line">        <span class="comment">/** 对结点列表取的hashCode **/</span></span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">int</span> identityHashCode;</span><br><span class="line">        <span class="comment">/** 虚拟结点个数 **/</span></span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">int</span> replicaNumber;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> <span class="title function_">ConsistentHashSelector</span><span class="params">(List&lt;Endpoint&gt; endpoints, <span class="type">int</span> identityHashCode)</span> &#123;</span><br><span class="line">            <span class="built_in">this</span>.virtualEndpoints = <span class="keyword">new</span> <span class="title class_">TreeMap</span>&lt;&gt;();</span><br><span class="line">            <span class="built_in">this</span>.identityHashCode = identityHashCode;</span><br><span class="line">            <span class="comment">// Dubbo 默认的虚拟结点个数是160，也可以通过配置文件修改</span></span><br><span class="line">            <span class="built_in">this</span>.replicaNumber = <span class="number">4</span>;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 注意：Dubbo在构建 ConsistentHashSelector 对象时会获取对参数的下标值，用于后续对参数做哈希，</span></span><br><span class="line">            <span class="comment">// 这里我们示例比较简单，直接对所有参数做哈希</span></span><br><span class="line">            <span class="keyword">for</span> (Endpoint endpoint : endpoints) &#123;</span><br><span class="line">                <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; replicaNumber / <span class="number">4</span>; i++) &#123;</span><br><span class="line">                    <span class="type">byte</span>[] digest = md5(endpoint.name() + i);</span><br><span class="line">                    <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">j</span> <span class="operator">=</span> <span class="number">0</span>; j &lt; <span class="number">4</span>; j++) &#123;</span><br><span class="line">                        <span class="type">long</span> <span class="variable">m</span> <span class="operator">=</span> hash(digest, j);</span><br><span class="line">                        virtualEndpoints.put(m, endpoint);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> Endpoint <span class="title function_">select</span><span class="params">(String param)</span> &#123;</span><br><span class="line">            <span class="type">byte</span>[] digest = md5(param);</span><br><span class="line">            <span class="comment">// 这里的哈希算法不要采用 java.util.Objects.hash的哈希算法</span></span><br><span class="line">            <span class="type">long</span> <span class="variable">key</span> <span class="operator">=</span> hash(digest, <span class="number">0</span>);</span><br><span class="line">            System.out.println(String.format(<span class="string">&quot;param key : %s&quot;</span>, key));</span><br><span class="line">            <span class="keyword">return</span> selectForKey(key);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">private</span> Endpoint <span class="title function_">selectForKey</span><span class="params">(<span class="type">long</span> hash)</span> &#123;</span><br><span class="line">            Map.Entry&lt;Long, Endpoint&gt; entry = virtualEndpoints.ceilingEntry(hash);</span><br><span class="line">            <span class="keyword">if</span> (entry == <span class="literal">null</span>) &#123;</span><br><span class="line">                entry = virtualEndpoints.firstEntry();</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> entry.getValue();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">private</span> <span class="type">long</span> <span class="title function_">hash</span><span class="params">(<span class="type">byte</span>[] digest, <span class="type">int</span> number)</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> (((<span class="type">long</span>) (digest[<span class="number">3</span> + number * <span class="number">4</span>] &amp; <span class="number">0xFF</span>) &lt;&lt; <span class="number">24</span>)</span><br><span class="line">                    | ((<span class="type">long</span>) (digest[<span class="number">2</span> + number * <span class="number">4</span>] &amp; <span class="number">0xFF</span>) &lt;&lt; <span class="number">16</span>)</span><br><span class="line">                    | ((<span class="type">long</span>) (digest[<span class="number">1</span> + number * <span class="number">4</span>] &amp; <span class="number">0xFF</span>) &lt;&lt; <span class="number">8</span>)</span><br><span class="line">                    | (digest[number * <span class="number">4</span>] &amp; <span class="number">0xFF</span>))</span><br><span class="line">                    &amp; <span class="number">0xFFFFFFFFL</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">private</span> <span class="type">byte</span>[] md5(String value) &#123;</span><br><span class="line">            MessageDigest md5;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                md5 = MessageDigest.getInstance(<span class="string">&quot;MD5&quot;</span>);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (NoSuchAlgorithmException e) &#123;</span><br><span class="line">                <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalStateException</span>(e.getMessage(), e);</span><br><span class="line">            &#125;</span><br><span class="line">            md5.reset();</span><br><span class="line">            <span class="type">byte</span>[] bytes = value.getBytes(StandardCharsets.UTF_8);</span><br><span class="line">            md5.update(bytes);</span><br><span class="line">            <span class="keyword">return</span> md5.digest();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>替换程序入口中的负载均衡算法为一致性哈希负载均衡的实现类，观察输出</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">identityHashCode : 1018547642</span><br><span class="line">param key : 119807787</span><br><span class="line">S1 -&gt; LoadBance</span><br><span class="line">identityHashCode : 1018547642</span><br><span class="line">param key : 119807787</span><br><span class="line">S1 -&gt; LoadBance</span><br><span class="line">identityHashCode : 1018547642</span><br><span class="line">param key : 119807787</span><br><span class="line">S1 -&gt; LoadBance</span><br><span class="line">...</span><br></pre></td></tr></table></figure>

<p>通过观察发现对于相同的参数，程序永远调用了 S1 结点。</p>
]]></content>
      <tags>
        <tag>Dubbo</tag>
        <tag>负载均衡</tag>
        <tag>微服务</tag>
      </tags>
  </entry>
  <entry>
    <title>git 恢复已删除的分支</title>
    <url>/2020/03/26/git-%E6%81%A2%E5%A4%8D%E5%B7%B2%E5%88%A0%E9%99%A4%E7%9A%84%E5%88%86%E6%94%AF/</url>
    <content><![CDATA[<p>在日常开发中，如果我们一不小心删除了某个分支，又想重新使用这个分支，该怎么找回该分支上的代码呢？</p>
<h2 id="场景还原"><a href="#场景还原" class="headerlink" title="场景还原"></a>场景还原</h2><ol>
<li>有一个 git 仓库，从 <code>master</code> 分支新建 <code>feature/delete_branch</code> 分支，并进行一次提交，如下：</li>
</ol>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">新建一个 feature/delete_branch 分支</span></span><br><span class="line">git branch feature/delete_branch</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">切换到 feature/delete_branch 分支</span></span><br><span class="line">git checkout feature/delete_branch</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">新建一个文件，进行一次提交并推送至远程分支</span></span><br><span class="line">git add delete-file.md</span><br><span class="line">git commit -m &#x27;:sparkles: add a delete-file.md file&#x27;</span><br><span class="line">git push origin feature/delete_branch:feature/delete_branch</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">查看当前分支图</span></span><br><span class="line">git log --all --decorate --oneline --graph</span><br><span class="line">* 88ac246 (HEAD -&gt; feature/delete_branch, origin/feature/delete_branch) :sparkles: add a delete-file.md file</span><br><span class="line">* 8693480 (origin/master, master) :sparkles: add a hello-git.md file</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>切换到 <code>master</code> 分支，并删除 <code>feature/delete_branch</code> 分支</li>
</ol>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">切换到 master 分支</span></span><br><span class="line">git checkout master</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">删除 feature/delete_branch 分支</span></span><br><span class="line">git branch -d feature/delete_branch</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">将删除的分支同步只至远程</span></span><br><span class="line">git push origin --delete feature/delete_branch</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">此时无论是本地还是远程都已经删除了 feature/delete_branch</span></span><br><span class="line">git log --all --decorate --oneline --graph</span><br><span class="line">* 8693480 (HEAD -&gt; master, origin/master) :sparkles: add a hello-git.md file</span><br></pre></td></tr></table></figure>

<h2 id="还原删除的分支"><a href="#还原删除的分支" class="headerlink" title="还原删除的分支"></a>还原删除的分支</h2><ol>
<li>通过 <code>git reflog</code> 或 <code>git log</code> 命令找到我们需要恢复的 <code>commit id</code></li>
</ol>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">这里演示 git reflog 命令展示出来的提交</span></span><br><span class="line">git reflog</span><br><span class="line">8693480 (HEAD -&gt; master, origin/master) HEAD@&#123;0&#125;: checkout: moving from feature/delete_branch to master</span><br><span class="line">88ac246 (feature/delete_branch) HEAD@&#123;1&#125;: commit: :sparkles: add a delete-file.md file</span><br><span class="line">8693480 (HEAD -&gt; master, origin/master) HEAD@&#123;2&#125;: checkout: moving from master to feature/delete_branch</span><br><span class="line">8693480 (HEAD -&gt; master, origin/master) HEAD@&#123;3&#125;: commit (initial): :sparkles: add a hello-git.md file</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">这里演示 git <span class="built_in">log</span> 命令 展示出来的提交</span></span><br><span class="line">git log -g</span><br><span class="line">commit 86934803092ae7ae7266f9ebff3429437e423b80 (HEAD -&gt; master, origin/master)</span><br><span class="line">Reflog: HEAD@&#123;0&#125; (fuyongde &lt;fuyongde@foxmail.com&gt;)</span><br><span class="line">Reflog message: checkout: moving from feature/delete_branch to master</span><br><span class="line">Author: fuyongde &lt;fuyongde@foxmail.com&gt;</span><br><span class="line">Date:   Thu Mar 26 18:36:35 2020 +0800</span><br><span class="line"></span><br><span class="line">    :sparkles: add a hello-git.md file</span><br><span class="line"></span><br><span class="line">commit 88ac246020a0cc25071fb7fd3ac88363f4bf2beb (feature/delete_branch)</span><br><span class="line">Reflog: HEAD@&#123;1&#125; (fuyongde &lt;fuyongde@foxmail.com&gt;)</span><br><span class="line">Reflog message: commit: :sparkles: add a delete-file.md file</span><br><span class="line">Author: fuyongde &lt;fuyongde@foxmail.com&gt;</span><br><span class="line">Date:   Thu Mar 26 18:44:01 2020 +0800</span><br><span class="line"></span><br><span class="line">    :sparkles: add a delete-file.md file</span><br><span class="line"></span><br><span class="line">commit 86934803092ae7ae7266f9ebff3429437e423b80 (HEAD -&gt; master, origin/master)</span><br><span class="line">Reflog: HEAD@&#123;2&#125; (fuyongde &lt;fuyongde@foxmail.com&gt;)</span><br><span class="line">Reflog message: checkout: moving from master to feature/delete_branch</span><br><span class="line">Author: fuyongde &lt;fuyongde@foxmail.com&gt;</span><br><span class="line">Date:   Thu Mar 26 18:36:35 2020 +0800</span><br><span class="line"></span><br><span class="line">    :sparkles: add a hello-git.md file</span><br><span class="line"></span><br><span class="line">commit 86934803092ae7ae7266f9ebff3429437e423b80 (HEAD -&gt; master, origin/master)</span><br><span class="line">Reflog: HEAD@&#123;3&#125; (fuyongde &lt;fuyongde@foxmail.com&gt;)</span><br><span class="line">Reflog message: commit (initial): :sparkles: add a hello-git.md file</span><br><span class="line">Author: fuyongde &lt;fuyongde@foxmail.com&gt;</span><br><span class="line">Date:   Thu Mar 26 18:36:35 2020 +0800</span><br><span class="line"></span><br><span class="line">    :sparkles: add a hello-git.md file</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>通过 <code>git branch [branch name] [commit id]</code> 命令来恢复删除的分支</li>
</ol>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">恢复 feature/delete_branch 分支到 88ac246 版本</span></span><br><span class="line">git branch feature/delete_branch 88ac246</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">检出 feature/delete_branch 分支</span></span><br><span class="line">git checkout feature/delete_branch</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">此时可以看到已经删除的分支都找回来了，将分支推送的远程</span></span><br><span class="line">git push origin feature/delete_branch:feature/delete_branch</span><br></pre></td></tr></table></figure>

<h2 id="原因"><a href="#原因" class="headerlink" title="原因"></a>原因</h2><p>当我们删除一个分支时，git 只是删除了指向相关提交的指针，但该提交对象依然会留在版本库中，因此，如果我们知道删除分支时的 <code>commit id</code>，就可以将某个删除的分支恢复过来。</p>
]]></content>
      <tags>
        <tag>git</tag>
        <tag>骚操作</tag>
      </tags>
  </entry>
  <entry>
    <title>Gradle 手册</title>
    <url>/2020/03/08/gradle-%E6%89%8B%E5%86%8C/</url>
    <content><![CDATA[<h2 id="Gradle-是什么"><a href="#Gradle-是什么" class="headerlink" title="Gradle 是什么"></a>Gradle 是什么</h2><blockquote>
<p>Gradle is an open-source build automation tool focused on flexibility and performance. Gradle build scripts are written using a Groovy or Kotlin DSL. Read about Gradle features to learn what is possible with Gradle.</p>
</blockquote>
<p>Gradle 是一个专注于灵活性和性能的开源自动化构建工具，其脚本由 <code>Groovy</code> 或 <code>Kotlin DSL</code> 编写。Gradle 有以下特性：</p>
<ul>
<li>高度可定制：Gradle 采用可以高度自定义的方式进行建模。</li>
<li>快速：Gradle 通过重用旧的输出、仅变更的输入以及并行的方式来快速完成任务。</li>
<li>功能强大：Gradle 是 <code>Android</code> 的官方构建工具，并附带对许多流行语言和技术的支持。</li>
</ul>
<h2 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h2><p>安装之前，请确保已经正确安装 <code>JDK8</code> 或更高版本</p>
<h3 id="使用包管理器安装"><a href="#使用包管理器安装" class="headerlink" title="使用包管理器安装"></a>使用包管理器安装</h3><p>针对类 <code>Unix</code> 系统（<code>macOS</code>、<code>Linux</code>、<code>Cygwin</code>、<code>Solaris</code> 和 <code>FreeBSD</code>）可以采用以下两种包管理的方式安装。</p>
<ul>
<li>SDKMAN 包管理器</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sdk install gradle</span><br></pre></td></tr></table></figure>

<ul>
<li>Homebrew 包管理器</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">brew install gradle</span><br></pre></td></tr></table></figure>

<h3 id="手动安装"><a href="#手动安装" class="headerlink" title="手动安装"></a>手动安装</h3><ol>
<li>下载指定的 Gradle 发行版，下载地址可参考<a href="https://gradle.org/releases/">发布页</a></li>
<li>解压缩到指定的目录，如 Windows 系统中的 <code>C:\gradle</code> 或 Linux 系统中的 <code>/opt/gradle</code> 等</li>
<li>配置环境变量</li>
</ol>
<ul>
<li>针对 <code>Windows</code> 系统，打开 <code>控制面板</code> &gt; <code>系统和安全</code> &gt; <code>系统</code> &gt; <code>高级系统设置</code> &gt; <code>环境变量</code> &gt; <code>系统变量</code>，新增 <code>GRADLE_HOME</code> 变量，该变量指向 Gradle 的安装路径，然后选中<code>系统变量</code>中的 <code>Path</code>，并新增 <code>%GRADLE_HOME%\bin\</code></li>
<li>针对 <code>Linux</code> 系统，略</li>
</ul>
<p>除了配置环境变量之外，还可以配置 <code>GRADLE_USER_HOME</code> 变量来指定 Gradle 的库文件的目录，该变量缺省值为当前系统用户目录下的 <code>.gradle</code> 目录。</p>
<h3 id="验证"><a href="#验证" class="headerlink" title="验证"></a>验证</h3><p>在命令行中执行 <code>gradle -v</code> 命令，若输出为你安装的版本信息，则说明安装成功了。</p>
<h2 id="使用-Gradle-构建一个项目"><a href="#使用-Gradle-构建一个项目" class="headerlink" title="使用 Gradle 构建一个项目"></a>使用 Gradle 构建一个项目</h2><ol>
<li>新建一个目录 <code>mkdir hello-world</code></li>
<li>进入该目录 <code>cd hello-world</code></li>
<li>使用 Gradle 初始化该目录 <code>gradle init</code>。执行完该命令之后按照指引选择要初始化工程的类型等。之后可以得到一个至少包含以下路径的目录结构。</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">├── build.gradle</span><br><span class="line">├── gradle</span><br><span class="line">│   └── wrapper</span><br><span class="line">│       ├── gradle-wrapper.jar</span><br><span class="line">│       └── gradle-wrapper.properties</span><br><span class="line">├── gradlew</span><br><span class="line">├── gradlew.bat</span><br><span class="line">└── settings.gradle</span><br></pre></td></tr></table></figure>

<p>看到 <code>gradlew</code>、<code>gradlew.bat</code> 以及 <code>wapper</code> 目录是不是有点熟悉？没错，这个和 Maven Wapper 的思想是一样的，可参考 <a href="https://fuyongde.github.io/2019/06/23/maven-wapper-%E6%89%8B%E5%86%8C/">Maven Wapper 手册</a>这篇文章。</p>
<p>未完待续…</p>
]]></content>
      <categories>
        <category>手册</category>
      </categories>
      <tags>
        <tag>未完待续</tag>
        <tag>Gradle</tag>
        <tag>构建工具</tag>
      </tags>
  </entry>
  <entry>
    <title>如何使用Hexo？</title>
    <url>/2010/05/24/hexo/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p>
<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo server</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo generate</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo deploy</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/deployment.html">Deployment</a></p>
]]></content>
  </entry>
  <entry>
    <title>如何使用 Java 的 HttpClient 请求 Https 服务</title>
    <url>/2020/12/10/how-use-httpclient-over-https/</url>
    <content><![CDATA[<p>近期收到一些小伙伴问 Java 中如何调用 https 中双向认证的接口的问题，本文采用 okhttp 以及 Java11 的 httpclient 来介绍如何使用 Java 代码调用 https 双向认证的接口。</p>
<h2 id="前置工作"><a href="#前置工作" class="headerlink" title="前置工作"></a>前置工作</h2><ol>
<li>生成服务端、客户端证书，并将各自的公钥加入到对方的信任链中以供后续的使用；</li>
<li>搭建一个 https 协议的服务以供客户端调用。</li>
</ol>
<h3 id="制作证书"><a href="#制作证书" class="headerlink" title="制作证书"></a>制作证书</h3><p>这里我们采用 jdk 提供的 keytool 命令来生成我们所需要的服务端、客户端证书，并且可以使用 keytool 命令来将各自的公钥加入到对方的信任连中。</p>
<h4 id="生成服务端、客户端密钥库以及证书"><a href="#生成服务端、客户端密钥库以及证书" class="headerlink" title="生成服务端、客户端密钥库以及证书"></a>生成服务端、客户端密钥库以及证书</h4><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">生成服务端keystore，这里加拓展项的目的是为了说明域名验证的问题</span></span><br><span class="line">keytool -genkey -validity 3650 -alias server -keyalg RSA -keystore server.keystore -ext san=dns:www.goku.edu,ip:127.0.0.1</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">生成客户端keystore，这里针对客户端的密钥库我们采用 PKCS12 的格式</span></span><br><span class="line">keytool -genkey -validity 3650 -alias client -keyalg RSA -storetype PKCS12 -keystore client.p12</span><br></pre></td></tr></table></figure>

<h4 id="导出服务端、客户端证书中的公钥"><a href="#导出服务端、客户端证书中的公钥" class="headerlink" title="导出服务端、客户端证书中的公钥"></a>导出服务端、客户端证书中的公钥</h4><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">导出服务端证书的公钥</span></span><br><span class="line">keytool -export -keystore server.keystore -alias server -file server.cer -rfc</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">导出客户端证书的公钥</span></span><br><span class="line">keytool -export -keystore client.p12 -storetype PKCS12 -alias client -file client.cer</span><br></pre></td></tr></table></figure>

<h4 id="将各自的公钥加入到对方的密钥库中"><a href="#将各自的公钥加入到对方的密钥库中" class="headerlink" title="将各自的公钥加入到对方的密钥库中"></a>将各自的公钥加入到对方的密钥库中</h4><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">将服务端证书导入到客户端的信任库中</span></span><br><span class="line">keytool -import -alias server -file server.cer -keystore client.p12</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">将客户端证书导入到服务端的信任库中</span></span><br><span class="line">keytool -import -alias client -file client.cer -keystore server.keystore</span><br></pre></td></tr></table></figure>

<h4 id="查看密钥库"><a href="#查看密钥库" class="headerlink" title="查看密钥库"></a>查看密钥库</h4><p>可以通过如下命令，来查看公钥是否加入到各自的密钥库中</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">keytool -list -v -keystore server.keystore</span><br><span class="line">keytool -list -rfc -keystore client.p12</span><br></pre></td></tr></table></figure>

<h3 id="搭建-HTTPS-服务"><a href="#搭建-HTTPS-服务" class="headerlink" title="搭建 HTTPS 服务"></a>搭建 HTTPS 服务</h3><p>由于搭建服务不是本文的重点，建议使用 SpringBoot 脚手架快速搭建一个服务，下面只列关键点。</p>
<h4 id="编写一个简单的-HTTP-接口"><a href="#编写一个简单的-HTTP-接口" class="headerlink" title="编写一个简单的 HTTP 接口"></a>编写一个简单的 HTTP 接口</h4><p>代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="meta">@RequestMapping(&quot;/index&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">IndexRestController</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping(&quot;/hello&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">hello</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;Hello World!&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>完成之后，启动服务，使用浏览器打开对应的接口地址，如果正确输出<code>Hello World!</code>则说明接口正常。</p>
<h4 id="开启-SpringBoot-的-SSL-双向认证"><a href="#开启-SpringBoot-的-SSL-双向认证" class="headerlink" title="开启 SpringBoot 的 SSL 双向认证"></a>开启 SpringBoot 的 SSL 双向认证</h4><p>在 <code>application.properties</code> 中新增如下配置，即可开启 SSL 的双向认证</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="attr">server.ssl.enabled</span>=<span class="string">true</span></span><br><span class="line"><span class="attr">server.ssl.protocol</span>=<span class="string">TLS</span></span><br><span class="line"><span class="attr">server.ssl.key-store</span>=<span class="string">classpath:certs/server.keystore</span></span><br><span class="line"><span class="attr">server.ssl.key-store-password</span>=<span class="string">111111</span></span><br><span class="line"><span class="attr">server.ssl.key-password</span>=<span class="string">111111</span></span><br><span class="line"><span class="attr">server.ssl.key-alias</span>=<span class="string">server</span></span><br><span class="line"><span class="attr">server.ssl.trust-store</span>=<span class="string">classpath:certs/server.keystore</span></span><br><span class="line"><span class="attr">server.ssl.trust-store-password</span>=<span class="string">111111</span></span><br><span class="line"><span class="attr">server.ssl.client-auth</span>=<span class="string">NEED</span></span><br></pre></td></tr></table></figure>

<p>配完之后，启动服务，再次使用浏览器打开上一步的接口地址，发现接口并不能正常使用，并且浏览器会提示”此站点的连接不安全”，如下图所示</p>
<img src="/2020/12/10/how-use-httpclient-over-https/certificate_not_trusted.png" class="">

<h2 id="使用-OkHttp"><a href="#使用-OkHttp" class="headerlink" title="使用 OkHttp"></a>使用 OkHttp</h2><h3 id="构建-OkHttpClient"><a href="#构建-OkHttpClient" class="headerlink" title="构建 OkHttpClient"></a>构建 OkHttpClient</h3><p>构建 SSL 的双向认证，有两个要点</p>
<ol>
<li>创建 SSLSocket</li>
<li>创建 HostnameVerifier</li>
</ol>
<h4 id="创建-SSLSocketFactory"><a href="#创建-SSLSocketFactory" class="headerlink" title="创建 SSLSocketFactory"></a>创建 SSLSocketFactory</h4><p>在进行 HTTPS 的请求时，最重要的就是创建 SSLSocket ，在 Java 中可以通过 SSLSocketFactory 来创建 SSLSocket，故在构建 OkHttpClient 时成员变量 SSLSocketFactory 的创建就显得非常关键。</p>
<p>代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> SSLSocketFactory <span class="title function_">sslSocketFactory</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">try</span> (<span class="type">InputStream</span> <span class="variable">inputStream</span> <span class="operator">=</span> HttpsUtils.class.getResourceAsStream(<span class="string">&quot;/certs/client.p12&quot;</span>)) &#123;</span><br><span class="line">        <span class="comment">// 加载 keyStore</span></span><br><span class="line">        <span class="type">KeyStore</span> <span class="variable">keyStore</span> <span class="operator">=</span> KeyStore.getInstance(<span class="string">&quot;PKCS12&quot;</span>);</span><br><span class="line">        keyStore.load(inputStream, <span class="string">&quot;111111&quot;</span>.toCharArray());</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 创建密钥管理器</span></span><br><span class="line">        <span class="type">KeyManagerFactory</span> <span class="variable">kmf</span> <span class="operator">=</span> KeyManagerFactory.getInstance(KeyManagerFactory.getDefaultAlgorithm());</span><br><span class="line">        kmf.init(keyStore, <span class="string">&quot;111111&quot;</span>.toCharArray());</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 创建信任链管理器</span></span><br><span class="line">        <span class="type">String</span> <span class="variable">defaultAlgorithm</span> <span class="operator">=</span> TrustManagerFactory.getDefaultAlgorithm();</span><br><span class="line">        <span class="type">TrustManagerFactory</span> <span class="variable">tmf</span> <span class="operator">=</span> TrustManagerFactory.getInstance(defaultAlgorithm);</span><br><span class="line">        tmf.init(keyStore);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 初始化 SSLContext</span></span><br><span class="line">        <span class="type">SSLContext</span> <span class="variable">sslContext</span> <span class="operator">=</span> SSLContext.getInstance(<span class="string">&quot;TLS&quot;</span>);</span><br><span class="line">        sslContext.init(kmf.getKeyManagers(), tmf.getTrustManagers(), <span class="keyword">new</span> <span class="title class_">SecureRandom</span>());</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> sslContext.getSocketFactory();</span><br><span class="line">    &#125; <span class="keyword">catch</span> (NoSuchAlgorithmException | KeyManagementException | KeyStoreException | UnrecoverableKeyException | IOException | CertificateException e) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">RuntimeException</span>(e);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>对于第二个参数所需的 X509TrustManager，我们采用 OkHttp 提供的默认实现<code>okhttp3.internal.Util#platformTrustManager</code>即可。</p>
<h4 id="创建-HostnameVerifier"><a href="#创建-HostnameVerifier" class="headerlink" title="创建 HostnameVerifier"></a>创建 HostnameVerifier</h4><p>HostnameVerifier 是为了帮助我们验证我们请求的域名以及对应的 IP 是否一致。这里可以回顾一下创建证书时我们添加的拓展项<code>-ext san=dns:www.goku.edu,ip:127.0.0.1</code>。由于我们在本机测试，可以信任对应的域名，也可以通过修改 hosts 文件的方式来指定域名，也可以自定义 HostnameVerifier 的实现。这里以不对域名做校验为例子，代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> HostnameVerifier <span class="title function_">hostnameVerifier</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> (hostname, session) -&gt; <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在完成以上所有步骤之后，就可以构建我们的 OkHttpClient，并发送我们的 https 请求。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException &#123;</span><br><span class="line">    <span class="comment">// 创建 OkHttpClient</span></span><br><span class="line">    <span class="type">OkHttpClient</span> <span class="variable">httpClient</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">OkHttpClient</span>.Builder()</span><br><span class="line">        .hostnameVerifier(hostnameVerifier())</span><br><span class="line">        .sslSocketFactory(sslSocketFactory(), Util.platformTrustManager())</span><br><span class="line">        .build();</span><br><span class="line">    <span class="comment">// 构造请求</span></span><br><span class="line">    <span class="type">Request</span> <span class="variable">request</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Request</span>.Builder()</span><br><span class="line">            .url(<span class="string">&quot;https://localhost:8443/index/hello&quot;</span>)</span><br><span class="line">            .build();</span><br><span class="line">    <span class="comment">// 发送请求</span></span><br><span class="line">    <span class="type">Response</span> <span class="variable">response</span> <span class="operator">=</span> httpClient.newCall(request).execute();</span><br><span class="line">    System.out.println(response.body().string());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>执行代码，发现控制台正确打印<code>Hello World!</code>，说明我们对于 SSL 双向认证的 Https 请求已经发送成功，并成功的接收到响应了。</p>
<h2 id="使用-Java11-的-HttpClient"><a href="#使用-Java11-的-HttpClient" class="headerlink" title="使用 Java11 的 HttpClient"></a>使用 Java11 的 HttpClient</h2><p>熟悉 Java11 HttpClient 的开发者都知道 Java11 中的 HttpClient 跟 OkHttpClient 中的用法几乎一致。回归到 SSL 的双向认证，有以下两个不同点</p>
<ol>
<li>Java11 的 HttpClient 所需传的参数为 SSLContext，这个跟我们在使用 OkHttp 时创建 SSLSocketFactory 时初始化的 SSLContext 完全一致；</li>
<li>Java11 的 HttpClient 并不支持自定义的 hostnameVerifier，只能通过配置项来关闭或打开域名验证。</li>
</ol>
<p>其代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException, InterruptedException, KeyStoreException,</span><br><span class="line">        CertificateException, NoSuchAlgorithmException, UnrecoverableKeyException, KeyManagementException &#123;</span><br><span class="line">    <span class="comment">// 如需禁用 HostnameVerification</span></span><br><span class="line">    <span class="comment">// System.setProperty(&quot;jdk.internal.httpclient.disableHostnameVerification&quot;, &quot;true&quot;);</span></span><br><span class="line"></span><br><span class="line">    <span class="type">InputStream</span> <span class="variable">inputStream</span> <span class="operator">=</span> HttpsUtils.class.getResourceAsStream(<span class="string">&quot;/certs/client.p12&quot;</span>);</span><br><span class="line">    <span class="comment">//创建KeyStore，用来存储信任证书</span></span><br><span class="line">    <span class="type">KeyStore</span> <span class="variable">keyStore</span> <span class="operator">=</span> KeyStore.getInstance(<span class="string">&quot;PKCS12&quot;</span>);</span><br><span class="line">    keyStore.load(inputStream, <span class="string">&quot;111111&quot;</span>.toCharArray());</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建密钥管理器</span></span><br><span class="line">    <span class="type">KeyManagerFactory</span> <span class="variable">kmf</span> <span class="operator">=</span> KeyManagerFactory.getInstance(KeyManagerFactory.getDefaultAlgorithm());</span><br><span class="line">    kmf.init(keyStore, <span class="string">&quot;111111&quot;</span>.toCharArray());</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建信任链管理器</span></span><br><span class="line">    <span class="type">String</span> <span class="variable">defaultAlgorithm</span> <span class="operator">=</span> TrustManagerFactory.getDefaultAlgorithm();</span><br><span class="line">    <span class="type">TrustManagerFactory</span> <span class="variable">tmf</span> <span class="operator">=</span> TrustManagerFactory.getInstance(defaultAlgorithm);</span><br><span class="line">    tmf.init(keyStore);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 初始化 SSLContext</span></span><br><span class="line">    <span class="type">SSLContext</span> <span class="variable">sslContext</span> <span class="operator">=</span> SSLContext.getInstance(<span class="string">&quot;TLS&quot;</span>);</span><br><span class="line">    sslContext.init(kmf.getKeyManagers(), tmf.getTrustManagers(), <span class="keyword">new</span> <span class="title class_">SecureRandom</span>());</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建 HttpClient</span></span><br><span class="line">    <span class="type">HttpClient</span> <span class="variable">httpClient</span> <span class="operator">=</span> HttpClient.newBuilder()</span><br><span class="line">            .sslContext(sslContext)</span><br><span class="line">            .build();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建一个自定义的HTTP请求对象</span></span><br><span class="line">    <span class="type">HttpRequest</span> <span class="variable">request</span> <span class="operator">=</span> HttpRequest.newBuilder()</span><br><span class="line">            .GET()</span><br><span class="line">            .uri(URI.create(<span class="string">&quot;https://www.goku.edu:8443/index/hello&quot;</span>))</span><br><span class="line">            .header(<span class="string">&quot;Accept-Language&quot;</span>, <span class="string">&quot;zh-CN&quot;</span>)</span><br><span class="line">            .timeout(Duration.ofMillis(<span class="number">5000</span>))</span><br><span class="line">            .build();</span><br><span class="line">    <span class="comment">// 客户端传递请求信息，且返回字符串形式的应答报文</span></span><br><span class="line">    HttpResponse&lt;String&gt; response = httpClient.send(request, HttpResponse.BodyHandlers.ofString());</span><br><span class="line">    System.out.println(response.body());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <tags>
        <tag>https</tag>
        <tag>okhttp</tag>
      </tags>
  </entry>
  <entry>
    <title>Java 多线程死锁问题</title>
    <url>/2019/11/19/java-deadlock/</url>
    <content><![CDATA[<p>本文描述一个 Java 多线程死锁问题，并演示使用 <code>jstack</code>、<code>arthas</code> 工具来发现应用程序中的线程死锁。</p>
<h2 id="先编写一段死锁的代码"><a href="#先编写一段死锁的代码" class="headerlink" title="先编写一段死锁的代码"></a>先编写一段死锁的代码</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">App</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">x</span> <span class="operator">=</span> <span class="string">&quot;x&quot;</span>;</span><br><span class="line">        <span class="type">String</span> <span class="variable">y</span> <span class="operator">=</span> <span class="string">&quot;y&quot;</span>;</span><br><span class="line">        <span class="type">Thread</span> <span class="variable">a</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Thread</span>(<span class="keyword">new</span> <span class="title class_">Runnable</span>() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">                <span class="type">String</span> <span class="variable">threadName</span> <span class="operator">=</span> Thread.currentThread().getName();</span><br><span class="line">                <span class="keyword">synchronized</span> (x) &#123;</span><br><span class="line">                    System.out.println(String.format(<span class="string">&quot;Thread %s Start %s&quot;</span>, threadName, <span class="string">&quot;------&quot;</span>));</span><br><span class="line">                    System.out.println(String.format(<span class="string">&quot;Thread %s, x = %s&quot;</span>, threadName, x));</span><br><span class="line">                    <span class="keyword">synchronized</span> (y) &#123;</span><br><span class="line">                        System.out.println(String.format(<span class="string">&quot;Thread %s, y = %s&quot;</span>, threadName, y));</span><br><span class="line">                    &#125;</span><br><span class="line">                    System.out.println(String.format(<span class="string">&quot;Thread %s End %s&quot;</span>, threadName, <span class="string">&quot;------&quot;</span>));</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        <span class="type">Thread</span> <span class="variable">b</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Thread</span>(<span class="keyword">new</span> <span class="title class_">Runnable</span>() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">                <span class="type">String</span> <span class="variable">threadName</span> <span class="operator">=</span> Thread.currentThread().getName();</span><br><span class="line">                <span class="keyword">synchronized</span> (y) &#123;</span><br><span class="line">                    System.out.println(String.format(<span class="string">&quot;Thread %s Start %s&quot;</span>, threadName, <span class="string">&quot;------&quot;</span>));</span><br><span class="line">                    System.out.println(String.format(<span class="string">&quot;Thread %s, y = %s&quot;</span>, threadName, y));</span><br><span class="line">                    <span class="keyword">synchronized</span> (x) &#123;</span><br><span class="line">                        System.out.println(String.format(<span class="string">&quot;Thread %s, x = %s&quot;</span>, threadName, x));</span><br><span class="line">                    &#125;</span><br><span class="line">                    System.out.println(String.format(<span class="string">&quot;Thread %s End %s&quot;</span>, threadName, <span class="string">&quot;------&quot;</span>));</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        a.start();</span><br><span class="line">        b.start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>运行以上代码，发现如下输出：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Thread Thread-0 Start ------</span><br><span class="line">Thread Thread-0, x = x</span><br><span class="line">Thread Thread-1 Start ------</span><br><span class="line">Thread Thread-1, y = y</span><br></pre></td></tr></table></figure>

<p>由于 Thread-0 和 Thread-1 均没有输出 End 信息，可知线程陷入了死锁。</p>
<h2 id="jstack-分析"><a href="#jstack-分析" class="headerlink" title="jstack 分析"></a>jstack 分析</h2><p>jstack 是 jdk 自带的线程堆栈分析工具，使用该命令可以查看或导出 Java 应用程序中线程堆栈信息。</p>
<p>Useage:</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">Usage:</span><br><span class="line">    jstack [-l] &lt;pid&gt;</span><br><span class="line">        (to connect to running process)</span><br><span class="line">    jstack -F [-m] [-l] &lt;pid&gt;</span><br><span class="line">        (to connect to a hung process)</span><br><span class="line">    jstack [-m] [-l] &lt;executable&gt; &lt;core&gt;</span><br><span class="line">        (to connect to a core file)</span><br><span class="line">    jstack [-m] [-l] [server_id@]&lt;remote server IP or hostname&gt;</span><br><span class="line">        (to connect to a remote debug server)</span><br><span class="line"></span><br><span class="line">Options:</span><br><span class="line">    -F  强制转储线程信息，当 jstack 不响应时使用</span><br><span class="line">    -m  打印Java和本机框架的所有栈信息（混合模式）</span><br><span class="line">    -l  长列表，答应更多关于锁的附加信息</span><br><span class="line">    -h or -help 打印帮住信息</span><br></pre></td></tr></table></figure>

<p>通过 <code>jps</code> 命令查询到程序的 pid，使用 <code>jstack -l</code> 来查看线程堆栈信息</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">PS C:\Users\fuyongde&gt; jstack -l 151792</span><br><span class="line">2019-11-19 16:20:14</span><br><span class="line">Full thread dump Java HotSpot(TM) 64-Bit Server VM (25.162-b12 mixed mode):</span><br><span class="line"></span><br><span class="line">&quot;DestroyJavaVM&quot; #15 prio=5 os_prio=0 tid=0x0000000003103800 nid=0x240b8 waiting on condition [0x0000000000000000]</span><br><span class="line">   java.lang.Thread.State: RUNNABLE</span><br><span class="line"></span><br><span class="line">   Locked ownable synchronizers:</span><br><span class="line">        - None</span><br><span class="line"></span><br><span class="line">&quot;Thread-1&quot; #14 prio=5 os_prio=0 tid=0x00000000227d2000 nid=0x21898 waiting for monitor entry [0x00000000235df000]</span><br><span class="line">   java.lang.Thread.State: BLOCKED (on object monitor)</span><br><span class="line">        at com.exp.App$2.run(App.java:34)</span><br><span class="line">        - waiting to lock &lt;0x0000000740da48c0&gt; (a java.lang.String)</span><br><span class="line">        - locked &lt;0x0000000740da48f0&gt; (a java.lang.String)</span><br><span class="line">        at java.lang.Thread.run(Thread.java:748)</span><br><span class="line"></span><br><span class="line">   Locked ownable synchronizers:</span><br><span class="line">        - None</span><br><span class="line"></span><br><span class="line">&quot;Thread-0&quot; #13 prio=5 os_prio=0 tid=0x00000000227d1000 nid=0x23f90 waiting for monitor entry [0x00000000234df000]</span><br><span class="line">   java.lang.Thread.State: BLOCKED (on object monitor)</span><br><span class="line">        at com.exp.App$1.run(App.java:19)</span><br><span class="line">        - waiting to lock &lt;0x0000000740da48f0&gt; (a java.lang.String)</span><br><span class="line">        - locked &lt;0x0000000740da48c0&gt; (a java.lang.String)</span><br><span class="line">        at java.lang.Thread.run(Thread.java:748)</span><br><span class="line"></span><br><span class="line">   Locked ownable synchronizers:</span><br><span class="line">        - None</span><br><span class="line"></span><br><span class="line">&quot;Service Thread&quot; #12 daemon prio=9 os_prio=0 tid=0x000000002271c000 nid=0x21868 runnable [0x0000000000000000]</span><br><span class="line">   java.lang.Thread.State: RUNNABLE</span><br><span class="line"></span><br><span class="line">   Locked ownable synchronizers:</span><br><span class="line">        - None</span><br><span class="line"></span><br><span class="line">&quot;C1 CompilerThread2&quot; #11 daemon prio=9 os_prio=2 tid=0x0000000022681800 nid=0x21874 waiting on condition [0x0000000000000000]</span><br><span class="line">   java.lang.Thread.State: RUNNABLE</span><br><span class="line"></span><br><span class="line">   Locked ownable synchronizers:</span><br><span class="line">        - None</span><br><span class="line"></span><br><span class="line">&quot;C2 CompilerThread1&quot; #10 daemon prio=9 os_prio=2 tid=0x0000000022678800 nid=0x24288 waiting on condition [0x0000000000000000]</span><br><span class="line">   java.lang.Thread.State: RUNNABLE</span><br><span class="line"></span><br><span class="line">   Locked ownable synchronizers:</span><br><span class="line">        - None</span><br><span class="line"></span><br><span class="line">&quot;C2 CompilerThread0&quot; #9 daemon prio=9 os_prio=2 tid=0x0000000022661000 nid=0x21870 waiting on condition [0x0000000000000000]</span><br><span class="line">   java.lang.Thread.State: RUNNABLE</span><br><span class="line"></span><br><span class="line">   Locked ownable synchronizers:</span><br><span class="line">        - None</span><br><span class="line"></span><br><span class="line">&quot;JDWP Command Reader&quot; #8 daemon prio=10 os_prio=0 tid=0x00000000225fe000 nid=0x21864 runnable [0x0000000000000000]</span><br><span class="line">   java.lang.Thread.State: RUNNABLE</span><br><span class="line"></span><br><span class="line">   Locked ownable synchronizers:</span><br><span class="line">        - None</span><br><span class="line"></span><br><span class="line">&quot;JDWP Event Helper Thread&quot; #7 daemon prio=10 os_prio=0 tid=0x00000000225fa800 nid=0x21834 runnable [0x0000000000000000]</span><br><span class="line">   java.lang.Thread.State: RUNNABLE</span><br><span class="line"></span><br><span class="line">   Locked ownable synchronizers:</span><br><span class="line">        - None</span><br><span class="line"></span><br><span class="line">&quot;JDWP Transport Listener: dt_socket&quot; #6 daemon prio=10 os_prio=0 tid=0x0000000021291800 nid=0x21854 runnable [0x0000000000000000]</span><br><span class="line">   java.lang.Thread.State: RUNNABLE</span><br><span class="line"></span><br><span class="line">   Locked ownable synchronizers:</span><br><span class="line">        - None</span><br><span class="line"></span><br><span class="line">&quot;Attach Listener&quot; #5 daemon prio=5 os_prio=2 tid=0x00000000212dc800 nid=0x21860 waiting on condition [0x0000000000000000]</span><br><span class="line">   java.lang.Thread.State: RUNNABLE</span><br><span class="line"></span><br><span class="line">   Locked ownable synchronizers:</span><br><span class="line">        - None</span><br><span class="line"></span><br><span class="line">&quot;Signal Dispatcher&quot; #4 daemon prio=9 os_prio=2 tid=0x00000000225f0800 nid=0x2184c runnable [0x0000000000000000]</span><br><span class="line">   java.lang.Thread.State: RUNNABLE</span><br><span class="line"></span><br><span class="line">   Locked ownable synchronizers:</span><br><span class="line">        - None</span><br><span class="line"></span><br><span class="line">&quot;Finalizer&quot; #3 daemon prio=8 os_prio=1 tid=0x000000002126a000 nid=0x240c4 in Object.wait() [0x00000000225df000]</span><br><span class="line">   java.lang.Thread.State: WAITING (on object monitor)</span><br><span class="line">        at java.lang.Object.wait(Native Method)</span><br><span class="line">        - waiting on &lt;0x0000000740988ec0&gt; (a java.lang.ref.ReferenceQueue$Lock)</span><br><span class="line">        at java.lang.ref.ReferenceQueue.remove(ReferenceQueue.java:143)</span><br><span class="line">        - locked &lt;0x0000000740988ec0&gt; (a java.lang.ref.ReferenceQueue$Lock)</span><br><span class="line">        at java.lang.ref.ReferenceQueue.remove(ReferenceQueue.java:164)</span><br><span class="line">        at java.lang.ref.Finalizer$FinalizerThread.run(Finalizer.java:212)</span><br><span class="line"></span><br><span class="line">   Locked ownable synchronizers:</span><br><span class="line">        - None</span><br><span class="line"></span><br><span class="line">&quot;Reference Handler&quot; #2 daemon prio=10 os_prio=2 tid=0x0000000021249800 nid=0x24090 in Object.wait() [0x00000000224de000]</span><br><span class="line">   java.lang.Thread.State: WAITING (on object monitor)</span><br><span class="line">        at java.lang.Object.wait(Native Method)</span><br><span class="line">        - waiting on &lt;0x0000000740986b68&gt; (a java.lang.ref.Reference$Lock)</span><br><span class="line">        at java.lang.Object.wait(Object.java:502)</span><br><span class="line">        at java.lang.ref.Reference.tryHandlePending(Reference.java:191)</span><br><span class="line">        - locked &lt;0x0000000740986b68&gt; (a java.lang.ref.Reference$Lock)</span><br><span class="line">        at java.lang.ref.Reference$ReferenceHandler.run(Reference.java:153)</span><br><span class="line"></span><br><span class="line">   Locked ownable synchronizers:</span><br><span class="line">        - None</span><br><span class="line"></span><br><span class="line">&quot;VM Thread&quot; os_prio=2 tid=0x0000000021246800 nid=0x218a0 runnable</span><br><span class="line"></span><br><span class="line">&quot;GC task thread#0 (ParallelGC)&quot; os_prio=0 tid=0x0000000003119000 nid=0x240ac runnable</span><br><span class="line"></span><br><span class="line">&quot;GC task thread#1 (ParallelGC)&quot; os_prio=0 tid=0x000000000311a800 nid=0x2188c runnable</span><br><span class="line"></span><br><span class="line">&quot;GC task thread#2 (ParallelGC)&quot; os_prio=0 tid=0x000000000311c000 nid=0x2189c runnable</span><br><span class="line"></span><br><span class="line">&quot;GC task thread#3 (ParallelGC)&quot; os_prio=0 tid=0x000000000311e000 nid=0x2409c runnable</span><br><span class="line"></span><br><span class="line">&quot;VM Periodic Task Thread&quot; os_prio=2 tid=0x00000000227c9000 nid=0x240f8 waiting on condition</span><br><span class="line"></span><br><span class="line">JNI global references: 1880</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">Found one Java-level deadlock:</span><br><span class="line">=============================</span><br><span class="line">&quot;Thread-1&quot;:</span><br><span class="line">  waiting to lock monitor 0x00000000031fdbd8 (object 0x0000000740da48c0, a java.lang.String),</span><br><span class="line">  which is held by &quot;Thread-0&quot;</span><br><span class="line">&quot;Thread-0&quot;:</span><br><span class="line">  waiting to lock monitor 0x00000000031fc5d8 (object 0x0000000740da48f0, a java.lang.String),</span><br><span class="line">  which is held by &quot;Thread-1&quot;</span><br><span class="line"></span><br><span class="line">Java stack information for the threads listed above:</span><br><span class="line">===================================================</span><br><span class="line">&quot;Thread-1&quot;:</span><br><span class="line">        at com.exp.App$2.run(App.java:34)</span><br><span class="line">        - waiting to lock &lt;0x0000000740da48c0&gt; (a java.lang.String)</span><br><span class="line">        - locked &lt;0x0000000740da48f0&gt; (a java.lang.String)</span><br><span class="line">        at java.lang.Thread.run(Thread.java:748)</span><br><span class="line">&quot;Thread-0&quot;:</span><br><span class="line">        at com.exp.App$1.run(App.java:19)</span><br><span class="line">        - waiting to lock &lt;0x0000000740da48f0&gt; (a java.lang.String)</span><br><span class="line">        - locked &lt;0x0000000740da48c0&gt; (a java.lang.String)</span><br><span class="line">        at java.lang.Thread.run(Thread.java:748)</span><br><span class="line"></span><br><span class="line">Found 1 deadlock.</span><br></pre></td></tr></table></figure>

<p>通过观察可以发现 Thread-0、Thread-1 两个线程均处于 <code>BLOCKED</code> 状态，而且堆栈信息的最后明确提示了 <code>Found 1 deadlock.</code>，相关锁定的代码行也都有非常明确的提示，相关提示如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Found one Java-level deadlock:</span><br><span class="line">=============================</span><br><span class="line">&quot;Thread-1&quot;:</span><br><span class="line">  waiting to lock monitor 0x00000000031fdbd8 (object 0x0000000740da48c0, a java.lang.String),</span><br><span class="line">  which is held by &quot;Thread-0&quot;</span><br><span class="line">&quot;Thread-0&quot;:</span><br><span class="line">  waiting to lock monitor 0x00000000031fc5d8 (object 0x0000000740da48f0, a java.lang.String),</span><br><span class="line">  which is held by &quot;Thread-1&quot;</span><br><span class="line"></span><br><span class="line">Java stack information for the threads listed above:</span><br><span class="line">===================================================</span><br><span class="line">&quot;Thread-1&quot;:</span><br><span class="line">        at com.exp.App$2.run(App.java:34)</span><br><span class="line">        - waiting to lock &lt;0x0000000740da48c0&gt; (a java.lang.String)</span><br><span class="line">        - locked &lt;0x0000000740da48f0&gt; (a java.lang.String)</span><br><span class="line">        at java.lang.Thread.run(Thread.java:748)</span><br><span class="line">&quot;Thread-0&quot;:</span><br><span class="line">        at com.exp.App$1.run(App.java:19)</span><br><span class="line">        - waiting to lock &lt;0x0000000740da48f0&gt; (a java.lang.String)</span><br><span class="line">        - locked &lt;0x0000000740da48c0&gt; (a java.lang.String)</span><br><span class="line">        at java.lang.Thread.run(Thread.java:748)</span><br><span class="line"></span><br><span class="line">Found 1 deadlock.</span><br></pre></td></tr></table></figure>

<h2 id="arthas-分析"><a href="#arthas-分析" class="headerlink" title="arthas 分析"></a>arthas 分析</h2><p><code>Arthas</code> 是 Alibaba 开源的 Java 诊断工具，深受开发者喜爱。在线排查问题，无需重启；动态跟踪 Java 代码；实时监控 JVM 状态。详情可参考<a href="https://alibaba.github.io/arthas/">官网</a></p>
<p>启动 arthas 并选择我们的程序。</p>
<p>由于我们是要查询线程死锁问题，故需要用到 <code>thread</code> 命令。</p>
<p>Useage：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">USAGE:</span><br><span class="line">   thread [-h] [-b] [-i &lt;value&gt;] [-n &lt;value&gt;] [id]</span><br><span class="line"></span><br><span class="line"> SUMMARY:</span><br><span class="line">   Display thread info, thread stack</span><br><span class="line"></span><br><span class="line"> EXAMPLES:</span><br><span class="line">   thread</span><br><span class="line">   thread 51</span><br><span class="line">   thread -n -1</span><br><span class="line">   thread -n 5</span><br><span class="line">   thread -b</span><br><span class="line">   thread -i 2000</span><br><span class="line"></span><br><span class="line"> WIKI:</span><br><span class="line">   https://alibaba.github.io/arthas/thread</span><br><span class="line"></span><br><span class="line"> OPTIONS:</span><br><span class="line"> -h, --help                                                                     this help</span><br><span class="line"> -b, --include-blocking-thread                                                  Find the thread who is holding a lock that blocks the most number of threads.</span><br><span class="line"> -i, --sample-interval &lt;value&gt;                                                  Specify the sampling interval (in ms) when calculating cpu usage.</span><br><span class="line"> -n, --top-n-threads &lt;value&gt;                                                    The number of thread(s) to show, ordered by cpu utilization, -1 to show all.</span><br><span class="line"> &lt;id&gt;                                                                           Show thread stack</span><br></pre></td></tr></table></figure>

<p>从帮助信息中可知 <code>thread -b</code> 可以帮我们查询阻塞时间最长的线程。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_">[arthas@664776]$ </span><span class="language-bash">thread -b</span></span><br><span class="line">&quot;Thread-0&quot; Id=13 BLOCKED on java.lang.String@45db1404 owned by &quot;Thread-1&quot; Id=14</span><br><span class="line">    at com.exp.App$1.run(App.java:19)</span><br><span class="line">    -  blocked on java.lang.String@45db1404</span><br><span class="line">    -  locked java.lang.String@2fa93a25 &lt;---- but blocks 1 other threads!</span><br><span class="line">    at java.lang.Thread.run(Thread.java:748)</span><br><span class="line"></span><br><span class="line">Affect(row-cnt:0) cost in 22 ms.</span><br></pre></td></tr></table></figure>

<p>通过观察输出可以发现，相比 jstack，arthas 可以更加快速的定位到阻塞线程。</p>
]]></content>
      <tags>
        <tag>Java</tag>
        <tag>jstack</tag>
        <tag>死锁</tag>
      </tags>
  </entry>
  <entry>
    <title>javaagent 使用指南</title>
    <url>/2022/01/08/javaagent-%E4%BD%BF%E7%94%A8%E6%8C%87%E5%8D%97/</url>
    <content><![CDATA[<p>最近在做基于 JaCoCo 的代码覆盖率工具，了解到了一下 javaagent 相关的一些知识点。</p>
<h2 id="javaagent-是什么？"><a href="#javaagent-是什么？" class="headerlink" title="javaagent 是什么？"></a>javaagent 是什么？</h2><p>javaagent 是 java 命令的一个参数选项，可以用于加载 Java 语言的代理。</p>
<p>那么这个参数加载的 Java 语言的代理需要满足什么样的规范呢？</p>
<ol>
<li>代理 jar 包中的 MANIFEST.MF 文件需指定 Premain-Class；</li>
<li>Premain-Class 指定的类中必须实现 premain 方法。</li>
</ol>
]]></content>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java8 Stream</title>
    <url>/2019/06/15/java8-stream/</url>
    <content><![CDATA[<blockquote>
<p>A sequence of elements supporting sequential and parallel aggregate operations.</p>
</blockquote>
<p>从 <code>Stream</code> 的定义可以看出</p>
<ul>
<li>Stream 是一组元素的集合</li>
<li>Stream 支持顺序和并行地对元素进行操作</li>
</ul>
<h1 id="How-it-work"><a href="#How-it-work" class="headerlink" title="How it work"></a>How it work</h1><p>先看示例：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">method1</span><span class="params">()</span> &#123;</span><br><span class="line">    List&lt;String&gt; list = Stream.of(<span class="string">&quot;a&quot;</span>, <span class="string">&quot;b&quot;</span>, <span class="string">&quot;c&quot;</span>, <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>)</span><br><span class="line">            .peek(System.out::print)</span><br><span class="line">            .map(String::valueOf)</span><br><span class="line">            .sorted()</span><br><span class="line">            .collect(Collectors.toList());</span><br><span class="line">    System.out.println(<span class="string">&quot;\n&quot;</span> + JSON.toJSONString(list));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上述程序输出结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">abc123</span><br><span class="line">[&quot;1&quot;,&quot;2&quot;,&quot;3&quot;,&quot;a&quot;,&quot;b&quot;,&quot;c&quot;]</span><br></pre></td></tr></table></figure>

<p>通过上述例子：</p>
<ol>
<li><code>Stream.of()</code> 产生一个流；</li>
<li><code>peek</code> 将所有元素进行打印；</li>
<li><code>map</code> 将所有元素转化为 String；</li>
<li><code>sorted</code> 将所有元素进行排需；</li>
<li><code>collect</code> 将所有元素聚合为一个 List。</li>
</ol>
<p>由此总结对流的操作可分为 3 个阶段</p>
<ol>
<li>创建流</li>
<li>转化流（每次转化，原有的 Stream 不变，产生一个新的 Stream 对象，可以对流进行多次转化）</li>
<li>消耗流（获取最终的结果）</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">+--------------------+   +------+   +-----+   +--------+   +-------+</span><br><span class="line">| stream of elements |-&gt; | peek |-&gt; | map |-&gt; | sorted |-&gt; |collect|</span><br><span class="line">+--------------------+   +------+   +-----+   +--------+   +-------+</span><br></pre></td></tr></table></figure>

<p><code>Stream</code> 的操作就像一条流水线。我们可以对流进行不同地操作（如 <code>peek</code> 、 <code>map</code> 等），最终得到前面处理的结果。</p>
<h2 id="操作类型"><a href="#操作类型" class="headerlink" title="操作类型"></a>操作类型</h2><p><code>Stream</code> 的操作分为两大类 <em>中间操作（intermediate）</em> 和 _结束操作（terminal）_。</p>
<h3 id="中间操作"><a href="#中间操作" class="headerlink" title="中间操作"></a>中间操作</h3><p>中间操作可进一步区分为 <em>无状态（stateless）</em> 和 _有状态（stateful）_。</p>
<table>
<thead>
<tr>
<th></th>
<th>方法</th>
</tr>
</thead>
<tbody><tr>
<td>无状态</td>
<td>peek、filter、map、mapToInt、mapToLong、mapToDouble、flatMap、flatMapToInt、flatMapToLong、flatMapToDouble</td>
</tr>
<tr>
<td>有状态</td>
<td>sorted、distinct、limit、skip</td>
</tr>
</tbody></table>
<h3 id="结束操作"><a href="#结束操作" class="headerlink" title="结束操作"></a>结束操作</h3><p>结束操作中有一部分操作可以视为 _短路操作（short-circuiting）_。</p>
<table>
<thead>
<tr>
<th></th>
<th>方法</th>
</tr>
</thead>
<tbody><tr>
<td>非短路</td>
<td>forEach、forEachOrdered、toArray、collect、min、max、reduce、count</td>
</tr>
<tr>
<td>短路</td>
<td>anyMatch、noneMatch、findFirst、findAny</td>
</tr>
</tbody></table>
<h2 id="有什么优点"><a href="#有什么优点" class="headerlink" title="有什么优点"></a>有什么优点</h2><p>一般来说，Java8 之前的编程方式，对数组、集合中元素遍历是显示的在外部进行，我们称之为 _外部迭代_，而 <code>Stream</code> 的方式叫做<em>内部迭代</em>，采用了 <em>访问者模式（Visitor）</em> 实现。<code>外部迭代</code>我们的关注点是怎么做，<code>内部迭代</code>我们的关注点是做什么。通过 <code>Stream</code> 对元素的 <em>内部迭代</em> 我们更容易清晰的看到一个方法对元素做了什么操作。</p>
<h2 id="元素执行顺序"><a href="#元素执行顺序" class="headerlink" title="元素执行顺序"></a>元素执行顺序</h2><p>在 <code>Stream</code> 编程中，元素的执行顺序非常重要。</p>
<p>还是先看例子</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">method3</span><span class="params">()</span> &#123;</span><br><span class="line">    Stream.of(<span class="string">&quot;a&quot;</span>, <span class="string">&quot;b&quot;</span>, <span class="string">&quot;c&quot;</span>)</span><br><span class="line">            .peek(s -&gt; System.out.println(<span class="string">&quot;peek -&gt; &quot;</span> + s))</span><br><span class="line">            .map(s -&gt; &#123;</span><br><span class="line">                System.out.println(<span class="string">&quot;map -&gt; &quot;</span> + s);</span><br><span class="line">                <span class="keyword">return</span> s.toUpperCase();</span><br><span class="line">            &#125;)</span><br><span class="line">            .forEach(s -&gt; System.out.println(<span class="string">&quot;forEach -&gt; &quot;</span> + s));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>直接看这段程序的输出</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">peek -&gt; a</span><br><span class="line">map -&gt; a</span><br><span class="line">forEach -&gt; A</span><br><span class="line">peek -&gt; b</span><br><span class="line">map -&gt; b</span><br><span class="line">forEach -&gt; B</span><br><span class="line">peek -&gt; c</span><br><span class="line">map -&gt; c</span><br><span class="line">forEach -&gt; C</span><br></pre></td></tr></table></figure>

<p>是不是与想象中的不一样？</p>
<p><code>Stream</code> 对元素的操作是一个元素执行完所有的操作在执行下一个元素，而不是一个操作执行完所有的元素再进行下一个操作。这就更加证明了 <code>Stream</code> 对元素的操作是像流水线那样。</p>
<h3 id="为什么执行顺序如此重要"><a href="#为什么执行顺序如此重要" class="headerlink" title="为什么执行顺序如此重要"></a>为什么执行顺序如此重要</h3><p>通过上文，还不足以说明执行顺序为什么非常重要。</p>
<p>再看一个例子</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">method4</span><span class="params">()</span> &#123;</span><br><span class="line">    Stream.of(<span class="number">2</span>, <span class="number">4</span>, <span class="number">5</span>, <span class="number">3</span>, <span class="number">1</span>)</span><br><span class="line">            .sorted((i1, i2)-&gt;&#123;</span><br><span class="line">                System.out.format(<span class="string">&quot;sorted -&gt; %d %d \n&quot;</span>, i1, i2);</span><br><span class="line">                <span class="keyword">return</span> i1.compareTo(i2);</span><br><span class="line">            &#125;)</span><br><span class="line">            .map(i -&gt; &#123;</span><br><span class="line">                System.out.println(<span class="string">&quot;map -&gt; &quot;</span> + i);</span><br><span class="line">                <span class="keyword">return</span> i * <span class="number">2</span>;</span><br><span class="line">            &#125;)</span><br><span class="line">            .filter(i -&gt; &#123;</span><br><span class="line">                System.out.println(<span class="string">&quot;filter -&gt; &quot;</span> + i);</span><br><span class="line">                <span class="keyword">return</span> i &gt; <span class="number">8</span>;</span><br><span class="line">            &#125;)</span><br><span class="line">            .forEach(i -&gt; System.out.println(<span class="string">&quot;forEach -&gt; &quot;</span> + i));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>输出如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sorted -&gt; 4 2</span><br><span class="line">sorted -&gt; 5 4</span><br><span class="line">sorted -&gt; 3 5</span><br><span class="line">sorted -&gt; 3 4</span><br><span class="line">sorted -&gt; 3 2</span><br><span class="line">sorted -&gt; 1 4</span><br><span class="line">sorted -&gt; 1 3</span><br><span class="line">sorted -&gt; 1 2</span><br><span class="line">map -&gt; 1</span><br><span class="line">filter -&gt; 2</span><br><span class="line">map -&gt; 2</span><br><span class="line">filter -&gt; 4</span><br><span class="line">map -&gt; 3</span><br><span class="line">filter -&gt; 6</span><br><span class="line">map -&gt; 4</span><br><span class="line">filter -&gt; 8</span><br><span class="line">map -&gt; 5</span><br><span class="line">filter -&gt; 10</span><br><span class="line">forEach -&gt; 10</span><br></pre></td></tr></table></figure>

<p>从上述输出可以看出，每个元素都进行了 <code>sorted</code>、<code>map</code> 操作，那么我们对上述程序做一个变形，如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">method4</span><span class="params">()</span> &#123;</span><br><span class="line">    Stream.of(<span class="number">2</span>, <span class="number">4</span>, <span class="number">5</span>, <span class="number">3</span>, <span class="number">1</span>)</span><br><span class="line">            .filter(i -&gt; &#123;</span><br><span class="line">                System.out.println(<span class="string">&quot;filter -&gt; &quot;</span> + i);</span><br><span class="line">                <span class="keyword">return</span> i * <span class="number">2</span> &gt; <span class="number">8</span>;</span><br><span class="line">            &#125;)</span><br><span class="line">            .map(i -&gt; &#123;</span><br><span class="line">                System.out.println(<span class="string">&quot;map -&gt; &quot;</span> + i);</span><br><span class="line">                <span class="keyword">return</span> i * <span class="number">2</span>;</span><br><span class="line">            &#125;)</span><br><span class="line">            .sorted((i1, i2)-&gt;&#123;</span><br><span class="line">                System.out.format(<span class="string">&quot;sorted -&gt; %d %d \n&quot;</span>, i1, i2);</span><br><span class="line">                <span class="keyword">return</span> i1.compareTo(i2);</span><br><span class="line">            &#125;)</span><br><span class="line">            .forEach(i -&gt; System.out.println(<span class="string">&quot;forEach -&gt; &quot;</span> + i));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>变形之后该段程序输出为：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">filter -&gt; 2</span><br><span class="line">filter -&gt; 4</span><br><span class="line">filter -&gt; 5</span><br><span class="line">map -&gt; 5</span><br><span class="line">filter -&gt; 3</span><br><span class="line">filter -&gt; 1</span><br><span class="line">forEach -&gt; 10</span><br></pre></td></tr></table></figure>

<p>可以看出，元素[1, 2, 3, 4]并未进行 <code>map</code> 操作，所有的元素都未进行 <code>sorted</code> 操作，因为 <code>filter</code> 操作将不满足条件的元素都过滤掉了，只剩下一个元素，故不会进行 <code>sorted</code> 操作。</p>
<p>所以在使用<code>Stream</code>对元素进行操作时，<strong>合理的调整操作顺序，可以提升性能</strong>。</p>
<h2 id="延时性"><a href="#延时性" class="headerlink" title="延时性"></a>延时性</h2><p><code>Stream</code> 对元素的操作有一个非常重要的特性：延时性。为了更加直观的看到这个特性，我们先看一个例子</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">method5</span><span class="params">()</span> &#123;</span><br><span class="line">    Stream.of(<span class="string">&quot;a&quot;</span>, <span class="string">&quot;b&quot;</span>, <span class="string">&quot;c&quot;</span>).peek(System.out::print);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>按照我们的预期，上述程序应该输出</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">abc</span><br></pre></td></tr></table></figure>

<p>可是在实际执行过程中却发现并没有任何输出，这是为什么？</p>
<p>这是因为<strong>在对 stream 操作过程中，没有调用结束操作的时候，所有的中间操作都不会执行。</strong></p>
<h2 id="高阶操作"><a href="#高阶操作" class="headerlink" title="高阶操作"></a>高阶操作</h2><h3 id="短路操作"><a href="#短路操作" class="headerlink" title="短路操作"></a>短路操作</h3><p>在遇到 <em>短路操作</em> 时，一旦满足 <em>短路操作</em> 的条件时，<code>Stream</code> 会马上终止。例如：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">method6</span><span class="params">()</span> &#123;</span><br><span class="line">    Stream.of(<span class="string">&quot;x&quot;</span>, <span class="string">&quot;y&quot;</span>, <span class="string">&quot;z&quot;</span>)</span><br><span class="line">        .map(s -&gt; &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;map -&gt; &quot;</span> + s);</span><br><span class="line">            <span class="keyword">return</span> s.toUpperCase();</span><br><span class="line">        &#125;)</span><br><span class="line">        .anyMatch(s -&gt; &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;anyMatch -&gt; &quot;</span> + s);</span><br><span class="line">            <span class="keyword">return</span> s.startsWith(<span class="string">&quot;X&quot;</span>);</span><br><span class="line">        &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>输出：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">map -&gt; x</span><br><span class="line">anyMatch -&gt; X</span><br></pre></td></tr></table></figure>

<p>上述例子中，第一个元素即满足了 <code>anyMatch</code> 的条件，后续的元素便不再进行 <code>Stream</code> 上的各种操作，其余 <em>短路操作</em> 不再一一举例。</p>
<h3 id="collect"><a href="#collect" class="headerlink" title="collect"></a>collect</h3><p><code>collect</code>操作是十分常用的 _终止操作_，可以将流中的元素聚合成我们想要的集合类（如 <code>List</code>、<code>Set</code>、<code>Map</code> 等），也可以对元素做一些拼接、汇总等。下面的例子演示了 <code>collect</code> 一些常用的场景。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">method7</span><span class="params">()</span> &#123;</span><br><span class="line">    System.out.println(<span class="string">&quot;---聚合为List---&quot;</span>);</span><br><span class="line">    List&lt;String&gt; list = Stream.of(<span class="string">&quot;a&quot;</span>, <span class="string">&quot;b&quot;</span>, <span class="string">&quot;c&quot;</span>).collect(Collectors.toList());</span><br><span class="line">    list.forEach(System.out::print);</span><br><span class="line"></span><br><span class="line">    System.out.println(<span class="string">&quot;---拼接为字符串---&quot;</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">str</span> <span class="operator">=</span> Stream.of(<span class="string">&quot;x&quot;</span>, <span class="string">&quot;y&quot;</span>, <span class="string">&quot;z&quot;</span>).collect(Collectors.joining(<span class="string">&quot;,&quot;</span>));</span><br><span class="line">    System.out.println(str);</span><br><span class="line"></span><br><span class="line">    System.out.println(<span class="string">&quot;---聚合为汇总信息---&quot;</span>);</span><br><span class="line">    <span class="type">IntSummaryStatistics</span> <span class="variable">intSummaryStatistics</span> <span class="operator">=</span> Stream.of(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>).collect(Collectors.summarizingInt(value -&gt; value));</span><br><span class="line">    System.out.println(intSummaryStatistics);</span><br><span class="line"></span><br><span class="line">    System.out.println(<span class="string">&quot;---求平均值---&quot;</span>);</span><br><span class="line">    <span class="type">Double</span> <span class="variable">average</span> <span class="operator">=</span> Stream.of(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>).collect(Collectors.averagingInt(value -&gt; value));</span><br><span class="line">    System.out.println(average);</span><br><span class="line"></span><br><span class="line">    System.out.println(<span class="string">&quot;---分组---&quot;</span>);</span><br><span class="line">    Map&lt;Integer, List&lt;Integer&gt;&gt; map1 = Stream.of(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>).collect(Collectors.groupingBy(value -&gt; value));</span><br><span class="line">    System.out.println(JSON.toJSONString(map1));</span><br><span class="line"></span><br><span class="line">    System.out.println(<span class="string">&quot;---将元素聚合为Map--&quot;</span>);</span><br><span class="line">    Map&lt;Integer, String&gt; map2 = Stream.of(<span class="string">&quot;a&quot;</span>, <span class="string">&quot;b&quot;</span>, <span class="string">&quot;c&quot;</span>).collect(Collectors.toMap(String::hashCode, Function.identity()));</span><br><span class="line">    System.out.println(JSON.toJSONString(map2));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="flatMap"><a href="#flatMap" class="headerlink" title="flatMap"></a>flatMap</h3><p>通过上文，我们已经知道如何使用 <code>map</code>，但是有的时候 <code>map</code> 使用起来十分受限，因为 <code>map</code> 只能将一个对象映射为另一个对象，但是当我们希望将一个对象转化对多个或零个对象的时候，就需要用到 <code>flatMap</code> 。</p>
<p>看例子：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">method8</span><span class="params">()</span> &#123;</span><br><span class="line">    List&lt;Foo&gt; foos = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">    IntStream.range(<span class="number">1</span>, <span class="number">4</span>).forEach(i -&gt; foos.add(<span class="keyword">new</span> <span class="title class_">Foo</span>(<span class="string">&quot;Foo&quot;</span> + <span class="string">&quot; -&gt; &quot;</span> + i)));</span><br><span class="line">    foos.forEach(foo -&gt; IntStream.range(<span class="number">1</span>, <span class="number">4</span>).forEach(i -&gt; foo.bars.add(<span class="keyword">new</span> <span class="title class_">Bar</span>(foo.name + <span class="string">&quot; -&gt; &quot;</span> + <span class="string">&quot;Bar&quot;</span> + <span class="string">&quot; -&gt; &quot;</span> + i))));</span><br><span class="line">    List&lt;Bar&gt; bars = foos.stream()</span><br><span class="line">            .flatMap(foo -&gt; foo.bars.stream())</span><br><span class="line">            .collect(Collectors.toList());</span><br><span class="line">    bars.forEach(bar -&gt; System.out.println(bar.name));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Foo</span> &#123;</span><br><span class="line">    String name;</span><br><span class="line">    List&lt;Bar&gt; bars = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Foo</span><span class="params">(String name)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Bar</span> &#123;</span><br><span class="line">    String name;</span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Bar</span><span class="params">(String name)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>输出：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Foo -&gt; 1 -&gt; Bar -&gt; 1</span><br><span class="line">Foo -&gt; 1 -&gt; Bar -&gt; 2</span><br><span class="line">Foo -&gt; 1 -&gt; Bar -&gt; 3</span><br><span class="line">Foo -&gt; 2 -&gt; Bar -&gt; 1</span><br><span class="line">Foo -&gt; 2 -&gt; Bar -&gt; 2</span><br><span class="line">Foo -&gt; 2 -&gt; Bar -&gt; 3</span><br><span class="line">Foo -&gt; 3 -&gt; Bar -&gt; 1</span><br><span class="line">Foo -&gt; 3 -&gt; Bar -&gt; 2</span><br><span class="line">Foo -&gt; 3 -&gt; Bar -&gt; 3</span><br></pre></td></tr></table></figure>

<p>可以看出，上述例子通过 <code>flatMap</code> 操作，我们成功地将一个含有 3 个 foo 元素的流转化为了含有 9 个 bar 元素的流。</p>
<h3 id="reduce"><a href="#reduce" class="headerlink" title="reduce"></a>reduce</h3><p><code>reduce</code> 操作是将所有流中的元素组合为单一结果。</p>
<p>下面例子演示了两种常见的规约操作。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">method9</span><span class="params">()</span> &#123;</span><br><span class="line">    List&lt;Foo&gt; foos = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">    IntStream.range(<span class="number">1</span>, <span class="number">4</span>).forEach(i -&gt; foos.add(<span class="keyword">new</span> <span class="title class_">Foo</span>(<span class="string">&quot;Foo&quot;</span> + <span class="string">&quot; -&gt; &quot;</span> + i)));</span><br><span class="line">    foos.forEach(foo -&gt; IntStream.range(<span class="number">1</span>, <span class="number">4</span>).forEach(i -&gt; foo.bars.add(<span class="keyword">new</span> <span class="title class_">Bar</span>(foo.name + <span class="string">&quot; -&gt; &quot;</span> + <span class="string">&quot;Bar&quot;</span> + <span class="string">&quot; -&gt; &quot;</span> + i))));</span><br><span class="line">    System.out.println(<span class="string">&quot;---foos中的元素---&quot;</span>);</span><br><span class="line">    foos.forEach(o -&gt; System.out.println(JSON.toJSONString(o)));</span><br><span class="line">    System.out.println(<span class="string">&quot;---foos进行规约之后---&quot;</span>);</span><br><span class="line">    foos.stream()</span><br><span class="line">            .reduce((foo1, foo2) -&gt; foo1.name.hashCode() &gt; foo2.name.hashCode() ? foo1 : foo2)</span><br><span class="line">            .ifPresent(foo -&gt; System.out.println(JSON.toJSONString(foo)));</span><br><span class="line"></span><br><span class="line">    System.out.println(<span class="string">&quot;---foos将元素规约成一个新的对象---&quot;</span>);</span><br><span class="line">    <span class="type">Foo</span> <span class="variable">foo</span> <span class="operator">=</span> foos.stream()</span><br><span class="line">            .reduce(<span class="keyword">new</span> <span class="title class_">Foo</span>(<span class="string">&quot;&quot;</span>), (foo1, foo2) -&gt; &#123;</span><br><span class="line">                foo1.name += foo2.name;</span><br><span class="line">                <span class="keyword">return</span> foo1;</span><br><span class="line">            &#125;);</span><br><span class="line">    System.out.println(JSON.toJSONString(foo));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="并行流"><a href="#并行流" class="headerlink" title="并行流"></a>并行流</h2><p>从流的定义中我们得知流是支持并行地对元素进行操作地。在流中存在大量元素地情况下，使用并行流可以极大的提升运行时的性能。</p>
<p>并行流使用公共的<code>ForkJoinPool</code>，由<code>ForkJoinPool.commonPool()</code>方法提供。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">ForkJoinPool</span> <span class="variable">commonPool</span> <span class="operator">=</span> ForkJoinPool.commonPool();</span><br><span class="line">System.out.println(commonPool.getParallelism());</span><br></pre></td></tr></table></figure>

<p>通过上述方法可以查看公共池的默认线程数，也可以通过 JVM 参数<code>-Djava.util.concurrent.ForkJoinPool.common.parallelism=5</code>来调整。</p>
<p>看下面的例子：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">method10</span><span class="params">()</span> &#123;</span><br><span class="line">    List&lt;String&gt; list = Arrays.asList(<span class="string">&quot;a&quot;</span>, <span class="string">&quot;b&quot;</span>, <span class="string">&quot;c&quot;</span>, <span class="string">&quot;d&quot;</span>)</span><br><span class="line">            .parallelStream()</span><br><span class="line">            .filter(s -&gt; &#123;</span><br><span class="line">                System.out.format(<span class="string">&quot;filter: %s [%s]\n&quot;</span>, s, Thread.currentThread().getName());</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    Thread.sleep(<span class="number">10</span>);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">            &#125;)</span><br><span class="line">            .map(s -&gt; &#123;</span><br><span class="line">                System.out.format(<span class="string">&quot;map: %s [%s]\n&quot;</span>, s, Thread.currentThread().getName());</span><br><span class="line">                <span class="keyword">return</span> s.toUpperCase();</span><br><span class="line">            &#125;)</span><br><span class="line">            .collect(Collectors.toList());</span><br><span class="line">    list.forEach(System.out::print);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>输出：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">filter: c [main]</span><br><span class="line">filter: b [ForkJoinPool.commonPool-worker-2]</span><br><span class="line">filter: a [ForkJoinPool.commonPool-worker-3]</span><br><span class="line">filter: d [ForkJoinPool.commonPool-worker-1]</span><br><span class="line">map: c [main]</span><br><span class="line">map: d [ForkJoinPool.commonPool-worker-1]</span><br><span class="line">map: b [ForkJoinPool.commonPool-worker-2]</span><br><span class="line">map: a [ForkJoinPool.commonPool-worker-3]</span><br><span class="line">ABCD</span><br></pre></td></tr></table></figure>

<p>上述例子中，为了使对元素的操作能在 <code>ForkJoinPool.commonPool-worker-*</code> 的线程上执行，我们针对 <code>filter</code> 操作进行了 _sleep_，通过观察发现并行流使用的线程确实是由 <code>ForkJoinPool.commonPool()</code> 提供。</p>
<p>让我们对上述程序增加一个 <code>sorted</code> 操作，看会发生什么变化：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">method11</span><span class="params">()</span> &#123;</span><br><span class="line">    System.out.println(<span class="string">&quot;\n-----&quot;</span>);</span><br><span class="line">    Arrays.asList(<span class="string">&quot;a&quot;</span>, <span class="string">&quot;b&quot;</span>, <span class="string">&quot;c&quot;</span>, <span class="string">&quot;d&quot;</span>)</span><br><span class="line">            .parallelStream()</span><br><span class="line">            .filter(s -&gt; &#123;</span><br><span class="line">                System.out.format(<span class="string">&quot;filter: %s [%s]\n&quot;</span>, s, Thread.currentThread().getName());</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    Thread.sleep(<span class="number">10</span>);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">            &#125;)</span><br><span class="line">            .map(s -&gt; &#123;</span><br><span class="line">                System.out.format(<span class="string">&quot;map: %s [%s]\n&quot;</span>, s, Thread.currentThread().getName());</span><br><span class="line">                <span class="keyword">return</span> s.toUpperCase();</span><br><span class="line">            &#125;)</span><br><span class="line">            .sorted((s1, s2) -&gt; &#123;</span><br><span class="line">                System.out.format(<span class="string">&quot;sort: %s &lt;&gt; %s [%s]\n&quot;</span>, s1, s2, Thread.currentThread().getName());</span><br><span class="line">                <span class="keyword">return</span> s1.compareTo(s2);</span><br><span class="line">            &#125;)</span><br><span class="line">            .collect(Collectors.toList()).forEach(System.out::print);</span><br><span class="line">    System.out.println(<span class="string">&quot;\n&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>输出：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">filter: d [ForkJoinPool.commonPool-worker-1]</span><br><span class="line">filter: c [main]</span><br><span class="line">filter: b [ForkJoinPool.commonPool-worker-3]</span><br><span class="line">filter: a [ForkJoinPool.commonPool-worker-2]</span><br><span class="line">map: c [main]</span><br><span class="line">map: d [ForkJoinPool.commonPool-worker-1]</span><br><span class="line">map: a [ForkJoinPool.commonPool-worker-2]</span><br><span class="line">map: b [ForkJoinPool.commonPool-worker-3]</span><br><span class="line">sort: B &lt;&gt; A [main]</span><br><span class="line">sort: C &lt;&gt; B [main]</span><br><span class="line">sort: D &lt;&gt; C [main]</span><br><span class="line">ABCD</span><br></pre></td></tr></table></figure>

<p>我们发现在经经过 <code>sorted</code> 操作时，全部元素都在 <code>main</code> 线程中执行了。实际上，并行流上的 <code>sorted</code> 在背后使用了 Java8 中新的方法 <code>Arrays.parallelSort()</code>，这个方法会根据流中元素的长度来决定是以串行还是并行的方式来执行。</p>
]]></content>
      <categories>
        <category>Java8</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>Java8</tag>
        <tag>Stream</tag>
      </tags>
  </entry>
  <entry>
    <title>Jib - Make docker simple again</title>
    <url>/2019/06/26/jib-make-docker-simple-again/</url>
    <content><![CDATA[<p>在 <a href="https://fuyongde.github.io/2019/06/25/Dockerizing-Spring-Boot-Application/">使用 Docker 构建 Spring Boot Application</a> 中简单介绍了如何使用 <code>Docker</code> 构建 <code>Spring Boot Application</code>。本文探讨如何更简单的构建 <code>Docker</code> 镜像。</p>
<h2 id="jib-简介"><a href="#jib-简介" class="headerlink" title="jib 简介"></a>jib 简介</h2><blockquote>
<p>Make America Great Again. – Donald Trump</p>
</blockquote>
<p>大概是受 <code>Donald Trump</code> 的启发 🙄，Google 发布的 <code>jib</code> 让原本已经很简单的 <code>Docker</code> 镜像构建过程变得更加简单。</p>
<p>一图顶千言，先看 <code>Docker</code> 构建镜像流程</p>
<img src="/2019/06/26/jib-make-docker-simple-again/docker_process.jpg" class="" title="Docker 构建容器镜像的流程">

<p>再看 <code>Jib</code> 构建容器镜像流程</p>
<img src="/2019/06/26/jib-make-docker-simple-again/jib_process.jpg" class="" title="Jib 构建容器镜像的流程">

<h2 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h2><p>下面来介绍以下如何使用 <code>Jib</code></p>
<h3 id="编写-Spring-Boot-Application"><a href="#编写-Spring-Boot-Application" class="headerlink" title="编写 Spring Boot Application"></a>编写 Spring Boot Application</h3><p>略</p>
<h3 id="配置-Jib-插件"><a href="#配置-Jib-插件" class="headerlink" title="配置 Jib 插件"></a>配置 Jib 插件</h3><p>在项目的 <code>pom.xml</code> 文件中，新增如下配置</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">plugin</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.google.cloud.tools<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>jib-maven-plugin<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.3.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">configuration</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">from</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">image</span>&gt;</span>openjdk:8u212-jdk-alpine<span class="tag">&lt;/<span class="name">image</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">from</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">to</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">image</span>&gt;</span>registry.cn-hangzhou.aliyuncs.com/fuyongde/ebony-maw:$&#123;project.version&#125;<span class="tag">&lt;/<span class="name">image</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">to</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">container</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">jvmFlags</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">jvmFlag</span>&gt;</span>-Xms128m<span class="tag">&lt;/<span class="name">jvmFlag</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">jvmFlag</span>&gt;</span>-Xmx128m<span class="tag">&lt;/<span class="name">jvmFlag</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">jvmFlags</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">container</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">configuration</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">executions</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">execution</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">phase</span>&gt;</span>package<span class="tag">&lt;/<span class="name">phase</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">goals</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">goal</span>&gt;</span>build<span class="tag">&lt;/<span class="name">goal</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">goals</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">execution</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">executions</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">plugin</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>其中</p>
<ul>
<li>from：等同于 <code>Dockerfile</code> 中的 <code>FROM</code></li>
<li>to：镜像容器服务的地址以及对应的 <code>tag</code>，这里我采用的是阿里云的地址</li>
<li>jvmFlag：容器启动时的 <code>JVM</code> 参数</li>
</ul>
<h3 id="构建"><a href="#构建" class="headerlink" title="构建"></a>构建</h3><p>在终端执行 <code>maven package</code> 命令，<code>Docker</code> 镜像就打包并上传好了 ✌。</p>
<p>至此，整个构建过程已经完成，我们甚至连 <code>Dockerfile</code> 都不用写，是不是超级简单 🤪。</p>
<p>如有需要，可参考本文中的 <a href="https://github.com/fuyongde/marvel/tree/master/ebony-maw">Demo</a> 。</p>
]]></content>
      <tags>
        <tag>Docker</tag>
        <tag>Spring Boot</tag>
        <tag>Jib</tag>
      </tags>
  </entry>
  <entry>
    <title>Jenkins-手册</title>
    <url>/2020/02/18/jenkins-%E6%89%8B%E5%86%8C/</url>
    <content><![CDATA[<p>Jenkins 是常见的 CI 平台</p>
<h2 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h2><h3 id="Windows-平台"><a href="#Windows-平台" class="headerlink" title="Windows 平台"></a>Windows 平台</h3><p>略</p>
<h3 id="CentOS-平台"><a href="#CentOS-平台" class="headerlink" title="CentOS 平台"></a>CentOS 平台</h3><p>更为详细的步骤及说明可查看<a href="https://pkg.jenkins.io/redhat-stable/">官网</a></p>
<h4 id="添加-jenkins-源"><a href="#添加-jenkins-源" class="headerlink" title="添加 jenkins 源"></a>添加 jenkins 源</h4><p>命令如下：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo wget -O /etc/yum.repos.d/jenkins.repo https://pkg.jenkins.io/redhat-stable/jenkins.repo</span><br><span class="line">sudo rpm --import https://pkg.jenkins.io/redhat-stable/jenkins.io.key</span><br></pre></td></tr></table></figure>

<h4 id="yum-安装"><a href="#yum-安装" class="headerlink" title="yum 安装"></a>yum 安装</h4><p>命令如下：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">在线安装的方式</span></span><br><span class="line">yum install jenkins</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">离线安装的方式，由于国内连接 jenkins 网站较慢，可先行下载 rpm 文件格式的离线安装包，采用离线的方式安装</span></span><br><span class="line">yum install jenkins-2.204.2-1.1.noarch.rpm</span><br></pre></td></tr></table></figure>

<h4 id="启动"><a href="#启动" class="headerlink" title="启动"></a>启动</h4><p>命令如下：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">systemctl start jenkins</span><br></pre></td></tr></table></figure>

<h4 id="解决插件中心安装慢的问题"><a href="#解决插件中心安装慢的问题" class="headerlink" title="解决插件中心安装慢的问题"></a>解决插件中心安装慢的问题</h4><p>第一次登陆 Jenkins 会出现初始化缓慢的问题，产生这个问题的原因有两个，一个是因为首次打开 Jenkins 会尝试连接 Google，另外一个原因是因为连接插件中心缓慢。</p>
<h5 id="将插件中心修改为清华的源"><a href="#将插件中心修改为清华的源" class="headerlink" title="将插件中心修改为清华的源"></a>将插件中心修改为清华的源</h5><p>修改文件 <code>$JENKINS_HOME/hudson.model.UpdateCenter.xml</code>，将 url 从 <code>https://updates.jenkins.io/update-center.json</code> 修改为 <code>https://mirrors.tuna.tsinghua.edu.cn/jenkins/updates/update-center.json</code></p>
<h5 id="将-Google-的地址修改为-Baidu"><a href="#将-Google-的地址修改为-Baidu" class="headerlink" title="将 Google 的地址修改为 Baidu"></a>将 Google 的地址修改为 Baidu</h5><p>修改文件 <code>$JENKINS_HOME/updates/default.json</code></p>
<ul>
<li>将 <code>connectionCheckUrl</code> 的值修改为 <code>http://www.baidu.com/</code></li>
<li>替换文件中 <code>updates.jenkins-ci.org/download</code> 为 <code>mirrors.tuna.tsinghua.edu.cn/jenkins</code></li>
</ul>
<p>未完待续…</p>
]]></content>
      <categories>
        <category>手册</category>
      </categories>
      <tags>
        <tag>未完待续</tag>
        <tag>Jenkins</tag>
        <tag>CI</tag>
        <tag>持续集成</tag>
      </tags>
  </entry>
  <entry>
    <title>JVM 手册</title>
    <url>/2019/11/10/jvm-%E6%89%8B%E5%86%8C/</url>
    <content><![CDATA[<p>记录常用的 JVM 参数，持续更新 …</p>
<h2 id="堆内存设置"><a href="#堆内存设置" class="headerlink" title="堆内存设置"></a>堆内存设置</h2><p><code>-Xms128M</code> : 设置 java 程序启动时堆内存 128M（默认为物理内存 1&#x2F;64,且小于 1G），阿里 p3c 文档中推荐 Xmx 与 Xms 配置同样的大小，避免 GC 后调整堆大小带来的压力</p>
<p><code>-Xmx256M</code> : 设置最大堆内存 256M，超出后会出现 OutOfMemoryError（默认为物理内存 1&#x2F;64，且小于 1G）</p>
<p><code>-Xmn10M</code> : 设置新生代区域大小为 10M</p>
<p><code>-XX:NewSize=2M</code> : 设置新生代初始大小为 2M</p>
<p><code>-XX:MaxNewSize=2M</code> : 设置新生代最大值为 2M</p>
<p><code>-Xss1M</code> : 设置线程栈的大小 1M（默认 1M）</p>
<p><code>-XX:MinHeapFreeRatio=40</code> : 设置堆空间最小空闲比例（默认 40）（当 Xmx 与 Xms 相等时，该配置无效）</p>
<p><code>-XX:MaxHeapFreeRatio=70</code> : 设置堆空间最大空闲比例（默认 70）（当 Xmx 与 Xms 相等时，该配置无效）</p>
<p><code>-XX:NewRatio=2</code> : 设置年轻代与年老代的比例为 2:1</p>
<p><code>-XX:SurvivorRatio=8</code> : 设置年轻代中 eden 区与 survivor 区的比例为 8 : 1</p>
<p><code>-XX:MetaspaceSize=64M</code> : 设置元数据空间初始大小（取代-XX:PermSize）</p>
<p><code>-XX:MaxMetaspaceSize=128M</code> : 设置元数据空间最大值（取代之前-XX:MaxPermSize）</p>
<p><code>-XX:TargetSurvivorRatio=50</code> : 设置 survivor 区使用率。当 survivor 区达到 50%时，将对象送入老年代</p>
<p><code>-XX:+UseTLAB</code> : 在年轻代空间中使用本地线程分配缓冲区(TLAB)，默认开启</p>
<p><code>-XX:TLABSize=512k</code> : 设置 TLAB 大小为 512k</p>
<p><code>-XX:+UseCompressedOops</code> : 使用压缩指针，默认开启</p>
<p><code>-XX:MaxTenuringThreshold=15</code> : 对象进入老年代的年龄（Parallel 是 15，CMS 是 6）</p>
<h2 id="垃圾收集器相关参数"><a href="#垃圾收集器相关参数" class="headerlink" title="垃圾收集器相关参数"></a>垃圾收集器相关参数</h2><p><code>-XX:+UseSerialGC</code> : 启用 <code>Serial</code> 收集器，开启之后会默认启用 Serial（年轻代）+ Serial Old（年老代）的收集器组合。</p>
<p><code>-XX:+UseParNewGC</code> : 启用 <code>ParNew</code> 收集器，开启之后会默认启用 ParNew（年轻代）+ Serial Old（老年代，该收集器已过时）的收集器组合，新生代使用<strong>复制</strong>算法，老年代采用<strong>标记-整理</strong>算法。推荐配合老年代使用 <code>CMS</code> 收集器。</p>
<p><code>-XX:+UseParallelGC</code> : 启用 <code>Parallel</code> 收集器，Java8 默认启用该收集器，俗称吞吐量优先的收集器。开启之后默认启用 Parallel Scavenge（年轻代）+ Parallel Old（老年代）。与 ParNew 的区别在于自适应调节策略，虚拟机会根据当前系统的运行情况手机性能监控信息，动态调整参数以提供最合适的停顿时间或最大吞吐量。新生代采用<strong>复制</strong>算法，老年代采用<strong>标记-整理</strong>算法。</p>
<p><code>-XX:+UseParallelOleGC</code> : 启用 <code>Parallel</code> 收集器，与 <code>-XX:+UseParallelGC</code> 可相互激活。</p>
<p><code>-XX:+ParallelGCThreads</code> : 限制垃圾收集的线程数量，默认开始和 CPU 数量相同的线程数。</p>
<p><code>-XX:+UseConcMarkSweepGC</code> : 启用 <code>CMS</code> 收集器，开启该参数之后，默认打开 <code>-XX:+UseParNewGC</code>，启用 ParNew（年轻代）+ CMS（老年代）+ Serial Old（老年代）收集器的组合，Serial Old 将作为 CMS 出错的后备收集器。</p>
<p><code>-XX:+UseG1GC</code> : 启用 <code>G1</code> 收集器，<code>G1</code> 收集器不同于以往的收集器，<code>G1</code> 收集器既可以回收年轻代的垃圾也可以回收年老代的垃圾。</p>
<h2 id="GC-相关的参数"><a href="#GC-相关的参数" class="headerlink" title="GC 相关的参数"></a>GC 相关的参数</h2><p><code>-XX:+PrintGC</code> : 输出 GC 日志</p>
<p><code>-XX:+PrintGCDetails</code> : 输出 GC 的详细日志</p>
<p><code>-XX:+PrintGCTimeStamps</code> : 输出 GC 的时间戳（以基准时间的形式）</p>
<p><code>-XX:+PrintGCDateStamps</code> : 输出 GC 的时间戳（以日期的形式，如 2013-05-04T21:53:59.234+0800）</p>
<p><code>-XX:+PrintHeapAtGC</code> : 在进行 GC 的前后打印出堆的信息</p>
<p><code>-XX:+PrintGCApplicationStoppedTime</code> : 输出 GC 造成应用暂停的时间</p>
<p><code>-Xloggc:../logs/gc.log</code> : 日志文件的输出路径</p>
<h2 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h2><p><code>-XX:+HeapDumpOnOutOfMemoryError</code> : 让 JVM 碰到 OOM 场景时输出 dump 信息。 说明：OOM 的发生是有概率的，甚至有规律地相隔数月才出现一例，出现时的现场信息对查错 非常有价值。</p>
<p><code>-XX:MaxGCPauseMillis</code> : 设置每次年轻代垃圾回收的最长时间，如果无法满足此时间，JVM 会自动调整年轻代大小，以满足此值。</p>
<p><code>-XX:+TraceClassLoading</code> : 跟踪类的加载信息</p>
<p><code>-XX:+PrintCommandLineFlags</code> : 打印启动命令参数</p>
]]></content>
      <categories>
        <category>手册</category>
      </categories>
      <tags>
        <tag>未完待续</tag>
        <tag>JVM</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux 手册</title>
    <url>/2019/09/24/linux-%E6%89%8B%E5%86%8C/</url>
    <content><![CDATA[<p>记录一些常用的 Linux 操作，持续更新 …</p>
<h3 id="查看系统版本"><a href="#查看系统版本" class="headerlink" title="查看系统版本"></a>查看系统版本</h3><p>常用的查看系统版本有以下命令</p>
<ul>
<li><code>cat /etc/os-release</code></li>
<li><code>uname -a</code></li>
<li><code>cat /proc/version</code></li>
<li><code>cat /etc/issue</code></li>
</ul>
<p>下面演示一下 <code>cat /etc/os-release</code>，其他不详细说明。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">cat /etc/os-release</span><br></pre></td></tr></table></figure>

<p>输出如下</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">PRETTY_NAME=&quot;Debian GNU/Linux 9 (stretch)&quot;</span><br><span class="line">NAME=&quot;Debian GNU/Linux&quot;</span><br><span class="line">VERSION_ID=&quot;9&quot;</span><br><span class="line">VERSION=&quot;9 (stretch)&quot;</span><br><span class="line">ID=debian</span><br><span class="line">HOME_URL=&quot;https://www.debian.org/&quot;</span><br><span class="line">SUPPORT_URL=&quot;https://www.debian.org/support&quot;</span><br><span class="line">BUG_REPORT_URL=&quot;https://bugs.debian.org/&quot;</span><br></pre></td></tr></table></figure>

<h3 id="替换源"><a href="#替换源" class="headerlink" title="替换源"></a>替换源</h3><p>由于网络的原因，国内部分 Linux 在安装软件时非常缓慢，将源替换为国内的镜像可以方便很多。</p>
<p>国内常见的镜像如清华大学、阿里云、163 等等，大家可自行选择。由于大部分网络上的博客，只写了具体源的地址，对很多没有基础的同学来说，由于系统版本的差异，反而会适得其反。</p>
<p>替换源一定要遵循下面的步骤：</p>
<ol>
<li>确认操作系统版本。如何查看可参考上文。</li>
<li>备份默认的源。不同的操作系统版本，源的文件位置各不一样，直接采用备份文件的方式备份即可。</li>
<li>选择对应版本的源并替换。</li>
</ol>
<p>下面针对常见的 <code>Debian</code>、<code>Ubuntu</code>、<code>CentOS</code>，这里以说明一下如何正确的查找并替换源。</p>
<h4 id="Debian"><a href="#Debian" class="headerlink" title="Debian"></a>Debian</h4><p><code>Debian</code> 系统的源文件目录位于 <code>/etc/apt/sources.list</code>，这里我们以清华大学的镜像为例。清华大学开源镜像站 <a href="https://mirror.tuna.tsinghua.edu.cn/help/debian/">Debian 系统的帮助页面</a> 详细列举了常用版本的源的信息。</p>
<p>例如，我们系统的版本是 <code>9 (stretch)</code>，则选择 <code>stretch</code>，替换掉对应的源即可。</p>
<p>stretch 版本的源信息如下：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line"># 默认注释了源码镜像以提高 apt update 速度，如有需要可自行取消注释</span><br><span class="line">deb https://mirrors.tuna.tsinghua.edu.cn/debian/ stretch main contrib non-free</span><br><span class="line"># deb-src https://mirrors.tuna.tsinghua.edu.cn/debian/ stretch main contrib non-free</span><br><span class="line">deb https://mirrors.tuna.tsinghua.edu.cn/debian/ stretch-updates main contrib non-free</span><br><span class="line"># deb-src https://mirrors.tuna.tsinghua.edu.cn/debian/ stretch-updates main contrib non-free</span><br><span class="line">deb https://mirrors.tuna.tsinghua.edu.cn/debian/ stretch-backports main contrib non-free</span><br><span class="line"># deb-src https://mirrors.tuna.tsinghua.edu.cn/debian/ stretch-backports main contrib non-free</span><br><span class="line">deb https://mirrors.tuna.tsinghua.edu.cn/debian-security stretch/updates main contrib non-free</span><br><span class="line"># deb-src https://mirrors.tuna.tsinghua.edu.cn/debian-security stretch/updates main contrib non-free</span><br></pre></td></tr></table></figure>

<p>buster 版本的源信息如下</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line"># 默认注释了源码镜像以提高 apt update 速度，如有需要可自行取消注释</span><br><span class="line">deb https://mirrors.tuna.tsinghua.edu.cn/debian/ buster main contrib non-free</span><br><span class="line"># deb-src https://mirrors.tuna.tsinghua.edu.cn/debian/ buster main contrib non-free</span><br><span class="line">deb https://mirrors.tuna.tsinghua.edu.cn/debian/ buster-updates main contrib non-free</span><br><span class="line"># deb-src https://mirrors.tuna.tsinghua.edu.cn/debian/ buster-updates main contrib non-free</span><br><span class="line">deb https://mirrors.tuna.tsinghua.edu.cn/debian/ buster-backports main contrib non-free</span><br><span class="line"># deb-src https://mirrors.tuna.tsinghua.edu.cn/debian/ buster-backports main contrib non-free</span><br><span class="line">deb https://mirrors.tuna.tsinghua.edu.cn/debian-security buster/updates main contrib non-free</span><br><span class="line"># deb-src https://mirrors.tuna.tsinghua.edu.cn/debian-security buster/updates main contrib non-free</span><br></pre></td></tr></table></figure>

<p>其他版本不一一列举。</p>
<h4 id="Ubuntu"><a href="#Ubuntu" class="headerlink" title="Ubuntu"></a>Ubuntu</h4><p><code>Ubuntu</code> 也是基于 <code>Debian</code> 的，故源的目录位置也是 <code>/etc/apt/sources.list</code>。清华大学开源镜像站 <a href="https://mirror.tuna.tsinghua.edu.cn/help/ubuntu/">Ubuntu 系统的帮助页面</a> 详细列举了常用版本的源的信息。</p>
<p>例如，我们系统的版本是 <code>18.04 LTS</code>，则选择 <code>18.04 LTS</code>，替换掉对应的源即可。</p>
<p>18.04 LTS 版本的源信息如下：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line"># 默认注释了源码镜像以提高 apt update 速度，如有需要可自行取消注释</span><br><span class="line">deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ bionic main restricted universe multiverse</span><br><span class="line"># deb-src https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ bionic main restricted universe multiverse</span><br><span class="line">deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ bionic-updates main restricted universe multiverse</span><br><span class="line"># deb-src https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ bionic-updates main restricted universe multiverse</span><br><span class="line">deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ bionic-backports main restricted universe multiverse</span><br><span class="line"># deb-src https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ bionic-backports main restricted universe multiverse</span><br><span class="line">deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ bionic-security main restricted universe multiverse</span><br><span class="line"># deb-src https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ bionic-security main restricted universe multiverse</span><br><span class="line"></span><br><span class="line"># 预发布软件源，不建议启用</span><br><span class="line"># deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ bionic-proposed main restricted universe multiverse</span><br><span class="line"># deb-src https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ bionic-proposed main restricted universe multiverse</span><br></pre></td></tr></table></figure>

<p><code>Ubuntu</code> 若安装了图形界面，也可以采用图形界面的方式。</p>
<h5 id="图形界面配置"><a href="#图形界面配置" class="headerlink" title="图形界面配置"></a>图形界面配置</h5><p>新手推荐使用图形界面配置： 系统设置 -&gt; 软件和更新 选择下载服务器 -&gt; “mirrors.aliyun.com”</p>
<h4 id="CentOS"><a href="#CentOS" class="headerlink" title="CentOS"></a>CentOS</h4><p><code>CentOS</code> 的源文件位于 <code>/etc/yum.repos.d/CentOS-Base.repo</code>，清华大学开源镜像站 <a href="https://mirror.tuna.tsinghua.edu.cn/help/centos/">CentOS 系统的帮助页面</a> 详细列举了常用版本的源的信息。</p>
<p>与 Debian 的不同，CentOS 替换源之后，需要运行 <code>yum makecache</code> 生成缓存。</p>
<p>此外阿里云提供了更为简单的方式，直接下载阿里云的 <code>CentOS-Base.repo</code> 文件到对应目录即可，命令如下：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">CentOS 6</span></span><br><span class="line">wget -O /etc/yum.repos.d/CentOS-Base.repo http://mirrors.aliyun.com/repo/Centos-6.repo</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">或者</span></span><br><span class="line">curl -o /etc/yum.repos.d/CentOS-Base.repo http://mirrors.aliyun.com/repo/Centos-6.repo</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">CentOS 7</span></span><br><span class="line">wget -O /etc/yum.repos.d/CentOS-Base.repo http://mirrors.aliyun.com/repo/Centos-7.repo</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">或者</span></span><br><span class="line">curl -o /etc/yum.repos.d/CentOS-Base.repo http://mirrors.aliyun.com/repo/Centos-7.repo</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">由于非阿里云 ECS 用户会出现 Couldn<span class="string">&#x27;t resolve host &#x27;</span>mirrors.cloud.aliyuncs.com<span class="string">&#x27; 信息，不影响使用。用户也可自行修改相关配置: eg:</span></span></span><br><span class="line">sed -i -e &#x27;/mirrors.cloud.aliyuncs.com/d&#x27; -e &#x27;/mirrors.aliyuncs.com/d&#x27; /etc/yum.repos.d/CentOS-Base.repo</span><br></pre></td></tr></table></figure>

<p>CentOS 7 版本的清华大学源信息如下：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line"># CentOS-Base.repo</span><br><span class="line">#</span><br><span class="line"># The mirror system uses the connecting IP address of the client and the</span><br><span class="line"># update status of each mirror to pick mirrors that are updated to and</span><br><span class="line"># geographically close to the client.  You should use this for CentOS updates</span><br><span class="line"># unless you are manually picking other mirrors.</span><br><span class="line">#</span><br><span class="line"># If the mirrorlist= does not work for you, as a fall back you can try the</span><br><span class="line"># remarked out baseurl= line instead.</span><br><span class="line">#</span><br><span class="line">#</span><br><span class="line"></span><br><span class="line">[base]</span><br><span class="line">name=CentOS-$releasever - Base</span><br><span class="line">baseurl=https://mirrors.tuna.tsinghua.edu.cn/centos/$releasever/os/$basearch/</span><br><span class="line">#mirrorlist=http://mirrorlist.centos.org/?release=$releasever&amp;arch=$basearch&amp;repo=os</span><br><span class="line">gpgcheck=1</span><br><span class="line">gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-CentOS-7</span><br><span class="line"></span><br><span class="line">#released updates</span><br><span class="line">[updates]</span><br><span class="line">name=CentOS-$releasever - Updates</span><br><span class="line">baseurl=https://mirrors.tuna.tsinghua.edu.cn/centos/$releasever/updates/$basearch/</span><br><span class="line">#mirrorlist=http://mirrorlist.centos.org/?release=$releasever&amp;arch=$basearch&amp;repo=updates</span><br><span class="line">gpgcheck=1</span><br><span class="line">gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-CentOS-7</span><br><span class="line"></span><br><span class="line">#additional packages that may be useful</span><br><span class="line">[extras]</span><br><span class="line">name=CentOS-$releasever - Extras</span><br><span class="line">baseurl=https://mirrors.tuna.tsinghua.edu.cn/centos/$releasever/extras/$basearch/</span><br><span class="line">#mirrorlist=http://mirrorlist.centos.org/?release=$releasever&amp;arch=$basearch&amp;repo=extras</span><br><span class="line">gpgcheck=1</span><br><span class="line">gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-CentOS-7</span><br><span class="line"></span><br><span class="line">#additional packages that extend functionality of existing packages</span><br><span class="line">[centosplus]</span><br><span class="line">name=CentOS-$releasever - Plus</span><br><span class="line">baseurl=https://mirrors.tuna.tsinghua.edu.cn/centos/$releasever/centosplus/$basearch/</span><br><span class="line">#mirrorlist=http://mirrorlist.centos.org/?release=$releasever&amp;arch=$basearch&amp;repo=centosplus</span><br><span class="line">gpgcheck=1</span><br><span class="line">enabled=0</span><br><span class="line">gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-CentOS-7</span><br></pre></td></tr></table></figure>

<h3 id="配置免密登录"><a href="#配置免密登录" class="headerlink" title="配置免密登录"></a>配置免密登录</h3><p>场景：A 主机实现免密登录 B 主机</p>
<ol>
<li>A 主机生成公私钥对 <code>ssh-keygen</code>；</li>
<li>将生成的公钥传输给 B 机器 <code>scp /root/.ssh/id_rsp.pub remoteUser@remoteIp:remoteFolder</code>；</li>
<li>登录 B 机器，将 A 机器的公钥追加到 B 主机用户目录下的 <code>.ssh/authorized_keys</code> 文件，执行 <code>cat id_rsa.pub &gt;&gt; .ssh/.ssh/authorized_keys</code> 命令即可。若没有<code>authorized_keys</code>文件，则需要创建，且其权限要为 <code>600</code>。</li>
</ol>
<h3 id="查看系统负载"><a href="#查看系统负载" class="headerlink" title="查看系统负载"></a>查看系统负载</h3><p><code>top</code>：重点关注 CPU、内存的使用率，以及 average（平均负载，三个值分别是 CPU 最近 1 分钟、5 分钟、15 分钟的平均负载）</p>
<p><code>vmstat</code>：</p>
<h3 id="Linux-子系统"><a href="#Linux-子系统" class="headerlink" title="Linux 子系统"></a>Linux 子系统</h3><p>在 <code>Windows 10</code> 中使用 Linux 子系统时，经常会需要用到 <code>root</code> 账号，初始情况下，该账号时没有设定密码的，需要设定之后才能正常使用。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">设置 root 账户的密码</span></span><br><span class="line">sudo passwd</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">切换 root 账户</span></span><br><span class="line">su</span><br></pre></td></tr></table></figure>

<h3 id="软件管理"><a href="#软件管理" class="headerlink" title="软件管理"></a>软件管理</h3><h4 id="Ubuntu-软件管理"><a href="#Ubuntu-软件管理" class="headerlink" title="Ubuntu 软件管理"></a>Ubuntu 软件管理</h4><h5 id="查看安装了那些软件"><a href="#查看安装了那些软件" class="headerlink" title="查看安装了那些软件"></a>查看安装了那些软件</h5><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">查看安装了哪些软件包</span></span><br><span class="line">apt list --installed</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">也可以使用 dpkg 命令来查看安装了哪些软件包</span></span><br><span class="line">dpkg --list</span><br></pre></td></tr></table></figure>

<h5 id="卸载已经安装的软件"><a href="#卸载已经安装的软件" class="headerlink" title="卸载已经安装的软件"></a>卸载已经安装的软件</h5><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">卸载某个软件包</span></span><br><span class="line">apt remove packagename</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">也可以使用自动卸载</span></span><br><span class="line">apt autoremove packagename</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">使用 dpkg 命令卸载</span></span><br><span class="line">dpkg --remove packagename</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>手册</category>
      </categories>
      <tags>
        <tag>未完待续</tag>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Maven 手册</title>
    <url>/2019/06/22/maven-%E6%89%8B%E5%86%8C/</url>
    <content><![CDATA[<h2 id="什么是-Maven"><a href="#什么是-Maven" class="headerlink" title="什么是 Maven"></a>什么是 Maven</h2><blockquote>
<p>Apache Maven is a software project management and comprehension tool. Based on the concept of a project object model (POM), Maven can manage a project’s build, reporting and documentation from a central piece of information.</p>
</blockquote>
<p>Apache Maven 本质是一个软件项目管理和理解工具，它提供了一种项目管理的方法，涵盖了了项目管理中常见的阶段：</p>
<ul>
<li>Builds</li>
<li>Documentation</li>
<li>Reporting</li>
<li>Dependencies</li>
<li>SCMs</li>
<li>Releases</li>
<li>Distribution</li>
</ul>
<h2 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h2><p>从<a href="http://maven.apache.org/download.cgi">官网</a>下载安装包。</p>
<h3 id="基础环境"><a href="#基础环境" class="headerlink" title="基础环境"></a>基础环境</h3><p>Maven 依赖 <code>Java</code> 环境，所以要先确保已经正确安装 <code>JDK</code>。</p>
<p>通过 <code>java -version</code> 命令来查看是否正确安装了 <code>JDK</code></p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">PS C:\Users\fuyon&gt; java -version</span><br><span class="line">java version &quot;11.0.1&quot; 2018-10-16 LTS</span><br><span class="line">Java(TM) SE Runtime Environment 18.9 (build 11.0.1+13-LTS)</span><br><span class="line">Java HotSpot(TM) 64-Bit Server VM 18.9 (build 11.0.1+13-LTS, mixed mode)</span><br></pre></td></tr></table></figure>

<h3 id="Windows"><a href="#Windows" class="headerlink" title="Windows"></a>Windows</h3><h4 id="解压"><a href="#解压" class="headerlink" title="解压"></a>解压</h4><p>将下载好的安装包 <code>apache-maven-3.6.1-bin.zip</code> 解压至 <code>C:\</code>（或其他路径）。</p>
<h3 id="配置环境变量"><a href="#配置环境变量" class="headerlink" title="配置环境变量"></a>配置环境变量</h3><p>打开 <code>控制面板</code> &gt; <code>系统和安全</code> &gt; <code>系统</code> &gt; <code>高级系统设置</code> &gt; <code>环境变量</code> &gt; <code>系统变量</code></p>
<ol>
<li>新建一个<code>变量名</code>为 <code>M2_HOME</code>，变量值为 <code>C:\apache-maven-3.6.1</code> (即 Maven 的安装路径)。</li>
<li>选中<code>系统变量</code>中的 <code>Path</code>，并新增 <code>%M2_HOME%\bin\</code></li>
</ol>
<h4 id="验证"><a href="#验证" class="headerlink" title="验证"></a>验证</h4><p>打开终端，输入 <code>mvn --version</code>，输出</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">PS C:\Users\fuyongde&gt; mvn --version</span><br><span class="line">Apache Maven 3.5.2 (138edd61fd100ec658bfa2d307c43b76940a5d7d; 2017-10-18T15:58:13+08:00)</span><br><span class="line">Maven home: C:\apache-maven-3.5.2\bin\..</span><br><span class="line">Java version: 1.8.0_162, vendor: Oracle Corporation</span><br><span class="line">Java home: C:\Program Files\Java\jdk1.8.0_162\jre</span><br><span class="line">Default locale: zh_CN, platform encoding: GBK</span><br><span class="line">OS name: &quot;windows 10&quot;, version: &quot;10.0&quot;, arch: &quot;amd64&quot;, family: &quot;windows&quot;</span><br></pre></td></tr></table></figure>

<p>表示安装成功</p>
<h3 id="Linux-amp-Mac-OS"><a href="#Linux-amp-Mac-OS" class="headerlink" title="Linux &amp; Mac OS"></a>Linux &amp; Mac OS</h3><p>你都用这操作系统了，那肯定会安装吧 🤣。</p>
<h2 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h2><h3 id="快速开始"><a href="#快速开始" class="headerlink" title="快速开始"></a>快速开始</h3><p>Maven 提供了丰富的模板，以供我们快速创建一个工程。其命令如下：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">mvn archetype:generate</span><br><span class="line"> -DgroupId=com.demo</span><br><span class="line"> -DartifactId=hello-maven</span><br><span class="line"> -DarchetypeArtifactId=maven-archetype-quickstart</span><br><span class="line"> -DinteractiveMode=false</span><br></pre></td></tr></table></figure>

<p>其中各个参数的含义：</p>
<ul>
<li><code>-DgroupId</code>：组织的唯一标识符</li>
<li><code>-DartifactId</code>：项目的唯一标识符</li>
<li><code>-DarchetypeArtifactId</code>：指定 ArchetypeId，常见的选项 <code>maven-archetype-quickstart</code> (Java project)、<code>maven-archetype-webapp</code> (web project)</li>
<li><code>-DinteractiveMode</code>：是否使用交互模式</li>
</ul>
<h3 id="目录结构"><a href="#目录结构" class="headerlink" title="目录结构"></a>目录结构</h3><p>不同的模板创建出来的项目目录可能会有差异，常见的 Maven 目录结构如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">├── src</span><br><span class="line">│   ├── main</span><br><span class="line">│   │   ├── java # 源码目录</span><br><span class="line">│   │   └── resources # 资源目录</span><br><span class="line">│   └── test</span><br><span class="line">│       ├── java # 单元测试源码目录</span><br><span class="line">│       └── resources # 单元测试资源目录</span><br><span class="line">├── target # 项目编译输出的目录</span><br><span class="line">├── pom.xml</span><br></pre></td></tr></table></figure>

<h3 id="Maven-生命周期"><a href="#Maven-生命周期" class="headerlink" title="Maven 生命周期"></a>Maven 生命周期</h3><p>Maven 默认的生命周期包括：</p>
<ol>
<li><code>validate</code>: 验证项目是否正确，并提供所有必要的信息</li>
<li><code>compile</code>: 编译源码</li>
<li><code>test</code>: 使用合适的单元测试框架测试编译的源代码，单元测试的代码并不会被打包。</li>
<li><code>package</code>: 获取已编译的代码并将其打包为可分发的格式，例如 JAR。</li>
<li><code>integration-test</code>: 如有必要，将程序包处理并部署到可以运行集成测试的环境中</li>
<li><code>verify</code>: 验证打包文件是否有效并符合质量标准</li>
<li><code>install</code>: 将项目打包安装到本地存储库中，以便在本地用作其他项目的依赖项</li>
<li><code>deploy</code>: 将项目打包并推送到 Maven 私服或中央仓库</li>
</ol>
<p>此外还有两个生命周期的阶段</p>
<ul>
<li><code>clean</code>: 清除之前构建</li>
<li><code>site</code>: 为此项目生成站点文档</li>
</ul>
<h3 id="POM"><a href="#POM" class="headerlink" title="POM"></a>POM</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">POM stands for &quot;Project Object Model&quot;. It is an XML representation of a Maven project held in a file named pom.xml.</span><br></pre></td></tr></table></figure>

<p><code>POM</code> 表示“项目对象模型”，在 Maven 项目中以一个 <code>pom.xml</code> 文件的形式存在。</p>
<h4 id="最基本的构成"><a href="#最基本的构成" class="headerlink" title="最基本的构成"></a>最基本的构成</h4><p>一个 <code>pom.xml</code> 最基本的构成如下：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">project</span> <span class="attr">xmlns</span>=<span class="string">&quot;http://maven.apache.org/POM/4.0.0&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">xmlns:xsi</span>=<span class="string">&quot;http://www.w3.org/2001/XMLSchema-instance&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">xsi:schemaLocation</span>=<span class="string">&quot;http://maven.apache.org/POM/4.0.0</span></span></span><br><span class="line"><span class="string"><span class="tag">                      http://maven.apache.org/xsd/maven-4.0.0.xsd&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">modelVersion</span>&gt;</span>4.0.0<span class="tag">&lt;/<span class="name">modelVersion</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.codehaus.mojo<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>my-project<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>其中</p>
<ul>
<li><code>modelVersion</code>：<code>Maven2 &amp; 3</code>只支持 4.0.0</li>
<li><code>groupId</code>：组织的唯一标识符</li>
<li><code>artifactId</code>：项目的唯一标识符</li>
<li><code>version</code>：版本号</li>
</ul>
<h4 id="packaging"><a href="#packaging" class="headerlink" title="packaging"></a>packaging</h4><p>在 <code>pom.xml</code> 中可以添加 <code>&lt;packaging&gt;war&lt;/packaging&gt;</code> 来标识项目最终的打包格式，缺省值为：<code>jar</code>，可选值：<code>pom</code>, <code>jar</code>, <code>maven-plugin</code>, <code>ejb</code>, <code>war</code>, <code>ear</code>, <code>rar</code>。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">project</span> <span class="attr">xmlns</span>=<span class="string">&quot;http://maven.apache.org/POM/4.0.0&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">xmlns:xsi</span>=<span class="string">&quot;http://www.w3.org/2001/XMLSchema-instance&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">xsi:schemaLocation</span>=<span class="string">&quot;http://maven.apache.org/POM/4.0.0</span></span></span><br><span class="line"><span class="string"><span class="tag">                      http://maven.apache.org/xsd/maven-4.0.0.xsd&quot;</span>&gt;</span></span><br><span class="line">    ...</span><br><span class="line">    <span class="tag">&lt;<span class="name">packaging</span>&gt;</span>war<span class="tag">&lt;/<span class="name">packaging</span>&gt;</span></span><br><span class="line">    ...</span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="POM-Relationships（重点）"><a href="#POM-Relationships（重点）" class="headerlink" title="POM Relationships（重点）"></a>POM Relationships（重点）</h4><p>Maven 的一个强大的功能，就是它处理项目之间关系的能力，包括了：依赖项管理、继承和聚合。</p>
<h4 id="依赖"><a href="#依赖" class="headerlink" title="依赖"></a>依赖</h4><p>依赖管理是 <code>POM</code> 的灵魂，大部分的项目都会依赖于其他项目来构建以及正确的运行。通过依赖管理，我们可以方便的下载、引入所需的其他依赖项。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">project</span> <span class="attr">xmlns</span>=<span class="string">&quot;http://maven.apache.org/POM/4.0.0&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">xmlns:xsi</span>=<span class="string">&quot;http://www.w3.org/2001/XMLSchema-instance&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">xsi:schemaLocation</span>=<span class="string">&quot;http://maven.apache.org/POM/4.0.0</span></span></span><br><span class="line"><span class="string"><span class="tag">                      https://maven.apache.org/xsd/maven-4.0.0.xsd&quot;</span>&gt;</span></span><br><span class="line">  ...</span><br><span class="line">  <span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>junit<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>junit<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.12<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">type</span>&gt;</span>jar<span class="tag">&lt;/<span class="name">type</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">scope</span>&gt;</span>test<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">optional</span>&gt;</span>true<span class="tag">&lt;/<span class="name">optional</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">      ...</span><br><span class="line">  <span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br><span class="line">  ...</span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h5 id="groupId、artifactId"><a href="#groupId、artifactId" class="headerlink" title="groupId、artifactId"></a>groupId、artifactId</h5><p>表示依赖项的坐标。由于依赖项都是由 Maven 坐标，意味着我们的项目只能依赖于同样是 Maven 管理的其他项目。但是很多时候，项目可能会依赖一些具有闭源许可的 <code>jar</code>，这些 <code>jar</code> 并不存在于<code>中央仓库</code>，有三种办法可以解决这个问题。</p>
<ul>
<li>使用 <code>install</code> 插件在本地安装依赖项，这是最简单也是最推荐的方式。例如：</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">mvn install:install-file -Dfile=non-maven-proj.jar -DgroupId=some.group -DartifactId=non-maven-proj -Dversion=1 -Dpackaging=jar</span><br></pre></td></tr></table></figure>

<ul>
<li><p>将依赖部署到私服</p>
</li>
<li><p>将依赖的 <code>scope</code> 设置为 <code>system</code> 并且定义 <code>systemPath</code>，强烈不推荐这种方式。</p>
</li>
</ul>
<h5 id="version"><a href="#version" class="headerlink" title="version"></a>version</h5><p>表示依赖的版本，关于 <code>version</code> 语法如下：</p>
<ul>
<li><code>1.0</code>：若匹配到了 1.0 版本，则使用 1.0 版本</li>
<li><code>[1.0]</code>: 强制配依赖项的 1.0 版本</li>
<li><code>(,1.0]</code>: x &lt;&#x3D; 1.0</li>
<li><code>[1.2,1.3]</code>: 1.2 &lt;&#x3D; x &lt;&#x3D; 1.3</li>
<li><code>[1.0,2.0)</code>: 1.0 &lt;&#x3D; x &lt; 2.0</li>
<li><code>[1.5,)</code>: x &gt;&#x3D; 1.5</li>
<li><code>(,1.0],[1.2,)</code>: x &lt;&#x3D; 1.0 or x &gt;&#x3D; 1.2，多个版本集合的情况以逗号分隔</li>
<li><code>(,1.1),(1.1,)</code>: 排除 1.1 版本</li>
</ul>
<p>此外，<code>version</code> 的不同写发也存在优先级，详情可参考<a href="https://maven.apache.org/pom.html#Version_Order_Specification">官网</a></p>
<h5 id="type"><a href="#type" class="headerlink" title="type"></a>type</h5><p>依赖项的类型，缺省值是 <code>jar</code>。除此之外还有<code>ejb-client</code>、<code>test-jar</code>，但都不常用</p>
<h5 id="scope"><a href="#scope" class="headerlink" title="scope"></a>scope</h5><p>依赖项作用的范围以及如何限制依赖项的传递性。共五个范围值可选：</p>
<ul>
<li><code>compile</code>：缺省值即为 <code>compile</code>，表示依赖项参与项目的编译、测试、运行阶段，项目打包时，也会打进去。</li>
<li><code>provided</code>：依赖项参与项目的编译、测试阶段，与 <code>compile</code> 不同的是不会打包进项目中，但是期望 <code>JDK</code>、<code>容器</code>或使用者会提供该依赖项。</li>
<li><code>runtime</code>：编译时不需要该依赖项，但是执行时需要。</li>
<li><code>test</code>：项目不需要该依赖项，并且仅适用于单元测试的编译和执行阶段，不具有传递性。</li>
<li><code>system</code>：使用上与 <code>provided</code> 相同，不同之处在于该依赖不从 Maven 仓库中提取，而是从本地文件系统中提取，其会参照 <code>systemPath</code> 的属性进行提取依赖。</li>
</ul>
<h5 id="systemPath"><a href="#systemPath" class="headerlink" title="systemPath"></a>systemPath</h5><p>仅在 <code>scope</code> 为 <code>system</code> 时使用，必须指定本地的<strong>绝对路径</strong>，且必须保证文件存在。例如：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">groupid</span>&gt;</span>org.hamcrest<span class="tag">&lt;/<span class="name">groupid</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactid</span>&gt;</span>hamcrest-core<span class="tag">&lt;/<span class="name">artifactid</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.5<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">scope</span>&gt;</span>system<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">systempath</span>&gt;</span>$&#123;basedir&#125;/WebContent/WEB-INF/lib/hamcrest-core-1.3.jar<span class="tag">&lt;/<span class="name">systempath</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h5 id="optional"><a href="#optional" class="headerlink" title="optional"></a>optional</h5><p>标记依赖项是否可选，缺省值为 <code>false</code>。</p>
<p>若项目 <code>A</code> 依赖项目 <code>B</code>，项目 <code>B</code> 依赖项目 <code>C</code> 且 <code>optional</code> 为 <code>true</code>，此时若 项目 <code>A</code> 没有显式的依赖 <code>C</code>，则项目 <code>A</code> 不依赖 <code>C</code>，且打包过程中，不会将 <code>C</code> 打包进来。例如 <code>fastjson</code> 依赖了 <code>spring-webmvc</code>，若使用者在开发的项目依赖了 <code>fastjson</code>并且没有依赖 <code>spring-webmvc</code>，则此项目是不依赖 <code>spring-webmvc</code>。</p>
<h5 id="排除依赖项"><a href="#排除依赖项" class="headerlink" title="排除依赖项"></a>排除依赖项</h5><p>由于依赖的传播的特性，我们可以排除一个依赖项中我们不需要的依赖。</p>
<h6 id="排除部分依赖项"><a href="#排除部分依赖项" class="headerlink" title="排除部分依赖项"></a>排除部分依赖项</h6><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">project</span> <span class="attr">xmlns</span>=<span class="string">&quot;http://maven.apache.org/POM/4.0.0&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">xmlns:xsi</span>=<span class="string">&quot;http://www.w3.org/2001/XMLSchema-instance&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">xsi:schemaLocation</span>=<span class="string">&quot;http://maven.apache.org/POM/4.0.0</span></span></span><br><span class="line"><span class="string"><span class="tag">                      https://maven.apache.org/xsd/maven-4.0.0.xsd&quot;</span>&gt;</span></span><br><span class="line">  ...</span><br><span class="line">  <span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.apache.maven<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>maven-embedder<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">version</span>&gt;</span>2.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">exclusions</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">exclusion</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.apache.maven<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>maven-core<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">exclusion</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;/<span class="name">exclusions</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">    ...</span><br><span class="line">  <span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br><span class="line">  ...</span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h6 id="排除所有依赖项"><a href="#排除所有依赖项" class="headerlink" title="排除所有依赖项"></a>排除所有依赖项</h6><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">project</span> <span class="attr">xmlns</span>=<span class="string">&quot;http://maven.apache.org/POM/4.0.0&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">xmlns:xsi</span>=<span class="string">&quot;http://www.w3.org/2001/XMLSchema-instance&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">xsi:schemaLocation</span>=<span class="string">&quot;http://maven.apache.org/POM/4.0.0</span></span></span><br><span class="line"><span class="string"><span class="tag">                      https://maven.apache.org/xsd/maven-4.0.0.xsd&quot;</span>&gt;</span></span><br><span class="line">  ...</span><br><span class="line">  <span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.apache.maven<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>maven-embedder<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">version</span>&gt;</span>3.1.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">exclusions</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">exclusion</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>*<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>*<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">exclusion</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;/<span class="name">exclusions</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">    ...</span><br><span class="line">  <span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br><span class="line">  ...</span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="继承"><a href="#继承" class="headerlink" title="继承"></a>继承</h4><p>在继承关系中，父工程的 <code>packaging</code> 类型必须为 <code>pom</code>，父工程的大多数属性会被子工程继承，包括</p>
<ul>
<li>groupId</li>
<li>version</li>
<li>description</li>
<li>url</li>
<li>inceptionYear</li>
<li>organization</li>
<li>licenses</li>
<li>developers</li>
<li>contributors</li>
<li>mailingLists</li>
<li>scm</li>
<li>issueManagement</li>
<li>ciManagement</li>
<li>properties</li>
<li>dependencyManagement</li>
<li>dependencies</li>
<li>repositories</li>
<li>pluginRepositories</li>
<li>build<ul>
<li>plugin executions with matching ids</li>
<li>plugin configuration</li>
<li>etc.</li>
</ul>
</li>
<li>reporting</li>
<li>profiles</li>
</ul>
<p>不会被继承的属性包括：</p>
<ul>
<li>artifactId</li>
<li>name</li>
<li>prerequisites</li>
</ul>
<p>看一个列子</p>
<p>父工程：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">project</span> <span class="attr">xmlns</span>=<span class="string">&quot;http://maven.apache.org/POM/4.0.0&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">xmlns:xsi</span>=<span class="string">&quot;http://www.w3.org/2001/XMLSchema-instance&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">xsi:schemaLocation</span>=<span class="string">&quot;http://maven.apache.org/POM/4.0.0</span></span></span><br><span class="line"><span class="string"><span class="tag">                      https://maven.apache.org/xsd/maven-4.0.0.xsd&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">modelVersion</span>&gt;</span>4.0.0<span class="tag">&lt;/<span class="name">modelVersion</span>&gt;</span></span><br><span class="line"></span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.codehaus.mojo<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>my-parent<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">version</span>&gt;</span>2.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">packaging</span>&gt;</span>pom<span class="tag">&lt;/<span class="name">packaging</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>子工程</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">project</span> <span class="attr">xmlns</span>=<span class="string">&quot;http://maven.apache.org/POM/4.0.0&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">xmlns:xsi</span>=<span class="string">&quot;http://www.w3.org/2001/XMLSchema-instance&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">xsi:schemaLocation</span>=<span class="string">&quot;http://maven.apache.org/POM/4.0.0</span></span></span><br><span class="line"><span class="string"><span class="tag">                      https://maven.apache.org/xsd/maven-4.0.0.xsd&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">modelVersion</span>&gt;</span>4.0.0<span class="tag">&lt;/<span class="name">modelVersion</span>&gt;</span></span><br><span class="line"></span><br><span class="line">  <span class="tag">&lt;<span class="name">parent</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.codehaus.mojo<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>my-parent<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>2.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">relativePath</span>&gt;</span>../my-parent<span class="tag">&lt;/<span class="name">relativePath</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">parent</span>&gt;</span></span><br><span class="line"></span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>my-project<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>其中 <code>relativePath</code> 不是必需的，但如果指定了，该路径则会作为搜索父工程的首选项。</p>
<h5 id="Dependency-Management"><a href="#Dependency-Management" class="headerlink" title="Dependency Management"></a>Dependency Management</h5><p>在存在多个项目的情况下，在在父项目中定义 <code>dependencyManagement</code> 来帮住管理所有子项目的依赖项。如果在父项目中的 <code>dependencyManagement</code> 标签下定义了某个依赖项的信息，则在子项目中只需要填写该依赖项的 <code>groupId</code> 和 <code>artifactId</code> 即可。好处是可以避免不同的子项目，引入不同版本的依赖项。</p>
<h4 id="聚合"><a href="#聚合" class="headerlink" title="聚合"></a>聚合</h4><p>聚合可以将不同的模块，聚合在一起进行构建。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">project</span> <span class="attr">xmlns</span>=<span class="string">&quot;http://maven.apache.org/POM/4.0.0&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">xmlns:xsi</span>=<span class="string">&quot;http://www.w3.org/2001/XMLSchema-instance&quot;</span></span></span><br><span class="line"><span class="tag">  <span class="attr">xsi:schemaLocation</span>=<span class="string">&quot;http://maven.apache.org/POM/4.0.0</span></span></span><br><span class="line"><span class="string"><span class="tag">                      https://maven.apache.org/xsd/maven-4.0.0.xsd&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">modelVersion</span>&gt;</span>4.0.0<span class="tag">&lt;/<span class="name">modelVersion</span>&gt;</span></span><br><span class="line"></span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.codehaus.mojo<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>my-parent<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">version</span>&gt;</span>2.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">packaging</span>&gt;</span>pom<span class="tag">&lt;/<span class="name">packaging</span>&gt;</span></span><br><span class="line"></span><br><span class="line">  <span class="tag">&lt;<span class="name">modules</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">module</span>&gt;</span>my-project<span class="tag">&lt;/<span class="name">module</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">module</span>&gt;</span>another-project<span class="tag">&lt;/<span class="name">module</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">module</span>&gt;</span>third-project/pom-example.xml<span class="tag">&lt;/<span class="name">module</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">modules</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>未完待续…</p>
]]></content>
      <categories>
        <category>手册</category>
      </categories>
      <tags>
        <tag>未完待续</tag>
        <tag>构建工具</tag>
        <tag>Maven</tag>
      </tags>
  </entry>
  <entry>
    <title>MySQL 优化器成本模型</title>
    <url>/2019/12/04/mysql-optimizer-cost-model/</url>
    <content><![CDATA[<p>本文探讨一下 MySQL5.7 版本中 优化器的代价模型。相关内容也可查看<a href="https://dev.mysql.com/doc/refman/5.7/en/cost-model.html">官网</a></p>
<p>MySQL 优化器成本模型由两个表组成，分别位于 mysql.server_cost 和 mysql.engine_cost 中。</p>
<ul>
<li>server_cost: 针对服务器操作的优化器成本估算</li>
<li>engine_cose: 针对特定存储引擎的特定操作的优化器成本估算</li>
</ul>
<h2 id="server-cost"><a href="#server-cost" class="headerlink" title="server_cost"></a>server_cost</h2><p>server_cost 包含以下值：</p>
<ul>
<li>disk_temptable_create_cost：缺省值 40.0，该字段标识基于磁盘的存储引擎（InnoDB 和 MyISAM）中创建临时表的成本估算。从缺省值也可以看出，默认情况下创建临时表的代价是非常昂贵的。</li>
<li>disk_temptable_row_cost：缺省值 1.0，该字段标识基于磁盘的存储引擎（InnoDB 和 MyISAM）中创建临时行的成本估算。</li>
<li>key_compare_cost：缺省值 0.1，该字段表示比较记录的成本。</li>
<li>memory_temptable_create_cost：缺省值 2.0，MEMORY 存储引擎创建临时表的成本，从与 disk_temptable_create_cost 的对比中可以看出，默认情况下 MEMORY 存储引擎创建临时表的成本是远小于 InnoDB 和 MyISAM 存储引擎的。</li>
<li>memory_temptable_row_cost：缺省值 0.2，MEMORY 存储引擎创建临时行的成本。</li>
<li>row_evaluate_cost：缺省值 0.2，行评估成本，即扫描了多少行的成本。</li>
</ul>
<h2 id="engine-cost"><a href="#engine-cost" class="headerlink" title="engine_cost"></a>engine_cost</h2><ul>
<li>io_block_read_cost：缺省值 1.0，从磁盘读取索引或数据块的成本。</li>
<li>memory_block_read_cost：缺省值 1.0，从内存数据库缓冲区读取索引说数据块的成本。</li>
</ul>
<h3 id="修改成本模型"><a href="#修改成本模型" class="headerlink" title="修改成本模型"></a>修改成本模型</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 修改所有的 io_block_read_cost 成本</span><br><span class="line">UPDATE mysql.engine_cost</span><br><span class="line">  SET cost_value = 2.0</span><br><span class="line">  WHERE cost_name = &#x27;io_block_read_cost&#x27;;</span><br><span class="line">FLUSH OPTIMIZER_COSTS;</span><br><span class="line"></span><br><span class="line">-- 仅修改 InnoDB 存储引擎的成本</span><br><span class="line">INSERT INTO mysql.engine_cost</span><br><span class="line">  VALUES (&#x27;InnoDB&#x27;, 0, &#x27;io_block_read_cost&#x27;, 3.0,</span><br><span class="line">  CURRENT_TIMESTAMP, &#x27;Using a slower disk for InnoDB&#x27;);</span><br><span class="line">FLUSH OPTIMIZER_COSTS;</span><br></pre></td></tr></table></figure>

<h2 id="实战"><a href="#实战" class="headerlink" title="实战"></a>实战</h2><h3 id="打开优化器跟踪"><a href="#打开优化器跟踪" class="headerlink" title="打开优化器跟踪"></a>打开优化器跟踪</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 查看优化器跟踪的开关</span><br><span class="line">SHOW VARIABLES LIKE &#x27;%optimizer_trace%&#x27;;</span><br><span class="line">-- 打开优化器跟踪</span><br><span class="line">SET GLOBAL optimizer_trace = &#x27;enabled=on,one_line=on&#x27;;</span><br></pre></td></tr></table></figure>

<p>未完待续 …</p>
]]></content>
  </entry>
  <entry>
    <title>MySQL 手册</title>
    <url>/2019/11/14/mysql-%E6%89%8B%E5%86%8C/</url>
    <content><![CDATA[<h2 id="参数配置"><a href="#参数配置" class="headerlink" title="参数配置"></a>参数配置</h2><h3 id="查看所有参数"><a href="#查看所有参数" class="headerlink" title="查看所有参数"></a>查看所有参数</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mysql&gt; SHOW VARIABLES;</span><br></pre></td></tr></table></figure>

<h3 id="慢查询、索引相关的参数"><a href="#慢查询、索引相关的参数" class="headerlink" title="慢查询、索引相关的参数"></a>慢查询、索引相关的参数</h3><h4 id="查看慢查询日志是否打开"><a href="#查看慢查询日志是否打开" class="headerlink" title="查看慢查询日志是否打开"></a>查看慢查询日志是否打开</h4><p>Usage:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mysql&gt; SHOW VARIABLES LIKE &#x27;%slow_query_log%&#x27;;</span><br></pre></td></tr></table></figure>

<p>Result:</p>
<table>
<thead>
<tr>
<th>Variable_name</th>
<th>Value</th>
</tr>
</thead>
<tbody><tr>
<td>slow_query_log</td>
<td>OFF</td>
</tr>
<tr>
<td>slow_query_log_file</td>
<td>&#x2F;var&#x2F;lib&#x2F;mysql&#x2F;17998065ff2d-slow.log</td>
</tr>
</tbody></table>
<h4 id="打开慢查询日志"><a href="#打开慢查询日志" class="headerlink" title="打开慢查询日志"></a>打开慢查询日志</h4><p>Usage:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 打开MySQL慢查询日志</span><br><span class="line">mysql&gt; SET GLOBAL slow_query_log = ON;</span><br><span class="line">-- 或者用这种方式</span><br><span class="line">mysql&gt; SET GLOBAL slow_query_log = 1;</span><br></pre></td></tr></table></figure>

<h4 id="慢查询阈值"><a href="#慢查询阈值" class="headerlink" title="慢查询阈值"></a>慢查询阈值</h4><h5 id="查看慢查询阈值"><a href="#查看慢查询阈值" class="headerlink" title="查看慢查询阈值"></a>查看慢查询阈值</h5><p>Usage:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mysql&gt; SHOW VARIABLES LIKE &#x27;long_query_time&#x27;;</span><br></pre></td></tr></table></figure>

<p>Result:</p>
<table>
<thead>
<tr>
<th>Variable_name</th>
<th>Value</th>
</tr>
</thead>
<tbody><tr>
<td>long_query_time</td>
<td>10.000000</td>
</tr>
</tbody></table>
<h5 id="设置慢查询阈值"><a href="#设置慢查询阈值" class="headerlink" title="设置慢查询阈值"></a>设置慢查询阈值</h5><p>Usage:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 设置完之后，在当前窗口查询，可能存在值不变的情况，重新打开窗口查询即可。</span><br><span class="line">mysql&gt; SET GLOBAL long_query_time = 0.1;</span><br></pre></td></tr></table></figure>

<h4 id="未使用索引的情况"><a href="#未使用索引的情况" class="headerlink" title="未使用索引的情况"></a>未使用索引的情况</h4><h5 id="查看未使用索引的查询是否计入慢查询日志"><a href="#查看未使用索引的查询是否计入慢查询日志" class="headerlink" title="查看未使用索引的查询是否计入慢查询日志"></a>查看未使用索引的查询是否计入慢查询日志</h5><p>Usage:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mysql&gt; SHOW VARIABLES LIKE &#x27;%log_queries_not_using_indexes%&#x27;;</span><br></pre></td></tr></table></figure>

<p>Result:</p>
<table>
<thead>
<tr>
<th>Variable_name</th>
<th>Value</th>
</tr>
</thead>
<tbody><tr>
<td>log_queries_not_using_indexes</td>
<td>OFF</td>
</tr>
</tbody></table>
<h5 id="打开未使用索引记入慢查询日志"><a href="#打开未使用索引记入慢查询日志" class="headerlink" title="打开未使用索引记入慢查询日志"></a>打开未使用索引记入慢查询日志</h5><p>Usage:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mysql&gt; SET GLOBAL log_queries_not_using_indexes = ON;</span><br><span class="line">-- 或者</span><br><span class="line">mysql&gt; SET GLOBAL log_queries_not_using_indexes = 1;</span><br></pre></td></tr></table></figure>

<h4 id="慢查询日志分析"><a href="#慢查询日志分析" class="headerlink" title="慢查询日志分析"></a>慢查询日志分析</h4><p><code>MySQL</code> 自带了 <code>mysqldumpslow</code> 可以帮助我们快速得分析慢查询日志。<code>pt-query-digest</code> 是一个非常优秀的第三方工具来版主我们分析 <code>MySQL</code> 的 binlog、generallog、slowlog。</p>
<h5 id="mysqldumpslow"><a href="#mysqldumpslow" class="headerlink" title="mysqldumpslow"></a>mysqldumpslow</h5><p>Usage：</p>
<p><code>mysqldumpslow [ OPTS... ] [ LOGS... ]</code></p>
<p>具体的参数，可自行查看其对应的含义</p>
<p>示例：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">得到返回记录集最多的10个SQL。</span></span><br><span class="line">mysqldumpslow -s r -t 10 /var/lib/mysql/izwz9ezwhlhq4hy1wzw9oqz-slow.log</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">得到访问次数最多的10个SQL</span></span><br><span class="line">mysqldumpslow -s c -t 10 /var/lib/mysql/izwz9ezwhlhq4hy1wzw9oqz-slow.log</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">得到按照时间排序的前10条里面含有左连接的查询语句。</span></span><br><span class="line">mysqldumpslow -s t -t 10 -g &quot;left join&quot; /var/lib/mysql/izwz9ezwhlhq4hy1wzw9oqz-slow.log</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">另外建议在使用这些命令时结合 | 和more 使用 ，否则有可能出现刷屏的情况。</span></span><br><span class="line">mysqldumpslow -s r -t 20 /var/lib/mysql/izwz9ezwhlhq4hy1wzw9oqz-slow.log | more</span><br></pre></td></tr></table></figure>

<h4 id="刨析-MySQL-查询"><a href="#刨析-MySQL-查询" class="headerlink" title="刨析 MySQL 查询"></a>刨析 MySQL 查询</h4><p>在实际项目中，我们通过慢查询日志，只是找到对应的慢查询，那么如何对慢查询进行刨析呢？</p>
<p>MySQL 在 5.1 版本之后引入了 <code>SHOW PROFILE</code> 命令，默认是禁用的，但可以在会话级别动态的修改。</p>
<h5 id="开启"><a href="#开启" class="headerlink" title="开启"></a>开启</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 设置为 ON 或 1 均可</span><br><span class="line">mysql&gt; SET PROFILING = ON;</span><br></pre></td></tr></table></figure>

<h5 id="刨析查询耗时"><a href="#刨析查询耗时" class="headerlink" title="刨析查询耗时"></a>刨析查询耗时</h5><p>开启之后执行任意一个查询，然后在执行 <code>SHOW PROFILING</code>，观察对应的结果。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mysql&gt; SHOW PROFILING;</span><br></pre></td></tr></table></figure>

<p>其结果如下</p>
<table>
<thead>
<tr>
<th>Query_ID</th>
<th>Duration</th>
<th>Query</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td>0.00060525</td>
<td>SELECT * FROM <code>actor</code></td>
</tr>
</tbody></table>
<p>如果需要看更为详细的信息</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mysql&gt; SHOW PROFILE FOR QUERY 1;</span><br></pre></td></tr></table></figure>

<p>其结果如下，可以看到详细的记录了该次查询各个阶段的耗时情况</p>
<table>
<thead>
<tr>
<th>Status</th>
<th>Duration</th>
</tr>
</thead>
<tbody><tr>
<td>starting</td>
<td>0.000069</td>
</tr>
<tr>
<td>checking permissions</td>
<td>0.000009</td>
</tr>
<tr>
<td>Opening tables</td>
<td>0.000021</td>
</tr>
<tr>
<td>init</td>
<td>0.000022</td>
</tr>
<tr>
<td>System lock</td>
<td>0.000010</td>
</tr>
<tr>
<td>optimizing</td>
<td>0.000006</td>
</tr>
<tr>
<td>statistics</td>
<td>0.000017</td>
</tr>
<tr>
<td>preparing</td>
<td>0.000015</td>
</tr>
<tr>
<td>executing</td>
<td>0.000004</td>
</tr>
<tr>
<td>Sending data</td>
<td>0.000298</td>
</tr>
<tr>
<td>end</td>
<td>0.000006</td>
</tr>
<tr>
<td>query end</td>
<td>0.000008</td>
</tr>
<tr>
<td>closing tables</td>
<td>0.000009</td>
</tr>
<tr>
<td>freeing items</td>
<td>0.000020</td>
</tr>
<tr>
<td>logging slow query</td>
<td>0.000074</td>
</tr>
<tr>
<td>cleaning up</td>
<td>0.000019</td>
</tr>
</tbody></table>
<h3 id="事务相关的参数"><a href="#事务相关的参数" class="headerlink" title="事务相关的参数"></a>事务相关的参数</h3><h4 id="事务隔离级别"><a href="#事务隔离级别" class="headerlink" title="事务隔离级别"></a>事务隔离级别</h4><h5 id="查看事务的隔离级别"><a href="#查看事务的隔离级别" class="headerlink" title="查看事务的隔离级别"></a>查看事务的隔离级别</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 查看当前数据库事务隔离级别</span><br><span class="line">mysql&gt; SHOW VARIABLES LIKE &#x27;%transaction_isolation%&#x27;;</span><br><span class="line">-- 或者</span><br><span class="line">mysql&gt; SELECT @@transaction_isolation;</span><br><span class="line">-- 低版本的MySQL属性是不一样的</span><br><span class="line">mysql&gt; SHOW VARIABLES LIKE &#x27;%tx_isolation%&#x27;;</span><br><span class="line">-- 或者</span><br><span class="line">mysql&gt; SELECT @@tx_isolation;</span><br></pre></td></tr></table></figure>

<h5 id="设置事务的隔离级别"><a href="#设置事务的隔离级别" class="headerlink" title="设置事务的隔离级别"></a>设置事务的隔离级别</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 设置当前会话级别为`read-uncommitted`</span><br><span class="line">mysql&gt; SET SESSION TRANSACTION ISOLATION LEVEL READ UNCOMMITTED;</span><br><span class="line">-- 设置当前会话级别为`read-committed`</span><br><span class="line">mysql&gt; SET SESSION TRANSACTION ISOLATION LEVEL READ COMMITTED;</span><br><span class="line">-- 设置当前会话级别为`repeatable-read`</span><br><span class="line">mysql&gt; SET SESSION TRANSACTION ISOLATION LEVEL REPEATABLE READ;</span><br><span class="line">-- 设置当前会话级别为`serializable`</span><br><span class="line">mysql&gt; SET SESSION TRANSACTION ISOLATION LEVEL SERIALIZABLE;</span><br></pre></td></tr></table></figure>

<h4 id="事务自动提交"><a href="#事务自动提交" class="headerlink" title="事务自动提交"></a>事务自动提交</h4><p><code>MySQL</code> 默认采用自动提交模式，也就是说如果不是显式地开始一个事务，则每个查询都被当做一个事务执行提交操作。另外还有一些命令，在执行之前会强制 <code>COMMIT</code> 提交当前活动地事务，典型的例子，<code>DDL</code> 语言中会导致大量数据改变的操作，比如 <code>ALTER TABLE</code> 等，此外还有 <code>LOCK TABLES</code> 等语句。</p>
<h5 id="查看事务自动提交状态"><a href="#查看事务自动提交状态" class="headerlink" title="查看事务自动提交状态"></a>查看事务自动提交状态</h5><p>Useage:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 查看事务自动提交的参数</span><br><span class="line">mysql&gt; SHOW VARIABLES LIKE &#x27;AUTOCOMMIT&#x27;;</span><br></pre></td></tr></table></figure>

<p>Result:</p>
<table>
<thead>
<tr>
<th>Variable_name</th>
<th>Value</th>
</tr>
</thead>
<tbody><tr>
<td>autocommit</td>
<td>ON</td>
</tr>
</tbody></table>
<h5 id="关闭事务自动提交"><a href="#关闭事务自动提交" class="headerlink" title="关闭事务自动提交"></a>关闭事务自动提交</h5><p>Useage:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 关闭事务自动提交</span><br><span class="line">mysql&gt; SET AUTOCOMMIT = OFF;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>手册</category>
      </categories>
      <tags>
        <tag>未完待续</tag>
        <tag>MySQL</tag>
      </tags>
  </entry>
  <entry>
    <title>Maven Wapper 手册</title>
    <url>/2019/06/23/maven-wapper-%E6%89%8B%E5%86%8C/</url>
    <content><![CDATA[<p>我们使用 Maven 时，基本上只会用到 <code>mvn</code> 这一个命令。有些童鞋可能听说过 <code>mvnw</code>，这个是啥？</p>
<p>mvnw 是 Maven Wrapper 的缩写，我们安装 Maven 时，默认情况下，系统所有项目都会使用全局安装的这个 Maven 版本。但是，对于某些项目来说，它可能必须使用某个特定的 Maven 版本，这个时候，就可以使用 Maven Wrapper，它可以负责给这个特定的项目安装指定版本的 Maven，而其他项目不受影响。</p>
<p>简单地说，Maven Wrapper 就是给一个项目提供一个独立的，指定版本的 Maven 给它使用。</p>
<h2 id="安装-Maven-Wrapper"><a href="#安装-Maven-Wrapper" class="headerlink" title="安装 Maven Wrapper"></a>安装 Maven Wrapper</h2><p>安装 Maven Wrapper 最简单的方式是在项目的根目录（即 pom.xml 所在的目录）下运行安装命令：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">mvn -N io.takari:maven:0.7.7:wrapper</span><br></pre></td></tr></table></figure>

<p>它会自动使用最新版本的 Maven。注意 0.7.7 是 Maven Wrapper 的版本。最新的 Maven Wrapper 版本可以去<a href="https://github.com/takari/maven-wrapper">官方网站</a>查看。</p>
<p>如果要指定使用的 Maven 版本，使用下面的安装命令指定版本，例如 3.6.0：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">mvn -N io.takari:maven:0.7.7:wrapper -Dmaven=3.6.0</span><br></pre></td></tr></table></figure>

<p>安装后，查看项目结构：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Hello-World</span><br><span class="line">├── .mvn</span><br><span class="line">│   └── wrapper</span><br><span class="line">│       ├── MavenWrapperDownloader.java</span><br><span class="line">│       ├── maven-wrapper.jar</span><br><span class="line">│       └── maven-wrapper.properties</span><br><span class="line">├── mvnw</span><br><span class="line">├── mvnw.cmd</span><br><span class="line">├── pom.xml</span><br><span class="line">└── src</span><br><span class="line">    ├── main</span><br><span class="line">    │   ├── java</span><br><span class="line">    │   └── resources</span><br><span class="line">    └── test</span><br><span class="line">        ├── java</span><br><span class="line">        └── resources</span><br></pre></td></tr></table></figure>

<p>发现多了 <code>mvnw</code>、<code>mvnw.cmd</code> 和 <code>.mvn</code>目录。</p>
<h2 id="使用-Maven-Wapper"><a href="#使用-Maven-Wapper" class="headerlink" title="使用 Maven Wapper"></a>使用 Maven Wapper</h2><p>我们只需要把 mvn 命令改成 mvnw 就可以使用跟项目关联的 Maven。例如：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">mvnw clean package</span><br></pre></td></tr></table></figure>

<p>Maven Wrapper 的另一个作用是把项目的 <code>mvnw</code>、<code>mvnw.cmd</code> 和 <code>.mvn</code> 提交到版本库中，可以使所有开发人员使用统一的 <code>Maven</code> 版本。</p>
]]></content>
      <categories>
        <category>手册</category>
      </categories>
      <tags>
        <tag>构建工具</tag>
        <tag>Maven</tag>
        <tag>Maven Wapper</tag>
      </tags>
  </entry>
  <entry>
    <title>MySQL 索引</title>
    <url>/2019/11/23/mysql-index-categories/</url>
    <content><![CDATA[<p>本文粗略说以下 MySQL 中常见的索引分类。</p>
<h2 id="物理存储角度"><a href="#物理存储角度" class="headerlink" title="物理存储角度"></a>物理存储角度</h2><ul>
<li>聚簇索引</li>
<li>非聚簇索引</li>
</ul>
<p><strong>聚簇索引</strong>将数据存储与索引放到了一起，找到索引也就找到了数据。<strong>非聚簇索引</strong>将数据存储与索引分开，索引结构的叶子结点指向了数据对应的行。</p>
<p>在 <code>InnoDB</code> 中，在聚簇索引之上创建的索引，称之为<strong>辅助索引</strong>，辅助索引叶子结点存储的不是行的物理位置，而是主键值，因此辅助索引访问数据总是需要二次查找的。</p>
<p>一图顶千言，如下图所示：</p>
<img src="/2019/11/23/mysql-index-categories/%E8%81%9A%E7%B0%87%E7%B4%A2%E5%BC%95%E4%B8%8E%E9%9D%9E%E8%81%9A%E7%B0%87%E7%B4%A2%E5%BC%95.jpg" class="" title="聚簇索引与非聚簇索引的区别">

<p>由于聚簇索引是将数据跟索引结构放在一起的，因此一个表仅有一个聚簇索引。</p>
<p><strong>聚簇索引默认是主键</strong>，如果表中没有定义主键，<code>InnoDB</code> 会选择一个<strong>唯一非空索引</strong>，如果没有这样的索引，<code>InnoDB</code> 会隐式定义一个主键来作为聚簇索引。</p>
<h2 id="数据结构角度"><a href="#数据结构角度" class="headerlink" title="数据结构角度"></a>数据结构角度</h2><ul>
<li>B-Tree 索引（技术上来说是 B+树）</li>
<li>hash 索引</li>
<li>FULLTEXT 索引（全文索引）</li>
<li>R-Tree 索引</li>
</ul>
<p>这里着重说以下 B-Tree 索引，B-Tree 索引之所以能加快访问数据的速度，是因为存储引擎不在需要进行全表扫描来获取数据，而是从索引的跟结点开始进行搜索，根节点的槽中存放了指向子节点的指针，存储引擎根据这些指针向下层查找，通过比较节点页的值和要查找的值找到合适的指针进入下层子节点，这些指针定义了子节点页中值的上限和下限。最终存储引擎要么找到对应的值，要么对应的记录不存在。</p>
<p><strong>叶子结点</strong>比较特别，它们的指针指向的是被索引的数据，而不是其他的结点页。同时所有的叶子结点到根节点的距离相同，所以 B-Tree 的查询效率非常的稳定。</p>
<p>相较于二叉查找树，B-Tree 在比较次数上并不占优，但是把磁盘里数据加载到内存的时候，是以<strong>页</strong>为单位来加载的，节点与节点之间的数据是不连续的，所以不同的节点，有可能分布在不同的磁盘页中。B-Tree 的每一个节点可以存放多个元素，所以磁盘寻址加载次数会比较少。所以总体来说 B-Tree 还是比二叉查找树快。</p>
<p>B-Tree 适用于全键值、键值范围或建前缀查找（只适用于最左前缀查找）。</p>
<h2 id="逻辑角度"><a href="#逻辑角度" class="headerlink" title="逻辑角度"></a>逻辑角度</h2><ul>
<li>主键索引</li>
<li>普通索引（单列索引）</li>
<li>复合索引（多列索引）</li>
<li>唯一索引</li>
</ul>
]]></content>
      <tags>
        <tag>MySQL</tag>
        <tag>索引</tag>
      </tags>
  </entry>
  <entry>
    <title>npm-手册</title>
    <url>/2020/07/19/npm-%E6%89%8B%E5%86%8C/</url>
    <content><![CDATA[<h2 id="配置-npmmirror-镜像"><a href="#配置-npmmirror-镜像" class="headerlink" title="配置 npmmirror 镜像"></a>配置 npmmirror 镜像</h2><ol>
<li>备份旧镜像</li>
</ol>
<p>在替换 npmmirror 镜像之前，我们先查看一下原有的镜像</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npm get registry</span><br></pre></td></tr></table></figure>

<p>结果为 <code>https://registry.npmjs.org/</code></p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">yarn get registry</span><br></pre></td></tr></table></figure>

<p>结果为 <code>https://registry.yarnpkg.com</code></p>
<ol start="2">
<li>替换为 npmmirror 镜像</li>
</ol>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">设置 npm 的源为 npmmirror</span></span><br><span class="line">npm config set registry https://registry.npmmirror.com</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">设置 yarn 的源为 npmmirror</span></span><br><span class="line">yarn config set registry https://registry.npmmirror.com</span><br></pre></td></tr></table></figure>

<h2 id="升级版本"><a href="#升级版本" class="headerlink" title="升级版本"></a>升级版本</h2><h3 id="查看-npm-版本"><a href="#查看-npm-版本" class="headerlink" title="查看 npm 版本"></a>查看 npm 版本</h3><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npm -v</span><br></pre></td></tr></table></figure>

<h3 id="升级到最新的-npm-版本"><a href="#升级到最新的-npm-版本" class="headerlink" title="升级到最新的 npm 版本"></a>升级到最新的 npm 版本</h3><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npm install -g npm</span><br></pre></td></tr></table></figure>

<h3 id="安装指定版本的-npm"><a href="#安装指定版本的-npm" class="headerlink" title="安装指定版本的 npm"></a>安装指定版本的 npm</h3><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npm install -g npm@版本号</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>手册</category>
      </categories>
      <tags>
        <tag>未完待续</tag>
        <tag>npm</tag>
      </tags>
  </entry>
  <entry>
    <title>PowerShell 手册</title>
    <url>/2019/09/27/powershell-%E6%89%8B%E5%86%8C/</url>
    <content><![CDATA[<p>记录一些常见的 <code>Powershell</code> 的使用问题</p>
<h2 id="别名命令"><a href="#别名命令" class="headerlink" title="别名命令"></a>别名命令</h2><p>在使用 Linux 系统时，我喜欢使用 <code>ll</code> 命令来查看当前目录的文件，但是 Windows 系统的 Powershell 没有这个命令，所以每次用的时候就很不习惯。我们可以通过对命令设置别名来让 Powershell 支持我们的使用习惯。</p>
<h3 id="查看别名"><a href="#查看别名" class="headerlink" title="查看别名"></a>查看别名</h3><p><code>Get-Alias</code> 或者 <code>gal</code> 命令来查看我们系统命令的别名，如下</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">PS C:\Users\fuyongde&gt; get-alias</span><br><span class="line"></span><br><span class="line">CommandType     Name                                               Version    Source</span><br><span class="line">-----------     ----                                               -------    ------</span><br><span class="line">Alias           % -&gt; ForEach-Object</span><br><span class="line">Alias           ? -&gt; Where-Object</span><br><span class="line">Alias           ac -&gt; Add-Content</span><br><span class="line">Alias           cat -&gt; Get-Content</span><br></pre></td></tr></table></figure>

<ul>
<li>查看某别名的原命令，如 <code>ls</code> 的原命令：<code>Get-Alias ls</code></li>
<li>查看某原命令的别名，如 <code>Get-ChildItem</code> 的别名：<code>Get-Alias -Definition Get-ChildItem</code></li>
</ul>
<h3 id="临时创建或更改别名"><a href="#临时创建或更改别名" class="headerlink" title="临时创建或更改别名"></a>临时创建或更改别名</h3><p>使用 <code>Set-Alias</code> 命令来创建或更改别名，但是改命令只在当前的的 PowerShell Session 中生效。另外还有一个 <code>New-Alias</code> 命令用法类似。</p>
<h4 id="创建不带参数的别名"><a href="#创建不带参数的别名" class="headerlink" title="创建不带参数的别名"></a>创建不带参数的别名</h4><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">PS C:\Users\fuyongde&gt; Set-Alias -Name ll -Value get-childitem</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">或者简单一些</span></span><br><span class="line">PS C:\Users\fuyongde&gt; Set-Alias ll get-childitem</span><br></pre></td></tr></table></figure>

<h4 id="创建带参数的别名"><a href="#创建带参数的别名" class="headerlink" title="创建带参数的别名"></a>创建带参数的别名</h4><p>如果命令带参数如想为 <code>Get-ChildItem -Name</code> 设定别名为 <code>ll</code> 则我们需要两步，第一步为这个带参数原命令设定一个中间 <code>function</code>，第二步为这个 <code>function</code> 指定别名</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">PS C:\Users\fuyongde&gt; function getlist &#123;Get-ChildItem -Name&#125;</span><br><span class="line">PS C:\Users\fuyongde&gt; Set-Alias ll getlist</span><br></pre></td></tr></table></figure>

<h4 id="创建永久的别名"><a href="#创建永久的别名" class="headerlink" title="创建永久的别名"></a>创建永久的别名</h4><p>在 PowerShell 中直接使用 <code>Set-Alias</code> 或 <code>New-Alias</code> 命令创建的别名在关闭此 Session 后即会失效，防止此现象的方法是将此命令写入 Windows PowerShell profile 文件。</p>
<p>查看此文件在计算机中的位置：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">PS C:\Users\fuyongde&gt; $profile</span><br></pre></td></tr></table></figure>

<p>结果如下：</p>
<p><code>C:\Users\fuyongde\Documents\WindowsPowerShell\Microsoft.PowerShell_profile.ps1</code></p>
<p>但如果安装了 <code>PowerShell6</code> 的话，则结果是：</p>
<p><code>C:\Users\fuyongde\Documents\PowerShell\Microsoft.PowerShell_profile.ps1</code></p>
<p>打开对应文件，并写入要预设的别名</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">Set-Alias ll get-childitem</span><br></pre></td></tr></table></figure>

<p>注意：该种方式有可能会遇到系统上禁止运行脚本的提示，这是因为 PowerShell 对安全做过充分考量，把脚本的执行分成了几个策略。以下是常用的执行策略：</p>
<ul>
<li><code>Restricted</code>：禁止运行任何脚本和配置文件，默认情况下就是该策略。</li>
<li><code>AllSigned</code>：可以运行脚本，但要求所有脚本和配置文件由可信任的发布者签名，包括在本地计算机编写的脚本。</li>
<li><code>RemoteSigned</code>：可以运行脚本，但要求从网络上下载的脚本和配置由可信任的发布者签名，不要求对已经运行和在本地计算机编写的脚本进行签名。</li>
<li><code>Unrestricted</code>：可以运行未签名的脚本，这种策略也是最危险的。</li>
</ul>
<p>通过 <code>Get-ExecutionPolicy</code> 命令可查看当前配置的执行策略，通过管理员运行 PowerShell 并执行 <code>Set-ExecutionPolicy RemoteSigned</code> 可更改执行策略。</p>
<h3 id="删除别名"><a href="#删除别名" class="headerlink" title="删除别名"></a>删除别名</h3><p>使用 <code>Remove-Item alias</code> 命令删除已设定的别名。</p>
<p>如删除别名 ll ：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">PS C:\Users\fuyongde&gt; Remove-Item alias:\ll</span><br></pre></td></tr></table></figure>

<h2 id="环境变量命令"><a href="#环境变量命令" class="headerlink" title="环境变量命令"></a>环境变量命令</h2><h3 id="切换-PowerShell-的-page-为-UTF-8-代码页"><a href="#切换-PowerShell-的-page-为-UTF-8-代码页" class="headerlink" title="切换 PowerShell 的 page 为 UTF-8 代码页"></a>切换 PowerShell 的 page 为 UTF-8 代码页</h3><p>在最新版本的 windows 操作系统中，powershell 默认的代码页已经是 UTF-8 字符集的代码页了。常见的代码页如下：</p>
<ul>
<li><p><code>65001</code>：UTF-8 代码页</p>
</li>
<li><p><code>950</code>：繁体中文</p>
</li>
<li><p><code>936</code>：简体中文默认的 GBK</p>
</li>
<li><p><code>437</code>：MS-DOS 美国英语</p>
</li>
</ul>
<p>命令如下：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">chcp 65001</span><br></pre></td></tr></table></figure>

<h2 id="Modules-管理"><a href="#Modules-管理" class="headerlink" title="Modules 管理"></a>Modules 管理</h2><h3 id="安装-Module"><a href="#安装-Module" class="headerlink" title="安装 Module"></a>安装 Module</h3><p>使用 <code>Install-Module</code> 命令来安装 Module，其支持的参数有很多，具体可以查看<a href="https://docs.microsoft.com/zh-cn/powershell/module/powershellget/install-module?view=powershell-7.2">官方文档</a>，这里我们只列举最常用的参数的使用方法：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">为当前用户安装 posh-git 模块</span></span><br><span class="line">Install-Module -Name posh-git -Scope CurrentUser</span><br></pre></td></tr></table></figure>

<h3 id="查找-Module"><a href="#查找-Module" class="headerlink" title="查找 Module"></a>查找 Module</h3><p>使用 <code>Find-Module</code> 命令可以来查找 Module，其支持的参数有很多，具体可以查看<a href="https://docs.microsoft.com/zh-cn/powershell/module/powershellget/find-module?view=powershell-7.2">官方文档</a>，这里我们只列举最常用的参数的使用方法：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">查找名称为 PowerShellGet 的 Module</span></span><br><span class="line">Find-Module -Name PowerShellGet</span><br></pre></td></tr></table></figure>

<h3 id="查看已经安装的-Modules"><a href="#查看已经安装的-Modules" class="headerlink" title="查看已经安装的 Modules"></a>查看已经安装的 Modules</h3><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">查看已经安装的所有的 Modules</span></span><br><span class="line">Get-InstalledModule</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">查看已经安装的指定名称、版本的 Modules</span></span><br><span class="line">Get-InstalledModule -Name &quot;AzureRM.Automation&quot; -MinimumVersion 1.0 -MaximumVersion 2.0</span><br></pre></td></tr></table></figure>

<h3 id="卸载-Module"><a href="#卸载-Module" class="headerlink" title="卸载 Module"></a>卸载 Module</h3><p>使用 <code>Uninstall-Module</code> 命令可以来卸载 Module，其支持的参数有很多，具体可以查看<a href="https://docs.microsoft.com/zh-cn/powershell/module/powershellget/uninstall-module?view=powershell-7.2">官方文档</a>，这里我们只列举最常用的参数的使用方法：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">卸载指定名称的模块</span></span><br><span class="line">Uninstall-Module -Name SpeculationControl</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">强制卸载指定名称的模块</span></span><br><span class="line">Uninstall-Module -Name SpeculationControl -Force</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">使用流水线的方式卸载，即先查找到某个模块并卸载</span></span><br><span class="line">Get-InstalledModule -Name SpeculationControl | Uninstall-Module</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">卸载指定版本的模块</span></span><br><span class="line">Uninstall-Module -Name &#x27;oh-my-posh&#x27; -RequiredVersion &#x27;7.31.1&#x27;</span><br></pre></td></tr></table></figure>

<h2 id="美化"><a href="#美化" class="headerlink" title="美化"></a>美化</h2><p>PowerShell 原始的界面是非常丑的，我们可以通过安装一些 modules 来进行美化，网上有很多博客介绍如何美化 PowerShell，但是大部门没有介绍每个 module 的作用。</p>
<h3 id="oh-my-posh"><a href="#oh-my-posh" class="headerlink" title="oh-my-posh"></a>oh-my-posh</h3><blockquote>
<p>A prompt theme engine for any shell.</p>
</blockquote>
<p>从介绍中可以看出 oh-my-posh 是为任何 shell 提供的一套主题引擎，具体更多的文档可以查阅<a href="https://ohmyposh.dev/">官网</a>。</p>
<h4 id="安装-oh-my-posh"><a href="#安装-oh-my-posh" class="headerlink" title="安装 oh-my-posh"></a>安装 oh-my-posh</h4><p>为 Powershell 安装 oh-my-posh 非常简单，安装命令如下：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">Install-Module oh-my-posh -Scope CurrentUser</span><br></pre></td></tr></table></figure>

<h4 id="更新-oh-my-posh"><a href="#更新-oh-my-posh" class="headerlink" title="更新 oh-my-posh"></a>更新 oh-my-posh</h4><p>如果之前安装过 oh-my-posh 但是想要更新版本，其更新命令如下：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">Update-Module oh-my-posh</span><br></pre></td></tr></table></figure>

<h4 id="启用-oh-my-posh"><a href="#启用-oh-my-posh" class="headerlink" title="启用 oh-my-posh"></a>启用 oh-my-posh</h4><p>安装 oh-my-posh 之后，并不会直接启用，跟上文创建别名的方法一样，在 Windows PowerShell profile 文件中启用即可。通过 <code>notepad $PROFILE</code> 命令，打开 Windows PowerShell profile 文件，在文件中添加如下内容：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">引入 oh-my-posh 模块</span></span><br><span class="line">Import-Module oh-my-posh</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">设置 oh-my-posh 的主题为 ys，这里也可以修改为其他主题</span></span><br><span class="line">Set-PoshPrompt -Theme ys</span><br></pre></td></tr></table></figure>

<h4 id="部分主题乱码问题"><a href="#部分主题乱码问题" class="headerlink" title="部分主题乱码问题"></a>部分主题乱码问题</h4><p>由于一些 oh-my-posh 中用到了一些特殊的字符，需要对应的终端更换为支持这种特殊字符的字体，可以在<a href="https://www.nerdfonts.com/">nerdfonts 网站</a>网站下载自己喜欢的字体，个人推荐 <code>DejaVuSansMono Nerd Font</code> 这个字体。</p>
<h3 id="posh-git"><a href="#posh-git" class="headerlink" title="posh-git"></a>posh-git</h3><p>posh-git 用于在 shell 上提示 git 的一些信息，其安装、更新、卸载过程跟其他的 PowerShell modules 一致。这里只列举安装命令：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">Install-Module posh-git -Scope CurrentUser</span><br></pre></td></tr></table></figure>

<h4 id="启用-posh-git"><a href="#启用-posh-git" class="headerlink" title="启用 posh-git"></a>启用 posh-git</h4><p>安装 posh-git 之后，并不会直接启用，跟上文创建别名的方法一样，在 Windows PowerShell profile 文件中启用即可。通过 <code>notepad $PROFILE</code> 命令，打开 Windows PowerShell profile 文件，在文件中添加如下内容：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">引入 oh-my-posh 模块</span></span><br><span class="line">Import-Module posh-git</span><br></pre></td></tr></table></figure>

<h3 id="PSReadLine"><a href="#PSReadLine" class="headerlink" title="PSReadLine"></a>PSReadLine</h3><p>PSReadLine 在 PowerShell 控制台中提供改进的命令行编辑体验。具体说明可参考其<a href="https://docs.microsoft.com/zh-cn/powershell/module/psreadline/about/about_psreadline?view=powershell-7.2">官方文档</a>。</p>
<h4 id="安装-PSReadLine"><a href="#安装-PSReadLine" class="headerlink" title="安装 PSReadLine"></a>安装 PSReadLine</h4><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">Install-Module -Name PSReadLine</span><br></pre></td></tr></table></figure>

<h4 id="启用-PSReadLine"><a href="#启用-PSReadLine" class="headerlink" title="启用 PSReadLine"></a>启用 PSReadLine</h4><p>安装 PSReadLine 之后，并不会直接启用，跟上文创建别名的方法一样，在 Windows PowerShell profile 文件中启用即可。通过 <code>notepad $PROFILE</code> 命令，打开 Windows PowerShell profile 文件，在文件中添加如下内容：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">引入 PSReadLine 模块</span></span><br><span class="line">Import-Module PSReadLine</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">设置编辑模式为 Emacs</span></span><br><span class="line">Set-PSReadlineOption -EditMode Emacs</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">开启预测 IntelliSense，其模式为历史命令，即历史敲过的命令都会进行提示</span></span><br><span class="line">Set-PSReadLineOption -PredictionSource History</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">开启预测 IntelliSense 之后，使用快捷键快速选中命令</span></span><br><span class="line">Set-PSReadlineKeyHandler -Key UpArrow -Function HistorySearchBackward</span><br><span class="line">Set-PSReadlineKeyHandler -Key DownArrow -Function HistorySearchForward</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>手册</category>
      </categories>
      <tags>
        <tag>未完待续</tag>
        <tag>PowerShell</tag>
      </tags>
  </entry>
  <entry>
    <title>RPC Introduction</title>
    <url>/2019/07/11/rpc-introduction/</url>
    <content><![CDATA[<h2 id="What-is-RPC"><a href="#What-is-RPC" class="headerlink" title="What is RPC"></a>What is RPC</h2><blockquote>
<p>Remote Procedure Call (RPC) is a protocol that one program can use to request a service from a program located in another computer on a network without having to understand the network’s details. A procedure call is also sometimes known as a function call or a subroutine call. A procedure call is also sometimes known as a function call or a subroutine call.</p>
</blockquote>
<p><code>RPC</code> 是一种远程过程调用协议，一个程序可以使用该协议从位于网络上另一台计算机中的程序请求服务，而无需了解网络的详细信息。<code>RPC</code> 有时也称为函数调用或子程序调用。<code>RPC</code> 是进程间通信的一种方式。</p>
<h2 id="Why-is-RPC"><a href="#Why-is-RPC" class="headerlink" title="Why is RPC"></a>Why is RPC</h2><p>网上很多关于 <code>RPC</code> 的优点，笔者都不太赞同。不能将那些成熟的 <code>RPC</code> 框架解决的问题全部归结为 <code>RPC</code> 的优点。比如 <code>RPC</code> 的概念中并没有提到服务治理等问题，但是常见的 <code>RPC</code> 框架（如 <a href="http://dubbo.apache.org/">DUBBO</a>）均解决了服务治理的相关问题，这显然不能作为 <code>RPC</code> 的优点。要解释“Why is RPC?”的问题，还是要回归到 <code>RPC</code> 的概念上来。</p>
<ul>
<li><code>RPC</code> 可以使<em>远程过程调用</em>变得像本地调用一样简单。</li>
<li>方便计算能力的横向扩展。</li>
</ul>
<p>有兴趣可以阅读 <a href="http://dubbo.apache.org/zh-cn/docs/user/preface/background.html">dubbo 诞生的背景</a> 以及<a href="http://dubbo.apache.org/zh-cn/docs/user/preface/requirements.html">dubbo 解决了哪些需求</a>。</p>
<h3 id="RPC-vs-HTTP"><a href="#RPC-vs-HTTP" class="headerlink" title="RPC vs HTTP"></a>RPC vs HTTP</h3><p>其实这两者不是同一个维度的概念。从 <code>RPC</code> 的概念上来讲，<code>HTTP</code> 其实就是 <code>RPC</code> 的一种实现。</p>
<h3 id="RPC-vs-RMI"><a href="#RPC-vs-RMI" class="headerlink" title="RPC vs RMI"></a>RPC vs RMI</h3><p>这两者也不是同一个维度的概念。</p>
<p><code>RMI</code> 是 <code>JAVA</code> 提供的一种<em>远程方法调用</em>，而 <code>RPC</code> 的变成模型是<em>远程过程调用</em>，<code>RPC</code> 并没有规定采用哪种编程语言实现，<strong>我们甚至可以使用 <code>JAVA</code> 语言实现一个具体的 <code>RPC</code> 协议，内部采用 <code>RMI</code> 来实现数据的传输</strong>，例如 <a href="http://dubbo.apache.org/zh-cn/docs/user/references/protocol/rmi.html">dubbo 中就可以采用 rmi 协议</a>。</p>
<h2 id="How-to-implement-RPC"><a href="#How-to-implement-RPC" class="headerlink" title="How to implement RPC"></a>How to implement RPC</h2><p>我们先思考这么一个问题：要怎么去调用远程的服务呢？</p>
<ol>
<li>要知道 ip 和端口，以确定远程唯一的一个进程。</li>
<li>要知道调用远程的进程中的什么服务，以确定方法和参数</li>
<li>调用远程的服务之后，要返回结果。</li>
</ol>
<p>根据上述的三点，我们来看一下如何实现一个 <code>RPC</code> 框架。</p>
<h3 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h3><p>在一个 <code>RPC</code> 的架构中包含 4 个组件：</p>
<ol>
<li><code>Client</code>：即客户端，服务调用方</li>
<li><code>Client Stub</code>：客户端存根，用于存方服务端地址信息，将客户端的信息打包成网络消息，再通过网络发送给服务提供者</li>
<li><code>Server Stub</code>：服务端存根，接收客户端发送过来的消息，并调用本地服务</li>
<li><code>Server</code>：服务提供者</li>
</ol>
<p>一个 <code>RPC</code> 的调用过程：</p>
<ol>
<li><code>Client</code> 调用以本地调用方式调用服务；</li>
<li><code>Client Stub</code> 接收到调用后负责将方法、参数等组装成能够进行网络传输的消息体（在 <code>Java</code> 中是序列化的过程）；</li>
<li><code>Client Stub</code> 找到服务地址，并将消息体通过网络发送到服务端；</li>
<li><code>Server Stub</code> 收到消息后进行解码（在 <code>Java</code> 中是反序列化的过程）；</li>
<li><code>Server Stub</code> 根据解码结果调用 <code>Server</code> 处理；</li>
<li><code>Server</code> 将结果返回给 <code>Server Stub</code>；</li>
<li><code>Server Stub</code> 将返回结果打包成消息体（在 <code>Java</code> 中是序列化的过程）；</li>
<li><code>Server Stub</code> 将打包后的消息通过网络并发送至消费方；</li>
<li><code>Client Stub</code> 接收到消息，并进行解码（在 <code>Java</code> 中是反序列化的过程）；</li>
<li><code>Client</code> 得到最终结果。</li>
</ol>
<p>一图顶千言</p>
<img src="/2019/07/11/rpc-introduction/theory.jpg" class="" title="RPC 原理">

<h3 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h3><p>我们简单的采用 <code>Java</code> 语言来实现一个 <code>RPC</code> 框架。</p>
<h4 id="命名"><a href="#命名" class="headerlink" title="命名"></a>命名</h4><p>作为钢铁侠粉丝，这里我们将项目命名为 <a href="https://github.com/fuyongde/edith/releases/tag/0.1.0">Edith</a>。</p>
<blockquote>
<p>Even dead,I am hero. -<em>Tony Stark</em></p>
</blockquote>
<p>伊人已逝，仍是英雄。永远纪念 _Tony Stark_。</p>
<h4 id="暴露服务"><a href="#暴露服务" class="headerlink" title="暴露服务"></a>暴露服务</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">RpcFramework</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">export</span><span class="params">(<span class="keyword">final</span> Object service, <span class="type">int</span> port)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        <span class="keyword">if</span> (service == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">&quot;Service instance is null&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (port &lt;= <span class="number">0</span> || port &gt; <span class="number">65535</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">&quot;Invalid port &quot;</span> + port);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">&quot;Export service &quot;</span> + service.getClass().getName() + <span class="string">&quot; on port &quot;</span> + port);</span><br><span class="line">        <span class="type">ServerSocket</span> <span class="variable">server</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ServerSocket</span>(port);</span><br><span class="line">        <span class="keyword">for</span> (; ; ) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="keyword">final</span> <span class="type">Socket</span> <span class="variable">socket</span> <span class="operator">=</span> server.accept();</span><br><span class="line">                <span class="keyword">new</span> <span class="title class_">Thread</span>(() -&gt; &#123;</span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        <span class="keyword">try</span> &#123;</span><br><span class="line">                            <span class="type">ObjectInputStream</span> <span class="variable">input</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectInputStream</span>(socket.getInputStream());</span><br><span class="line">                            <span class="keyword">try</span> &#123;</span><br><span class="line">                                <span class="type">String</span> <span class="variable">methodName</span> <span class="operator">=</span> input.readUTF();</span><br><span class="line">                                Class&lt;?&gt;[] parameterTypes = (Class&lt;?&gt;[]) input.readObject();</span><br><span class="line">                                Object[] arguments = (Object[]) input.readObject();</span><br><span class="line">                                <span class="type">ObjectOutputStream</span> <span class="variable">output</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectOutputStream</span>(socket.getOutputStream());</span><br><span class="line">                                <span class="keyword">try</span> &#123;</span><br><span class="line">                                    <span class="type">Method</span> <span class="variable">method</span> <span class="operator">=</span> service.getClass().getMethod(methodName, parameterTypes);</span><br><span class="line">                                    <span class="type">Object</span> <span class="variable">result</span> <span class="operator">=</span> method.invoke(service, arguments);</span><br><span class="line">                                    output.writeObject(result);</span><br><span class="line">                                &#125; <span class="keyword">catch</span> (Throwable t) &#123;</span><br><span class="line">                                    output.writeObject(t);</span><br><span class="line">                                &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                                    output.close();</span><br><span class="line">                                &#125;</span><br><span class="line">                            &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                                input.close();</span><br><span class="line">                            &#125;</span><br><span class="line">                        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                            socket.close();</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">                        e.printStackTrace();</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;).start();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="引用服务"><a href="#引用服务" class="headerlink" title="引用服务"></a>引用服务</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">RpcFramework</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt; T <span class="title function_">refer</span><span class="params">(<span class="keyword">final</span> Class&lt;T&gt; interfaceClass, <span class="keyword">final</span> String host, <span class="keyword">final</span> <span class="type">int</span> port)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (interfaceClass == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">&quot;Interface class is null&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (!interfaceClass.isInterface()) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">&quot;The &quot;</span> + interfaceClass.getName() + <span class="string">&quot; must be interface class!&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (host == <span class="literal">null</span> || host.length() == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">&quot;Host is null!&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (port &lt;= PORT_MIN || port &gt; PORT_MAX) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">&quot;Invalid port &quot;</span> + port);</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">&quot;Get remote service &quot;</span> + interfaceClass.getName() + <span class="string">&quot; from server &quot;</span> + host + <span class="string">&quot;:&quot;</span> + port);</span><br><span class="line">        <span class="keyword">return</span> (T) Proxy.newProxyInstance(interfaceClass.getClassLoader(), <span class="keyword">new</span> <span class="title class_">Class</span>&lt;?&gt;[]&#123;interfaceClass&#125;, (proxy, method, arguments) -&gt; &#123;</span><br><span class="line">            <span class="type">Socket</span> <span class="variable">socket</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Socket</span>(host, port);</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="type">ObjectOutputStream</span> <span class="variable">output</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectOutputStream</span>(socket.getOutputStream());</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    output.writeUTF(method.getName());</span><br><span class="line">                    output.writeObject(method.getParameterTypes());</span><br><span class="line">                    output.writeObject(arguments);</span><br><span class="line">                    <span class="type">ObjectInputStream</span> <span class="variable">input</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectInputStream</span>(socket.getInputStream());</span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        <span class="type">Object</span> <span class="variable">result</span> <span class="operator">=</span> input.readObject();</span><br><span class="line">                        <span class="keyword">if</span> (result <span class="keyword">instanceof</span> Throwable) &#123;</span><br><span class="line">                            <span class="keyword">throw</span> (Throwable) result;</span><br><span class="line">                        &#125;</span><br><span class="line">                        <span class="keyword">return</span> result;</span><br><span class="line">                    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                        input.close();</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                    output.close();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                socket.close();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="定义接口"><a href="#定义接口" class="headerlink" title="定义接口"></a>定义接口</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">HelloService</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 返回一个字符串</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> name name</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> Hello name</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    String <span class="title function_">hello</span><span class="params">(String name)</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="编写服务提供者"><a href="#编写服务提供者" class="headerlink" title="编写服务提供者"></a>编写服务提供者</h4><p>先实现 <code>HelloService</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HelloServiceImpl</span> <span class="keyword">implements</span> <span class="title class_">HelloService</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">hello</span><span class="params">(String name)</span> &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> <span class="string">&quot;Hello &quot;</span> + name;</span><br><span class="line">        System.out.println(Thread.currentThread().getName() + <span class="string">&quot; &quot;</span> + result);</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>启动并暴漏服务</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Provider</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        <span class="type">HelloService</span> <span class="variable">helloService</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">HelloServiceImpl</span>();</span><br><span class="line">        RpcFramework.export(helloService, <span class="number">8888</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="编写服务调用者"><a href="#编写服务调用者" class="headerlink" title="编写服务调用者"></a>编写服务调用者</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Consumer</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">HelloService</span> <span class="variable">helloService</span> <span class="operator">=</span> RpcFramework.refer(HelloService.class, <span class="string">&quot;127.0.0.1&quot;</span>, <span class="number">8888</span>);</span><br><span class="line">        <span class="type">String</span> <span class="variable">name</span> <span class="operator">=</span> <span class="string">&quot;Peter Parker&quot;</span>;</span><br><span class="line">        System.out.println(helloService.hello(name));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>执行 <code>Consumer</code>，查看控制台输出：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Hello Peter Parker</span><br></pre></td></tr></table></figure>

<h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><p>本文的例子，采用了阻塞的 Socket IO 流来进行 Client 和 Server 之间的端对端通信。方法的远程调用采用了 <code>Java</code> 的动态代理，参数的序列化与反序列化采用 <code>ObjectStream</code>。成熟的 <code>RPC</code> 框架会对上述的实现方式进行替换，比如采用 <code>Netty</code> 的 <code>NIO</code> 来实现非阻塞通信，采用成熟的注册中心来统一管理服务的注册与发现，采用 <code>hession2</code>，<code>fastjson</code> 等序列化方式等等。成熟的 <code>RPC</code> 框架还会考虑调用过程中的诸多问题，比如重试机制、超时配置等等。</p>
<p>本文参考了 <a href="https://www.cnkirito.moe/easy-know-rpc/">徐靖峰的博客</a>。</p>
]]></content>
      <tags>
        <tag>RPC</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Cloud Alibaba Introduction</title>
    <url>/2019/10/11/spring-cloud-alibaba-introduction/</url>
    <content><![CDATA[<h2 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h2><blockquote>
<p><code>Spring Cloud Alibaba</code> 致力于提供微服务开发的一站式解决方案。此项目包含开发分布式应用服务的必需组件（涵盖了服务治理、配置管理、限流降级以及阿里开源生态和商业生态的诸多组件），方便开发者通过 <code>Spring Cloud</code> 编程模型轻松使用这些组件来开发分布式应用服务。依托 <code>Spring Cloud Alibaba</code>，您只需要添加一些注解和少量配置，就可以将 <code>Spring Cloud</code> 应用接入阿里分布式应用解决方案，通过阿里中间件来迅速搭建分布式应用系统。</p>
</blockquote>
<h3 id="Spring-Cloud-是什么"><a href="#Spring-Cloud-是什么" class="headerlink" title="Spring Cloud 是什么"></a>Spring Cloud 是什么</h3><blockquote>
<p>Spring Cloud provides tools for developers to quickly build some of the common patterns in distributed systems .</p>
</blockquote>
<p><code>Spring Cloud</code> 为开发者提供了快速构建分布式系统的模型。<code>Spring Cloud</code> 除了抽象了分布式系统里的一些接口，并且提供了一系列开箱即用的组件。</p>
<p><code>Spring Cloud</code> 更详细的介绍以及相关的组件，可以参考<a href="https://spring.io/projects/spring-cloud">官网</a>。</p>
<h3 id="Spring-Cloud-Alibaba-对-Spring-Cloud-的意义"><a href="#Spring-Cloud-Alibaba-对-Spring-Cloud-的意义" class="headerlink" title="Spring Cloud Alibaba 对 Spring Cloud 的意义"></a>Spring Cloud Alibaba 对 Spring Cloud 的意义</h3><ul>
<li><code>Spring Cloud Alibaba</code> 在 <code>Spring Cloud</code> 体系中是一个套件，类似于 <code>Netflix OSS</code>，为开发者提供了更多的选择。</li>
<li>在 <code>Netflix OSS</code> 诸多重要组件先后宣布停止新功能开发的大背景下 <code>Spring Cloud Alibaba</code> 作为一个高速迭代中的新生项目无疑是更合适的选择。</li>
<li>将国内非常流行的 <code>Dubbo</code>、<code>RocketMQ</code> 等融入 <code>Spring Cloud</code> 体系，使得应用的接入和改造的成本更低。</li>
<li><code>Spring Cloud Alibaba</code> 是中文文档最全的 <code>Spring Cloud</code> 组件。</li>
</ul>
<h2 id="组件"><a href="#组件" class="headerlink" title="组件"></a>组件</h2><h3 id="开源组件"><a href="#开源组件" class="headerlink" title="开源组件"></a>开源组件</h3><ul>
<li>Nacos Config：配置中心</li>
<li>Nacos Discovery：服务发现（注册中心）</li>
<li>Sentinel：把流量作为切入点，从流量控制、熔断降级、系统负载保护等多个维度保护服务的稳定性。</li>
<li>RocketMQ：<code>Apache RocketMQ™</code> 基于 Java 的高性能、高吞吐量的分布式消息和流计算平台。</li>
<li>Dubbo Spring Cloud：<code>Apache Dubbo™</code> 是一款高性能 Java RPC 框架。</li>
<li>Seata：阿里巴巴开源产品，一个易于使用的高性能微服务分布式事务解决方案。</li>
</ul>
<h3 id="商业化组件"><a href="#商业化组件" class="headerlink" title="商业化组件"></a>商业化组件</h3><ul>
<li>ANS：注册中心</li>
<li>ACM：配置中心</li>
<li>OSS：阿里云对象存储服务</li>
<li>SchedulerX：阿里中间件团队开发的一款分布式任务调度产品，支持周期性的任务与固定时间点触发任务。</li>
<li>SMS：覆盖全球的短信服务，友好、高效、智能的互联化通讯能力，帮助企业迅速搭建客户触达通道。</li>
</ul>
<h3 id="组件的版本关系"><a href="#组件的版本关系" class="headerlink" title="组件的版本关系"></a>组件的版本关系</h3><table>
<thead>
<tr>
<th>Spring Cloud Alibaba Version</th>
<th>Sentinel Version</th>
<th>Nacos Version</th>
<th>RocketMQ Version</th>
<th>Dubbo Version</th>
<th>Seata Version</th>
</tr>
</thead>
<tbody><tr>
<td>2.1.0.RELEASE 2.0.0.RELEASE 1.5.0.RELEASE</td>
<td>1.6.3</td>
<td>1.1.1</td>
<td>4.4.0</td>
<td>2.7.3</td>
<td>0.7.1</td>
</tr>
<tr>
<td>0.9.0.RELEASE 0.2.2.RELEASE 0.1.2.RELEASE</td>
<td>1.5.2</td>
<td>1.0.0</td>
<td>4.4.0</td>
<td>2.7.1</td>
<td>0.4.2</td>
</tr>
<tr>
<td>0.2.1.RELEASE 0.1.1.RELEASE</td>
<td>1.4.0</td>
<td>0.6.2</td>
<td>4.3.1</td>
<td>x</td>
<td>x</td>
</tr>
<tr>
<td>0.2.0.RELEASE 0.1.0.RELEASE</td>
<td>1.3.0-GA</td>
<td>0.3.0</td>
<td>x</td>
<td>x</td>
<td>x</td>
</tr>
</tbody></table>
<h3 id="与-Spring-Cloud-以及-Spring-Boot-的版本依赖关系"><a href="#与-Spring-Cloud-以及-Spring-Boot-的版本依赖关系" class="headerlink" title="与 Spring Cloud 以及 Spring Boot 的版本依赖关系"></a>与 Spring Cloud 以及 Spring Boot 的版本依赖关系</h3><table>
<thead>
<tr>
<th>Spring Cloud Version</th>
<th>Spring Cloud Alibaba Version</th>
<th>Spring Boot Version</th>
</tr>
</thead>
<tbody><tr>
<td>Spring Cloud Greenwich</td>
<td>2.1.0.RELEASE</td>
<td>2.1.X.RELEASE</td>
</tr>
<tr>
<td>Spring Cloud Finchley</td>
<td>2.0.0.RELEASE</td>
<td>2.0.X.RELEASE</td>
</tr>
<tr>
<td>Spring Cloud Edgware</td>
<td>1.5.0.RELEASE</td>
<td>1.5.X.RELEASE</td>
</tr>
</tbody></table>
<p>过老的版本不推荐</p>
<h2 id="Spring-Cloud-Alibaba-VS-Dubbo"><a href="#Spring-Cloud-Alibaba-VS-Dubbo" class="headerlink" title="Spring Cloud Alibaba VS Dubbo"></a>Spring Cloud Alibaba VS Dubbo</h2><p><code>Dubbo Spring Cloud</code> (归属于 Spring Cloud Alibaba 项目)是基于 <code>Dubbo Spring Boot</code> (归属于 Dubbo 项目)和 <code>Spring Cloud</code>开发，使得 Dubbo 应用以一种更优雅的方式融入 <code>Spring Cloud</code> 体系。</p>
<h3 id="功能对比"><a href="#功能对比" class="headerlink" title="功能对比"></a>功能对比</h3><p>由于 <code>Dubbo Spring Cloud</code> 构建在原生的 <code>Spring Cloud</code> 之上，其服务治理方面的能力可认为是 <code>Spring Cloud</code> 的加强版，不仅完全覆盖 <code>Spring Cloud</code> <a href="https://cloud.spring.io/spring-cloud-static/Greenwich.RELEASE/single/spring-cloud.html#_features">原生特性</a>，而且提供更为稳定和成熟的实现，特性比对如下表所示：</p>
<table>
<thead>
<tr>
<th>功能组件</th>
<th>Spring Cloud</th>
<th>Dubbo Spring Cloud</th>
<th>Dubbo</th>
</tr>
</thead>
<tbody><tr>
<td>分布式配置（Distributed configuration）</td>
<td>Git、Zookeeper、Consul、JDBC</td>
<td>Spring Cloud 分布式配置 + <a href="http://dubbo.apache.org/zh-cn/docs/user/configuration/config-center.html">Dubbo 配置中心</a></td>
<td>Dubbo 配置中心</td>
</tr>
<tr>
<td>服务注册与发现（Service registration and discovery）</td>
<td>Eureka、Zookeeper、Consul</td>
<td>Spring Cloud 原生注册中心[1] + <a href="http://dubbo.apache.org/zh-cn/docs/user/references/registry/introduction.html">Dubbo 原生注册中心</a></td>
<td>Dubbo 原生注册中心</td>
</tr>
<tr>
<td>负载均衡（Load balancing）</td>
<td>Ribbon（随机、轮询等算法）</td>
<td>Dubbo 内建实现（随机、轮询等算法 + 权重等特性）[2]</td>
<td>Dubbo 内建实现（随机、轮询等算法 + 权重等特性）</td>
</tr>
<tr>
<td>服务熔断（Circuit Breakers）</td>
<td>Spring Cloud Hystrix</td>
<td>Spring Cloud Hystrix + Alibaba Sentinel[3] 等</td>
<td>Alibaba Sentinel</td>
</tr>
<tr>
<td>服务调用（Service-to-service calls）</td>
<td>Open Feign、<code>RestTemplate</code></td>
<td>Spring Cloud 服务调用 + Dubbo <code>@Reference</code></td>
<td>Dubbo <code>@Reference</code></td>
</tr>
<tr>
<td>链路跟踪（Tracing）</td>
<td><a href="https://spring.io/projects/spring-cloud-sleuth">Spring Cloud Sleuth</a> + <a href="https://github.com/apache/incubator-zipkin">Zipkin</a></td>
<td>Zipkin、opentracing 等</td>
<td>Zipkin、opentracing 等</td>
</tr>
</tbody></table>
<p>[1]: <code>Spring Cloud</code> 原生注册中心，除 <code>Eureka</code>、<code>Zookeeper</code>、<code>Consul</code> 之外，还包括 <code>Spring Cloud Alibaba</code> 中的 <code>Nacos</code></p>
<p>[2]: <code>Dubbo Spring Cloud</code> 相对于单纯的 <code>Dubbo</code> 服务的一个坑：单纯的 <code>Dubbo</code> 是将每一个由 @org.apache.dubbo.config.annotation.Service 注解的服务作为单独的服务暴露的，<code>Dubbo Spring Cloud</code> 如果采用推荐的写法，是将所属的工程作为一个服务来暴露的，若同一个服务版本不一致，会导致消费者找不到服务的情况。</p>
<p>[3]: <a href="https://github.com/alibaba/Sentinel/wiki/%E4%BB%8B%E7%BB%8D">Alibaba Sentinel</a>：<code>Sentinel</code> 以流量为切入点，从流量控制、熔断降级、系统负载保护等多个维度保护服务的稳定性 - ，目前 <code>Sentinel</code> 已被 <code>Spring Cloud</code> 项目纳为<a href="https://spring.io/blog/2019/04/16/introducing-spring-cloud-circuit-breaker">断路器的候选实现</a></p>
<h3 id="写法对比"><a href="#写法对比" class="headerlink" title="写法对比"></a>写法对比</h3><p>从依赖上，<code>Dubbo Spring Boot</code> 的项目需要引入 <code>dubbo-spring-boot-starter</code> 相关依赖，<code>Dubbo Spring Cloud</code> 项目需要引入 <code>spring-cloud-starter-dubbo</code>相关依赖。</p>
<p>Example：</p>
<p><code>Dubbo Spring Boot</code></p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.apache.dubbo<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>dubbo-spring-boot-starter<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.apache.dubbo<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>dubbo<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.apache.dubbo<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>dubbo-registry-nacos<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.alibaba.nacos<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>nacos-client<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><code>Dubbo Spring Cloud</code></p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.alibaba.cloud<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-cloud-starter-dubbo<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.alibaba.cloud<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-cloud-starter-alibaba-nacos-discovery<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>从配置和代码上，<code>Dubbo Spring Cloud</code> 完全兼容 <code>Dubbo Spring Boot</code> 的配置写法，但是更推荐将 <code>Dubbo</code> 挂载本机 <code>Spring Cloud</code> 的服务发现的地址，并且推荐配置消费者所订阅的服务，从而来降低服务寻址时的 CPU 和内存开销。写发如下：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">cloud:</span></span><br><span class="line">    <span class="attr">nacos:</span></span><br><span class="line">      <span class="attr">discovery:</span></span><br><span class="line">        <span class="attr">server-addr:</span> <span class="string">nacos1.dafy.com:8848</span></span><br><span class="line"><span class="attr">dubbo:</span></span><br><span class="line">  <span class="attr">registry:</span></span><br><span class="line">    <span class="attr">address:</span> <span class="string">spring-cloud://localhost</span></span><br><span class="line">  <span class="attr">cloud:</span></span><br><span class="line">    <span class="attr">subscribed-services:</span> <span class="string">bs-auth</span></span><br></pre></td></tr></table></figure>
]]></content>
      <tags>
        <tag>Dubbo</tag>
        <tag>Spring Cloud</tag>
        <tag>Spring Cloud Alibaba</tag>
      </tags>
  </entry>
  <entry>
    <title>pip-手册</title>
    <url>/2020/03/20/pip-%E6%89%8B%E5%86%8C/</url>
    <content><![CDATA[<p>PyPI (Python Package Index) 是 <code>Python</code> 编程语言的软件存储库。开发者可以通过 PyPI 查找和安装由 <code>Python</code> 社区开发和共享的软件，也可以将自己开发的库上传至 PyPI 。</p>
<p>由于国外访问比较慢，我们可以通过配置国内的镜像来进行加速访问。</p>
<h2 id="配置-Aliyun-的镜像"><a href="#配置-Aliyun-的镜像" class="headerlink" title="配置 Aliyun 的镜像"></a>配置 Aliyun 的镜像</h2><h3 id="Linux-平台"><a href="#Linux-平台" class="headerlink" title="Linux 平台"></a>Linux 平台</h3><ol>
<li>找到下列文件</li>
</ol>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">~/.pip/pip.conf</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>在上述文件中添加或修改:</li>
</ol>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">[global]</span><br><span class="line">index-url = https://mirrors.aliyun.com/pypi/simple/</span><br><span class="line"></span><br><span class="line">[install]</span><br><span class="line">trusted-host=mirrors.aliyun.com</span><br></pre></td></tr></table></figure>

<h3 id="Windows-平台"><a href="#Windows-平台" class="headerlink" title="Windows 平台"></a>Windows 平台</h3><p>在 <code>exporler</code> 地址栏输入 <code>%AppData%</code>，进入文件夹后，在 <code>pip</code> 文件夹下新建 <code>pip.ini</code> 文件并写入以下配置。</p>
<figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="section">[global]</span></span><br><span class="line"><span class="attr">index-url</span> = http://mirrors.aliyun.com/pypi/simple</span><br><span class="line"><span class="attr">trusted-host</span> = mirrors.aliyun.com</span><br></pre></td></tr></table></figure>
]]></content>
      <tags>
        <tag>pip</tag>
        <tag>Python</tag>
      </tags>
  </entry>
  <entry>
    <title>事务隔离级别</title>
    <url>/2019/11/14/transaction-isolation/</url>
    <content><![CDATA[<p>本文简单介绍一下数据库的事务隔离级别，所用数据库采用 <code>MySQL</code>，<code>MySQL</code> 提供了两种事务性的存储引擎 <code>InnoDB</code> 和 <code>NDB Cluster</code>，本文示例采用 <code>InnoDB</code> 存储引擎。</p>
<h2 id="查看当前数据的事务隔离级别"><a href="#查看当前数据的事务隔离级别" class="headerlink" title="查看当前数据的事务隔离级别"></a>查看当前数据的事务隔离级别</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 查看当前数据库事务隔离级别</span><br><span class="line">mysql&gt; SHOW VARIABLES LIKE &#x27;%transaction_isolation%&#x27;;</span><br><span class="line">-- 或者</span><br><span class="line">mysql&gt; SELECT @@transaction_isolation;</span><br><span class="line">-- 低版本的 MySQL 查看事务隔离级别的属性是不一样的</span><br><span class="line">mysql&gt; SHOW VARIABLES LIKE &#x27;%tx_isolation%&#x27;;</span><br><span class="line">-- 或者</span><br><span class="line">mysql&gt; SELECT @@tx_isolation;</span><br></pre></td></tr></table></figure>

<h2 id="不同的事务隔离级别所面对的问题"><a href="#不同的事务隔离级别所面对的问题" class="headerlink" title="不同的事务隔离级别所面对的问题"></a>不同的事务隔离级别所面对的问题</h2><table>
<thead>
<tr>
<th>事务隔离级别</th>
<th>脏读</th>
<th>不可重复读</th>
<th>幻读</th>
</tr>
</thead>
<tbody><tr>
<td>读未提交（read-uncommitted）</td>
<td>是</td>
<td>是</td>
<td>是</td>
</tr>
<tr>
<td>读已提交（read-committed）</td>
<td>否</td>
<td>是</td>
<td>是</td>
</tr>
<tr>
<td>可重复读（repeatable-read）</td>
<td>否</td>
<td>否</td>
<td>是</td>
</tr>
<tr>
<td>串行化（serializable）</td>
<td>否</td>
<td>否</td>
<td>否</td>
</tr>
</tbody></table>
<ul>
<li><p><code>脏读</code>：事务 A 读取了事务 B 更新的数据，然后 B 回滚操作，那么 A 读取到的数据是脏数据。</p>
</li>
<li><p><code>不可重复读</code>：事务 A 多次读取同一数据，事务 B 在事务 A 多次读取的过程中，对数据作了更新并提交，导致事务 A 多次读取同一数据时，结果不一致。</p>
</li>
<li><p><code>幻读</code>：系统管理员 A 将数据库中所有学生的成绩从具体分数改为 ABCDE 等级，但是系统管理员 B 就在这个时候插入了一条具体分数的记录，当系统管理员 A 改结束后发现还有一条记录没有改过来，就好像发生了幻觉一样，这就叫幻读。</p>
</li>
</ul>
<p>小结：不可重复读的和幻读很容易混淆，不可重复读侧重于修改，幻读侧重于新增或删除。解决不可重复读的问题只需锁住满足条件的行，解决幻读需要锁表。</p>
<h2 id="脏读的场景"><a href="#脏读的场景" class="headerlink" title="脏读的场景"></a>脏读的场景</h2><h3 id="客户端-A-先开启事务，查询用户余额"><a href="#客户端-A-先开启事务，查询用户余额" class="headerlink" title="客户端 A 先开启事务，查询用户余额"></a>客户端 A 先开启事务，查询用户余额</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 客户端A设置当前会话级别为`read-uncommitted`</span><br><span class="line">mysq&gt; SET SESSION TRANSACTION ISOLATION LEVEL READ UNCOMMITTED;</span><br><span class="line">-- 客户端A开启事务</span><br><span class="line">mysq&gt; START TRANSACTION;</span><br><span class="line">-- 客户端A查询用户余额</span><br><span class="line">mysq&gt; SELECT * FROM wallet WHERE user_id = 1;</span><br><span class="line">-- 结果如下：</span><br><span class="line"># +----+---------+-----------+</span><br><span class="line"># | id | user_id | balance   |</span><br><span class="line"># +----+---------+-----------+</span><br><span class="line"># |  1 |       1 | 100000000 |</span><br><span class="line"># +----+---------+-----------+</span><br></pre></td></tr></table></figure>

<h3 id="客户端-B-开启事务，并修改用户余额"><a href="#客户端-B-开启事务，并修改用户余额" class="headerlink" title="客户端 B 开启事务，并修改用户余额"></a>客户端 B 开启事务，并修改用户余额</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 客户端B设置当前会话级别为`read-uncommitted`</span><br><span class="line">mysq&gt; SET SESSION TRANSACTION ISOLATION LEVEL READ UNCOMMITTED;</span><br><span class="line">-- 客户端B开启事务</span><br><span class="line">mysq&gt; START TRANSACTION;</span><br><span class="line">-- 客户端B修改用户余额，</span><br><span class="line">mysql&gt; UPDATE wallet SET balance = balance + 100 WHERE user_id = 1;</span><br></pre></td></tr></table></figure>

<h3 id="客户端-A-查询用户余额"><a href="#客户端-A-查询用户余额" class="headerlink" title="客户端 A 查询用户余额"></a>客户端 A 查询用户余额</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 客户端A获取用户余额（此时由于B的事务并未提交，故A事务读到了B事务已经更新后的数据，此时发生脏读）</span><br><span class="line">mysq&gt; SELECT * FROM wallet WHERE user_id = 1;</span><br><span class="line">-- 结果如下：</span><br><span class="line"># +----+---------+-----------+</span><br><span class="line"># | id | user_id | balance   |</span><br><span class="line"># +----+---------+-----------+</span><br><span class="line"># |  1 |       1 | 100000100 |</span><br><span class="line"># +----+---------+-----------+</span><br></pre></td></tr></table></figure>

<h3 id="客户端-B-回滚事务"><a href="#客户端-B-回滚事务" class="headerlink" title="客户端 B 回滚事务"></a>客户端 B 回滚事务</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 客户端B回滚事务</span><br><span class="line">mysql&gt; ROLLBACK;</span><br></pre></td></tr></table></figure>

<h3 id="客户端-A-修改用户余额"><a href="#客户端-A-修改用户余额" class="headerlink" title="客户端 A 修改用户余额"></a>客户端 A 修改用户余额</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 客户端A修改用户余额</span><br><span class="line">mysq&gt; UPDATE wallet SET balance = balance - 500 WHERE user_id = 1;</span><br><span class="line"></span><br><span class="line">-- 客户端A查询用户余额</span><br><span class="line">mysq&gt; SELECT * FROM wallet WHERE user_id = 1;</span><br><span class="line">-- 结果如下：</span><br><span class="line"># +----+---------+----------+</span><br><span class="line"># | id | user_id | balance  |</span><br><span class="line"># +----+---------+----------+</span><br><span class="line"># |  1 |       1 | 99999500 |</span><br><span class="line"># +----+---------+----------+</span><br><span class="line">-- 对于A客户端来讲，此时的结果是很奇怪的。</span><br><span class="line">-- 客户端A提交事务</span><br><span class="line">mysql&gt; COMMIT;</span><br></pre></td></tr></table></figure>

<h2 id="不可重复读的场景"><a href="#不可重复读的场景" class="headerlink" title="不可重复读的场景"></a>不可重复读的场景</h2><h3 id="客户端-A-开启事务，查询用户余额"><a href="#客户端-A-开启事务，查询用户余额" class="headerlink" title="客户端 A 开启事务，查询用户余额"></a>客户端 A 开启事务，查询用户余额</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 客户端A设置当前会话级别为`read-committed`</span><br><span class="line">mysql&gt; SET SESSION TRANSACTION ISOLATION LEVEL READ COMMITTED;</span><br><span class="line">-- 客户端A开启事务</span><br><span class="line">mysql&gt; BEGIN;</span><br><span class="line">-- 客户端A查询用户余额</span><br><span class="line">SELECT * FROM wallet WHERE user_id = 1;</span><br><span class="line">-- 结果如下:</span><br><span class="line"># +----+---------+---------+</span><br><span class="line"># | id | user_id | balance |</span><br><span class="line"># +----+---------+---------+</span><br><span class="line"># |  1 |       1 |     200 |</span><br><span class="line"># +----+---------+---------+</span><br></pre></td></tr></table></figure>

<h3 id="客户端-B-开启事务，并修改用户余额-1"><a href="#客户端-B-开启事务，并修改用户余额-1" class="headerlink" title="客户端 B 开启事务，并修改用户余额"></a>客户端 B 开启事务，并修改用户余额</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 客户端B设置当前会话级别为`read-committed`</span><br><span class="line">mysql&gt; SET SESSION TRANSACTION ISOLATION LEVEL READ COMMITTED;</span><br><span class="line">-- 客户端B开启事务</span><br><span class="line">mysql&gt; BEGIN;</span><br><span class="line">-- 客户端B修改用户余额</span><br><span class="line">mysql&gt; UPDATE wallet SET balance = balance + 100 WHERE user_id = 1;</span><br><span class="line">-- 客户端B查询用户余额</span><br><span class="line">mysql&gt; SELECT * FROM wallet WHERE user_id = 1;</span><br><span class="line">-- 结果如下：</span><br><span class="line"># +----+---------+---------+</span><br><span class="line"># | id | user_id | balance |</span><br><span class="line"># +----+---------+---------+</span><br><span class="line"># |  1 |       1 |     300 |</span><br><span class="line"># +----+---------+---------+</span><br></pre></td></tr></table></figure>

<h3 id="客户端-A-查询用户余额-1"><a href="#客户端-A-查询用户余额-1" class="headerlink" title="客户端 A 查询用户余额"></a>客户端 A 查询用户余额</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 客户端A查询用户余额</span><br><span class="line">mysql&gt; SELECT * FROM wallet WHERE user_id = 1;</span><br><span class="line">-- 结果如下：</span><br><span class="line"># +----+---------+---------+</span><br><span class="line"># | id | user_id | balance |</span><br><span class="line"># +----+---------+---------+</span><br><span class="line"># |  1 |       1 |     200 |</span><br><span class="line"># +----+---------+---------+</span><br><span class="line">-- 此时客户端B的事务还没有提交，故客户端A不能查询到B事务已经更新的数据，解决了脏读的问题</span><br></pre></td></tr></table></figure>

<h3 id="客户端-B-提交事务"><a href="#客户端-B-提交事务" class="headerlink" title="客户端 B 提交事务"></a>客户端 B 提交事务</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 客户端B提交事务</span><br><span class="line">mysql&gt; COMMIT;</span><br></pre></td></tr></table></figure>

<h3 id="客户端-A-查询用户余额-2"><a href="#客户端-A-查询用户余额-2" class="headerlink" title="客户端 A 查询用户余额"></a>客户端 A 查询用户余额</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 客户端A查询用户余额</span><br><span class="line">mysql&gt; SELECT * FROM wallet WHERE user_id = 1;</span><br><span class="line">-- 结果如下：</span><br><span class="line"># +----+---------+---------+</span><br><span class="line"># | id | user_id | balance |</span><br><span class="line"># +----+---------+---------+</span><br><span class="line"># |  1 |       1 |     300 |</span><br><span class="line"># +----+---------+---------+</span><br><span class="line">-- 此时客户端B的事务还已经提交，结果与上一步不一致，即产生了不可重复读的问题。</span><br><span class="line">-- 客户端A提交事务</span><br><span class="line">mysql&gt; COMMIT;</span><br></pre></td></tr></table></figure>

<h2 id="幻读的场景"><a href="#幻读的场景" class="headerlink" title="幻读的场景"></a>幻读的场景</h2><h3 id="客户端-A-开启事务，查询所有用户"><a href="#客户端-A-开启事务，查询所有用户" class="headerlink" title="客户端 A 开启事务，查询所有用户"></a>客户端 A 开启事务，查询所有用户</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 客户端A设置当前会话级别为`repeatable-read`</span><br><span class="line">mysql&gt; SET SESSION TRANSACTION ISOLATION LEVEL REPEATABLE READ;</span><br><span class="line">-- 客户端A开启事务</span><br><span class="line">mysql&gt; BEGIN;</span><br><span class="line">-- 客户端A查询所有用户余额</span><br><span class="line">SELECT * FROM wallet;</span><br><span class="line">-- 结果如下:</span><br><span class="line"># +----+---------+---------+</span><br><span class="line"># | id | user_id | balance |</span><br><span class="line"># +----+---------+---------+</span><br><span class="line"># |  1 |       1 |     100 |</span><br><span class="line"># +----+---------+---------+</span><br></pre></td></tr></table></figure>

<h3 id="客户端-B-开启事务，并插入一条记录"><a href="#客户端-B-开启事务，并插入一条记录" class="headerlink" title="客户端 B 开启事务，并插入一条记录"></a>客户端 B 开启事务，并插入一条记录</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 客户端B设置当前会话级别为`repeatable-read`</span><br><span class="line">mysql&gt; SET SESSION TRANSACTION ISOLATION LEVEL REPEATABLE READ;</span><br><span class="line">-- 客户端B开启事务</span><br><span class="line">mysql&gt; BEGIN;</span><br><span class="line">-- 客户端B查询所有用户余额</span><br><span class="line">SELECT * FROM wallet;</span><br><span class="line">-- 结果如下:</span><br><span class="line"># +----+---------+---------+</span><br><span class="line"># | id | user_id | balance |</span><br><span class="line"># +----+---------+---------+</span><br><span class="line"># |  1 |       1 |     100 |</span><br><span class="line"># +----+---------+---------+</span><br><span class="line">-- 客户端B插入一条记录</span><br><span class="line">mysq&gt; INSERT INTO wallet (`id`, `user_id`, `balance`) VALUES (2, 2, 200);</span><br><span class="line">-- 客户端B查询所有的用户余额</span><br><span class="line">mysql&gt; SELECT * FROM wallet;</span><br><span class="line">-- 结果如下</span><br><span class="line"># +----+---------+---------+</span><br><span class="line"># | id | user_id | balance |</span><br><span class="line"># +----+---------+---------+</span><br><span class="line"># |  1 |       1 |     100 |</span><br><span class="line"># |  2 |       2 |     200 |</span><br><span class="line"># +----+---------+---------+</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h3 id="客户端-A-查询所有用户余额"><a href="#客户端-A-查询所有用户余额" class="headerlink" title="客户端 A 查询所有用户余额"></a>客户端 A 查询所有用户余额</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 客户端A查询所有用户余额, 此时发生幻读。</span><br><span class="line">SELECT * FROM wallet;</span><br><span class="line">-- 结果如下:</span><br><span class="line"># +----+---------+---------+</span><br><span class="line"># | id | user_id | balance |</span><br><span class="line"># +----+---------+---------+</span><br><span class="line"># |  1 |       1 |     100 |</span><br><span class="line"># +----+---------+---------+</span><br></pre></td></tr></table></figure>

<h3 id="客户端-B-提交事务-1"><a href="#客户端-B-提交事务-1" class="headerlink" title="客户端 B 提交事务"></a>客户端 B 提交事务</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 客户端B提交事务</span><br><span class="line">mysq&gt; COMMIT;</span><br></pre></td></tr></table></figure>

<h3 id="客户端-A-插入一条记录"><a href="#客户端-A-插入一条记录" class="headerlink" title="客户端 A 插入一条记录"></a>客户端 A 插入一条记录</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 客户A插入id为2的记录</span><br><span class="line">mysql&gt; INSERT INTO wallet (`id`, `user_id`, `balance`) VALUES (2, 2, 200);</span><br><span class="line">-- 结果失败了，</span><br><span class="line">ERROR 1062 (23000): Duplicate entry &#x27;2&#x27; for key &#x27;PRIMARY&#x27;</span><br><span class="line">-- 客户端A回滚</span><br><span class="line">mysql&gt; ROLLBACK;</span><br></pre></td></tr></table></figure>
]]></content>
      <tags>
        <tag>数据库</tag>
        <tag>MySQL</tag>
        <tag>事务</tag>
      </tags>
  </entry>
  <entry>
    <title>zsh 手册</title>
    <url>/2021/08/24/zsh-%E6%89%8B%E5%86%8C/</url>
    <content><![CDATA[<p>zsh 可能是目前最好用的终端。</p>
<h2 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h2><h3 id="Mac-OS"><a href="#Mac-OS" class="headerlink" title="Mac OS"></a>Mac OS</h3><p>MacOS 自带 zsh，并且当前最新版本 MasOS 默认终端即为 zsh</p>
<h3 id="Ubuntu"><a href="#Ubuntu" class="headerlink" title="Ubuntu"></a>Ubuntu</h3><p>使用 <code>sudo apt install zsh</code> 命令即可安装 zsh</p>
<h3 id="CentOS"><a href="#CentOS" class="headerlink" title="CentOS"></a>CentOS</h3><p>使用 <code>sudo yum install zsh</code> 命令即可安装 zsh</p>
<p>在安装好 zsh 之后，第一次使用的时候，会有如下的提示：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">This is the Z Shell configuration function for new users,</span><br><span class="line">zsh-newuser-install.</span><br><span class="line">You are seeing this message because you have no zsh startup files</span><br><span class="line">(the files .zshenv, .zprofile, .zshrc, .zlogin in the directory</span><br><span class="line">~).  This function can help you with a few settings that should</span><br><span class="line">make your use of the shell easier.</span><br><span class="line"></span><br><span class="line">You can:</span><br><span class="line"></span><br><span class="line">(q)  Quit and do nothing.  The function will be run again next time.</span><br><span class="line"></span><br><span class="line">(0)  Exit, creating the file ~/.zshrc containing just a comment.</span><br><span class="line">     That will prevent this function being run again.</span><br><span class="line"></span><br><span class="line">(1)  Continue to the main menu.</span><br><span class="line"></span><br><span class="line">(2)  Populate your ~/.zshrc with the configuration recommended</span><br><span class="line">     by the system administrator and exit (you will need to edit</span><br><span class="line">     the file by hand, if so desired).</span><br><span class="line"></span><br><span class="line">--- Type one of the keys in parentheses ---</span><br></pre></td></tr></table></figure>

<p>这里我推荐直接使用选项 (2)，通过 <code>~/.zshrc</code> 文件来配置我们的 zsh。</p>
<h3 id="查看-zsh-版本"><a href="#查看-zsh-版本" class="headerlink" title="查看 zsh 版本"></a>查看 zsh 版本</h3><p>使用 <code>zsh --version</code> 命令即可查看 zsh 的版本。</p>
<h3 id="将-zsh-设置为默认的-shell"><a href="#将-zsh-设置为默认的-shell" class="headerlink" title="将 zsh 设置为默认的 shell"></a>将 zsh 设置为默认的 shell</h3><p>使用 <code>chsh -s /bin/zsh root</code> 即可将 root 用户的默认 shell 设置为 zsh，如需为其他用户设置默认的 shell，将 <code>root</code> 替换为其他用户名即可。</p>
<h3 id="查看默认的-shell"><a href="#查看默认的-shell" class="headerlink" title="查看默认的 shell"></a>查看默认的 shell</h3><p>使用 <code>echo $SHELL</code> 命令即可查看默认的 shell</p>
<h3 id="查看当前正在使用的-shell"><a href="#查看当前正在使用的-shell" class="headerlink" title="查看当前正在使用的 shell"></a>查看当前正在使用的 shell</h3><p>使用 <code>echo $0</code> 命令即可查看当前正在使用的 shell</p>
<h2 id="使用-oh-my-zsh-美化-zsh"><a href="#使用-oh-my-zsh-美化-zsh" class="headerlink" title="使用 oh-my-zsh 美化 zsh"></a>使用 oh-my-zsh 美化 zsh</h2><h3 id="安装-oh-my-zsh"><a href="#安装-oh-my-zsh" class="headerlink" title="安装 oh-my-zsh"></a>安装 oh-my-zsh</h3><p>打开 oh-my-zsh 官网，直接使用官网提供的命令，即可安装 oh-my-zsh，命令如下（任选其一即可）：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">使用 curl 下载</span></span><br><span class="line">sh -c &quot;$(curl -fsSL https://raw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh)&quot;</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">或者使用</span></span><br><span class="line">sh -c &quot;$(wget https://raw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh -O -)&quot;</span><br></pre></td></tr></table></figure>

<p>对于国内的用户，上面命令下载 install.sh 可能存在连接超时的问题，我们可以替换为 gitee 的镜像来进行下载，命令如下：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">wget https://gitee.com/mirrors/oh-my-zsh/raw/master/tools/install.sh</span><br></pre></td></tr></table></figure>

<p>下载之后，使用 <code>sh install.sh</code> 来安装 oh-my-zsh，如果在 clone oh-my-zsh 过程仍然连接超时的情况下，可以通过替换 install.sh 脚本中的仓库地址来进行加速。</p>
<p>将脚本中的如下内容</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">REPO=$&#123;REPO:-ohmyzsh/ohmyzsh&#125;</span><br><span class="line">REMOTE=$&#123;REMOTE:-https://github.com/$&#123;REPO&#125;.git&#125;</span><br></pre></td></tr></table></figure>

<p>替换为</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">REPO=$&#123;REPO:-mirrors/oh-my-zsh&#125;</span><br><span class="line">REMOTE=$&#123;REMOTE:-https://gitee.com/$&#123;REPO&#125;.git&#125;</span><br></pre></td></tr></table></figure>

<h3 id="配置-oh-my-zsh-主题"><a href="#配置-oh-my-zsh-主题" class="headerlink" title="配置 oh-my-zsh 主题"></a>配置 oh-my-zsh 主题</h3><p>修改 <code>~/.zshrc</code> 文件，找到 ZSH_THEME 的选项，修改为自己喜欢的，笔者喜欢使用 ys 主题，其配置如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ZSH_THEME=&quot;ys&quot;</span><br></pre></td></tr></table></figure>

<p>如果你比较喜欢每次打开终端都体验不一样的主题的话，可以使用 random 主题。</p>
<h3 id="安装常用的插件"><a href="#安装常用的插件" class="headerlink" title="安装常用的插件"></a>安装常用的插件</h3><p>下面列举一些非常实用的插件</p>
<ul>
<li><p>zsh-syntax-highlighting 有时候记不住一些命令，等输入完了才知道命令输错了，这个插件直接在输入过程中就会提示你，当前命令是否正确，错误红色，正确绿色</p>
</li>
<li><p>zsh-autosuggestions 该插件会记录你之前输入过的所有命令，并且自动匹配你可能想要输入命令，然后按 → 补全</p>
</li>
</ul>
<p>可以使用如下命令来安装插件</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 安装 zsh-syntax-highlighting</span><br><span class="line">git clone https://github.com/zsh-users/zsh-syntax-highlighting.git $&#123;ZSH_CUSTOM:-~/.oh-my-zsh/custom&#125;/plugins/zsh-syntax-highlighting</span><br><span class="line"></span><br><span class="line"># 安装 zsh-autosuggestions</span><br><span class="line">git clone https://github.com/zsh-users/zsh-autosuggestions $&#123;ZSH_CUSTOM:-~/.oh-my-zsh/custom&#125;/plugins/zsh-autosuggestions</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>修改 ~&#x2F;.zshrc 中插件的配置</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">plugins=(git zsh-syntax-highlighting zsh-autosuggestions)</span><br></pre></td></tr></table></figure>

<p>然后使用 <code>source ~/.zshrc</code> 来使配置生效</p>
]]></content>
      <categories>
        <category>手册</category>
      </categories>
      <tags>
        <tag>shell</tag>
        <tag>bash</tag>
        <tag>zsh</tag>
      </tags>
  </entry>
  <entry>
    <title>一个 Spring 依赖注入的问题浅析</title>
    <url>/2021/08/08/%E4%B8%80%E4%B8%AA-Spring-%E4%BE%9D%E8%B5%96%E6%B3%A8%E5%85%A5%E7%9A%84%E9%97%AE%E9%A2%98%E6%B5%85%E6%9E%90/</url>
    <content><![CDATA[<p>使用 Spring 做项目的先思考一个问题，一个接口有在多个实现类的情况下，在成员变量的声明上如果没有指定注入那个别名的 Bean 的时候 Spring 会如何选择对应的 Bean 来进行注入？</p>
<h2 id="问题模拟"><a href="#问题模拟" class="headerlink" title="问题模拟"></a>问题模拟</h2><p>先来简单说明一下示例代码。</p>
<p>定义一个接口，用于打印当前注入的 Bean 的名称</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">TestService</span> &#123;</span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">printBeanName</span><span class="params">()</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>针对上面定义的接口编写两个实现类，并分别定义其 beanName 为 testService 和 testService2</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Service(&quot;testService&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">TestServiceImpl</span> <span class="keyword">implements</span> <span class="title class_">TestService</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">printBeanName</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;testService&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Service(&quot;testService2&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">TestServiceImpl2</span> <span class="keyword">implements</span> <span class="title class_">TestService</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">printBeanName</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;testService2&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>编写 Handler，并注入 TestService</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">TestHandler</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> TestService testService;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">test</span><span class="params">()</span> &#123;</span><br><span class="line">        testService.printBeanName();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>编写单元测试，并观察输出</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootTest</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">TestHandlerTest</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> TestHandler testHandler;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">test</span><span class="params">()</span> &#123;</span><br><span class="line">        testHandler.test();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>执行该单元测试，发现其打印的 beanName 为 testService。从输出结果可以看出在这种情况下最终注入的是 beanName 为 testService 的 bean，与组合交易子系统的现象一致。</p>
<h2 id="源码分析与验证"><a href="#源码分析与验证" class="headerlink" title="源码分析与验证"></a>源码分析与验证</h2><p>根据 Spring 的文档，使用 Autowired 注解在自动装配时会通过 <code>org.springframework.beans.factory.annotation.AutowiredAnnotationBeanPostProcessor#postProcessProperties</code> 方法来对属性进行赋值操作。我们可以在该方法入口打上断点，并对断点指定条件，如下图：</p>
<img src="/2021/08/08/%E4%B8%80%E4%B8%AA-Spring-%E4%BE%9D%E8%B5%96%E6%B3%A8%E5%85%A5%E7%9A%84%E9%97%AE%E9%A2%98%E6%B5%85%E6%9E%90/debug.png" class="" title="debug">

<p>由于方法深度较深，调试的过程这里不再赘述，我们直接分析影响我们最终结果的方法 <code>org.springframework.beans.factory.support.DefaultListableBeanFactory#determineAutowireCandidate</code>，其源码及分析如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> candidates 为我们要注入的属性对应的所有 Bean 组成的 map</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> descriptor 描述了我们要注入的属性的一些信息，包含了属性对应的类、方法、fieldName 等信息</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">protected</span> String <span class="title function_">determineAutowireCandidate</span><span class="params">(Map&lt;String, Object&gt; candidates, DependencyDescriptor descriptor)</span> &#123;</span><br><span class="line">    <span class="comment">// requiredType 为我们要注入的属性的类型，针对本的例子 requiredType 的值为 TestService 这个类</span></span><br><span class="line">    Class&lt;?&gt; requiredType = descriptor.getDependencyType();</span><br><span class="line">    <span class="comment">// 如果 candidates 中有 Bean 添加了 @Primary 注解，则会返回该 Bean 对应的 beanName</span></span><br><span class="line">    <span class="type">String</span> <span class="variable">primaryCandidate</span> <span class="operator">=</span> determinePrimaryCandidate(candidates, requiredType);</span><br><span class="line">    <span class="keyword">if</span> (primaryCandidate != <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> primaryCandidate;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 如果 candidates 中的 Bean 没有 @Primary 注解，但是添加了 @Priority 注解，会解析 Bean 的优先级，返回优先级最高的 Bean 对应的 beanName</span></span><br><span class="line">    <span class="type">String</span> <span class="variable">priorityCandidate</span> <span class="operator">=</span> determineHighestPriorityCandidate(candidates, requiredType);</span><br><span class="line">    <span class="keyword">if</span> (priorityCandidate != <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> priorityCandidate;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 降级逻辑</span></span><br><span class="line">    <span class="keyword">for</span> (Map.Entry&lt;String, Object&gt; entry : candidates.entrySet()) &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">candidateName</span> <span class="operator">=</span> entry.getKey();</span><br><span class="line">        <span class="type">Object</span> <span class="variable">beanInstance</span> <span class="operator">=</span> entry.getValue();</span><br><span class="line">        <span class="comment">// 此时的 this.resolvableDependencies 是不包含本例子中的 TestService 对应的任一 bean 的</span></span><br><span class="line">        <span class="comment">// descriptor.getDependencyName() 方法的处理中会返回要注入的属性所对应的 fieldName，本例子中的 fieldName 为 testService</span></span><br><span class="line">        <span class="comment">// 最终如果要注入属性名跟 candidates 的 Bean 的 beanName 相同，则会返回对应的 beanName</span></span><br><span class="line">        <span class="keyword">if</span> ((beanInstance != <span class="literal">null</span> &amp;&amp; <span class="built_in">this</span>.resolvableDependencies.containsValue(beanInstance)) ||</span><br><span class="line">                matchesBeanName(candidateName, descriptor.getDependencyName())) &#123;</span><br><span class="line">            <span class="keyword">return</span> candidateName;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>下面我们来验证一下源码中的我们未验证到的情况</p>
<h3 id="使用-Primary-注解来标识主要的类"><a href="#使用-Primary-注解来标识主要的类" class="headerlink" title="使用 Primary 注解来标识主要的类"></a>使用 Primary 注解来标识主要的类</h3><p>将 beanName 为 testService2 的 Bean 加上 Primary 注解并运行单元测试，其打印的 beanName 为 testService2。</p>
<h3 id="使用-Priority-来标识-Bean-的优先级"><a href="#使用-Priority-来标识-Bean-的优先级" class="headerlink" title="使用 Priority 来标识 Bean 的优先级"></a>使用 Priority 来标识 Bean 的优先级</h3><p>去掉上面操作所添加的 Primary 注解，来验证使用 Priority 时的情况。</p>
<ul>
<li>将 beanName 为 testService2 的 bean 加上 @Priority(2) 注解并运行单元测试，其打印的 beanName 为 testService2；</li>
<li>将 beanName 为 testService 的 bean 加上 @Priority(1) 注解，将 beanName 为 testService2 的 bean 加上 @Priority(2) 注解并运行单元测试，其打印的 beanName 为 testService；</li>
<li>将 beanName 为 testService 的 bean 加上 @Priority(3) 注解，将 beanName 为 testService2 的 bean 加上 @Priority(2) 注解并运行单元测试，其打印的 beanName 为 testService2。</li>
</ul>
<h3 id="修改-TestHandler-中的属性名"><a href="#修改-TestHandler-中的属性名" class="headerlink" title="修改 TestHandler 中的属性名"></a>修改 TestHandler 中的属性名</h3><p>去掉上面操作所添加的 Primary 和 Priority 注解，来验证根据属性名注入时的情况。</p>
<ul>
<li>将 TestHandler 中的属性名修改为 testServcie2 并运行并运行单元测试，其打印的 beanName 为 testService2；</li>
<li>将 TestHandler 中的属性名修改为 testServcie3 并运行并运行单元测试，程序抛出 NoUniqueBeanDefinitionException 异常。</li>
</ul>
<h2 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h2><p>根据上面源码的分析与实践，同一类型存在多个 Bean 的情况下，在使用 Autowired 注解时 Spring 会采用以下装配顺序来选择要装配的 Bean</p>
<ol>
<li>使用了 Primary 注解的 Bean;</li>
<li>查找使用了 Priority 注解的 Bean，并选择优先级最高的；</li>
<li>要注入的属性的属性名称与 Bean 名称相同的；</li>
</ol>
<p>在我们平时写代码时，为了代码的可读性，建议使用 Qualifier 注解来明确指明要注入 Bean。</p>
]]></content>
      <tags>
        <tag>Spring</tag>
        <tag>源码</tag>
      </tags>
  </entry>
  <entry>
    <title>不要过分相信自己的直觉之 RateLimiter.getRate()</title>
    <url>/2022/07/20/%E4%B8%8D%E8%A6%81%E8%BF%87%E5%88%86%E7%9B%B8%E4%BF%A1%E8%87%AA%E5%B7%B1%E7%9A%84%E7%9B%B4%E8%A7%89%E4%B9%8BRateLimiter-getRate/</url>
    <content><![CDATA[<p>最近使用 Guava 中 RateLimiter.getRate() 方法时遇到了一个反直觉的现象，这里贴出来给大家分享一下。</p>
<h3 id="现象"><a href="#现象" class="headerlink" title="现象"></a>现象</h3><p>我们先看下下面这段代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">    <span class="type">RateLimiter</span> <span class="variable">rateLimiter</span> <span class="operator">=</span> RateLimiter.create(<span class="number">10.0D</span>);</span><br><span class="line">    <span class="type">double</span> <span class="variable">rate</span> <span class="operator">=</span> rateLimiter.getRate();</span><br><span class="line">    System.out.println(rate);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>直觉上最终会输出 <code>10.0</code>，实际最终也是输出了 <code>10.0</code>。</p>
<p>可是如果我们将上述代码中的 <code>10.0D</code> 变为 <code>15.0D</code>，结果会输出什么呢？</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">    <span class="type">RateLimiter</span> <span class="variable">rateLimiter</span> <span class="operator">=</span> RateLimiter.create(<span class="number">15.0D</span>);</span><br><span class="line">    <span class="type">double</span> <span class="variable">rate</span> <span class="operator">=</span> rateLimiter.getRate();</span><br><span class="line">    System.out.println(rate);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>执行后输出的值竟然是 <code>14.999999999999998</code>。看到这个结果我觉得非常的奇怪，毕竟直觉告诉我应该输出 <code>15.0</code> 才对。</p>
<h3 id="刨根问底"><a href="#刨根问底" class="headerlink" title="刨根问底"></a>刨根问底</h3><p>看到这样的现象，我们首先就要从源码上分析一下，看一下 Guava 内部是如何处理的。</p>
<p>打开 RateLimiter.getRate() 方法，发现其返回的是抽象方法 doGetRate() 的值。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="type">double</span> <span class="title function_">getRate</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">synchronized</span> (mutex()) &#123;</span><br><span class="line">        <span class="keyword">return</span> doGetRate();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我们创建 RateLimiter 的实例时默认创建的是类型为 SmoothBursty 的对象，该类的 doGetRate() 方法实现如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">final</span> <span class="type">double</span> <span class="title function_">doGetRate</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> SECONDS.toMicros(<span class="number">1L</span>) / stableIntervalMicros;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里竟然不是直接返回我们创建 RataLimiter 时设置的值，而是通过除法运算得来的。</p>
<p>继续分析 stableIntervalMicros 是怎么设值的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 该方法是创建 RateLimiter 时调用的方法</span></span><br><span class="line"><span class="comment">**/</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">final</span> <span class="keyword">void</span> <span class="title function_">doSetRate</span><span class="params">(<span class="type">double</span> permitsPerSecond, <span class="type">long</span> nowMicros)</span> &#123;</span><br><span class="line">    resync(nowMicros);</span><br><span class="line">    <span class="type">double</span> <span class="variable">stableIntervalMicros</span> <span class="operator">=</span> SECONDS.toMicros(<span class="number">1L</span>) / permitsPerSecond;</span><br><span class="line">    <span class="built_in">this</span>.stableIntervalMicros = stableIntervalMicros;</span><br><span class="line">    doSetRate(permitsPerSecond, stableIntervalMicros);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>至此结论已经非常清晰了：由于在创建 RateLimiter 时成员变量 stableIntervalMicros 便是通过简单的除法运算得来的，当 1000000 微妙（1 秒）不能够被传入的限流值除尽时 stableIntervalMicros 就已经丢失了精度，从而导致在这种情况下调用 doGetRate() 是不能够得到我们创建 RateLimiter 时传入的原始值的。</p>
<h3 id="发散"><a href="#发散" class="headerlink" title="发散"></a>发散</h3><p>Guava 为什么不在创建 RateLimiter 时把我们传入的原始值保存在一个成员变量里，调用 doGetRate() 方法时直接该成员变量的值呢？</p>
<p>我个人的理解可以从变量和方法命名上去分析，RateLimiter.create(double permitsPerSecond) 方法的变量名 permitsPerSecond 其含义是每秒产生多少令牌。而 RateLimiter.getRate() 从方法名以及 Guava 的注释上来看该方法的含义更倾向于提供产生令牌的实际速率。</p>
<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>在使用一些三方的库甚至是 Java 本身提供的一些方法时要弄清楚其使用场景，尽量多验证一下各种边界值、特殊值的情况，不能盲目相信自己的直觉。</p>
]]></content>
      <tags>
        <tag>源码</tag>
        <tag>Guava</tag>
        <tag>限流</tag>
      </tags>
  </entry>
  <entry>
    <title>使用 Guava 中 RateLimiter 遇到的一个问题</title>
    <url>/2021/06/26/%E4%BD%BF%E7%94%A8Guava%E4%B8%ADRateLimiter%E9%81%87%E5%88%B0%E7%9A%84%E4%B8%80%E4%B8%AA%E9%97%AE%E9%A2%98/</url>
    <content><![CDATA[<p>线上业务使用了 Guava 中的 RateLimiter 来做单机的限流。最近运维同事反馈，在修改了限流的配置之后，请求频率没有超过限流的配置却触发了限流逻辑，在此对该问题进行一下分析与总结。</p>
<h2 id="问题模拟"><a href="#问题模拟" class="headerlink" title="问题模拟"></a>问题模拟</h2><p>根据上述问题的现象，初步推测问题可能跟 Guava 中 RateLimiter 的限流算法实现有关，为了方便排查问题，参考线上业务的限流的逻辑写了一个 demo，其核心代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="meta">@RequestMapping(&quot;/hello&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HelloController</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/** 用于存储不同的接口，对应的 RateLimiter **/</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> Map&lt;String, RateLimiter&gt; RATE_LIMITER_MAP = Maps.newConcurrentMap();</span><br><span class="line">    <span class="comment">/** 用于存储不同接口限流配置的参数 **/</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> Map&lt;String, String&gt; RATE_MAP = Maps.newConcurrentMap();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        <span class="comment">// 初始化一个接口默认的限流配置</span></span><br><span class="line">        RATE_MAP.put(<span class="string">&quot;loan&quot;</span>, <span class="string">&quot;100000&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 测试限流的接口</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> key 限流配置的 key</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@GetMapping(&quot;/testLimit&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">testLimit</span><span class="params">(String key)</span> &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">value</span> <span class="operator">=</span> RATE_MAP.get(key);</span><br><span class="line">        logger.info(<span class="string">&quot;自定义限流配置=&#123;&#125;&quot;</span>, value);</span><br><span class="line">        <span class="type">Double</span> <span class="variable">currentRate</span> <span class="operator">=</span> Double.parseDouble(value);</span><br><span class="line"></span><br><span class="line">        <span class="type">RateLimiter</span> <span class="variable">rateLimiter</span> <span class="operator">=</span> RATE_LIMITER_MAP.get(key);</span><br><span class="line">        <span class="keyword">if</span> (rateLimiter == <span class="literal">null</span> || !isSameRate(currentRate, rateLimiter.getRate())) &#123;</span><br><span class="line">            logger.info(<span class="string">&quot;创建新的RateLimiter|速率=&#123;&#125;&quot;</span>, currentRate.toString());</span><br><span class="line">            rateLimiter = RateLimiter.create(currentRate);</span><br><span class="line">            RATE_LIMITER_MAP.putIfAbsent(key, rateLimiter);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="type">boolean</span> <span class="variable">allowed</span> <span class="operator">=</span> rateLimiter.tryAcquire();</span><br><span class="line">        logger.info(<span class="string">&quot;是否允许当前请求通过=&#123;&#125;|rate=&#123;&#125;&quot;</span>, allowed, rateLimiter.getRate());</span><br><span class="line">        <span class="keyword">return</span> allowed ? <span class="string">&quot;SUCCESS&quot;</span> : <span class="string">&quot;FAIL&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 模拟修改限流配置</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> key 限流配置的key</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> rate 限流的值</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@GetMapping(&quot;/setRate&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">setRate</span><span class="params">(String key, String rate)</span> &#123;</span><br><span class="line">        RATE_MAP.put(key, rate);</span><br><span class="line">        <span class="type">String</span> <span class="variable">currentValue</span> <span class="operator">=</span> RATE_MAP.get(key);</span><br><span class="line">        logger.info(<span class="string">&quot;设置完之后的值=&#123;&#125;&quot;</span>, currentValue);</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;SUCCESS&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>启动工程，我们模拟一下出现问题时的操作。</p>
<ol>
<li>构造 HTTP 请求调用 <code>/setRate</code> 接口，将 loan 对应的限流的值修改为 5；</li>
<li>构造 HTTP 请求快速调用 <code>/testLimit</code> 接口 2 次，发现第一调用接口返回 SUCCESS， 第二次接口返回 FAIL，说明第 2 次请求触发了限流；</li>
<li>构造 HTTP 请求调用 <code>/testLimit</code> 5 次，保证 5 次请求在 1 秒内接收，发现这 5 次请求接口均返回 SUCCESS，说明这 5 次请求均未触发限流；</li>
<li>构造 HTTP 请求调用 <code>/testLimit</code> 6 次，保证 6 次请求均在 1 秒内接收，发现前 6 次请求接口均返回 SUCCESS，说明这 6 次请求均未触发限流；</li>
<li>构造 HTTP 请求调用 <code>/testLimit</code> 7 次，保证 7 次请求均在 1 秒内接收，发发现前 6 次请求接口均返回 SUCCESS，第 7 次请求接口返回 FAIL，说明前 6 次请求均未触发限流，第 7 次请求触发限流。</li>
</ol>
<p>至此，我们模拟的现象与线上一致。步骤 3 是我们预期的结果，步骤 2 中在未超过限流配置的情况下触发了限流，步骤 4、5 中的第 6 次请求在超过了限流配置的情况下，仍然允许通过，我们直观上认为这些是不合理的。</p>
<h2 id="排查与分析"><a href="#排查与分析" class="headerlink" title="排查与分析"></a>排查与分析</h2><p>Guava 中 RateLimiter 提供了两种令牌桶的算法实现：</p>
<ul>
<li>平滑突发限流（SmoothBursty）</li>
<li>平滑预热限流（SmoothWarmingUp）</li>
</ul>
<p>我们项目中使用了默认的平滑突发限流（SmoothBursty）算法，这里我们只针对该算法进行分析。</p>
<h3 id="tryAcquire-方法"><a href="#tryAcquire-方法" class="headerlink" title="tryAcquire 方法"></a>tryAcquire 方法</h3><p>其源码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">tryAcquire</span><span class="params">(<span class="type">int</span> permits, <span class="type">long</span> timeout, TimeUnit unit)</span> &#123;</span><br><span class="line">  <span class="type">long</span> <span class="variable">timeoutMicros</span> <span class="operator">=</span> max(unit.toMicros(timeout), <span class="number">0</span>);</span><br><span class="line">  checkPermits(permits);</span><br><span class="line">  <span class="type">long</span> microsToWait;</span><br><span class="line">  <span class="keyword">synchronized</span> (mutex()) &#123;</span><br><span class="line">    <span class="type">long</span> <span class="variable">nowMicros</span> <span class="operator">=</span> stopwatch.readMicros();</span><br><span class="line">    <span class="keyword">if</span> (!canAcquire(nowMicros, timeoutMicros)) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      microsToWait = reserveAndGetWaitLength(permits, nowMicros);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  stopwatch.sleepMicrosUninterruptibly(microsToWait);</span><br><span class="line">  <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以观察到只有在 canAcquire 方法返回 false 的情况下，才会返回 false，通过阅读 canAcquire 方法可知该方法跟 SmoothBursty 中的成员变量 nextFreeTicketMicros 有关，该成员变量的释义如下：</p>
<blockquote>
<p>The time when the next request (no matter its size) will be granted. After granting a request, this is pushed further in the future. Large requests push this further than small requests.</p>
</blockquote>
<p>该成员变量标识下一次请求获得令牌的最早时间，在创建 SmoothBursty 时初始值为 0，并且每次授权一个请求之后，这个值会向后推移。查找该成员变量引用的地方，发现 SmoothBursty 类中 reserveEarliestAvailable、resync 方法会触发该成员变量的更新，并且观察该方法的调用栈，其中就有 <code>RateLimiter#tryAcquire()</code> 方法。</p>
<h4 id="reserveEarliestAvailable、resync-方法"><a href="#reserveEarliestAvailable、resync-方法" class="headerlink" title="reserveEarliestAvailable、resync 方法"></a>reserveEarliestAvailable、resync 方法</h4><p>其源码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">  * <span class="doctag">@param</span> requiredPermits 表示我们该次请求所取得令牌数，我们这里值为 1</span></span><br><span class="line"><span class="comment">  * <span class="doctag">@param</span> nowMicros 距创建 RateLimiter 时经过了多少时长，单位为微秒</span></span><br><span class="line"><span class="comment">  */</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">final</span> <span class="type">long</span> <span class="title function_">reserveEarliestAvailable</span><span class="params">(<span class="type">int</span> requiredPermits, <span class="type">long</span> nowMicros)</span> &#123;</span><br><span class="line">  <span class="comment">// 刷新令牌数</span></span><br><span class="line">  resync(nowMicros);</span><br><span class="line">  <span class="type">long</span> <span class="variable">returnValue</span> <span class="operator">=</span> nextFreeTicketMicros;</span><br><span class="line">  <span class="comment">// storedPermits 是创建 RataLimiter 时的一个成员变量，表示当前存储的令牌数，在 resync 方法和本方法中会更新所存储的令牌数</span></span><br><span class="line">  <span class="comment">// 当前存储的令牌数跟需要获取的令牌数进行比较，取两者的的最小值，即为当前可以得到令牌数</span></span><br><span class="line">  <span class="type">double</span> <span class="variable">storedPermitsToSpend</span> <span class="operator">=</span> min(requiredPermits, <span class="built_in">this</span>.storedPermits);</span><br><span class="line">  <span class="comment">// 需要预先支付的令牌</span></span><br><span class="line">  <span class="type">double</span> <span class="variable">freshPermits</span> <span class="operator">=</span> requiredPermits - storedPermitsToSpend;</span><br><span class="line">  <span class="comment">// 在突然涌入大量请求，而现有的令牌数又不够用的情况下，会预先支付一定的令牌数</span></span><br><span class="line">  <span class="comment">// stableIntervalMicros 为创建 RateLimiter 时计算出的稳定时间间隔，其计算公式为 周期 / 令牌数，在我们的例子中，该值即为 200000 微妙（即 200ms）</span></span><br><span class="line">  <span class="comment">// waitMicros 表示预先支付令牌的时间</span></span><br><span class="line">  <span class="type">long</span> <span class="variable">waitMicros</span> <span class="operator">=</span></span><br><span class="line">      storedPermitsToWaitTime(<span class="built_in">this</span>.storedPermits, storedPermitsToSpend)</span><br><span class="line">          + (<span class="type">long</span>) (freshPermits * stableIntervalMicros);</span><br><span class="line">  <span class="comment">// 预先支付令牌的时间+原请求获取令牌的最早时间，即为下一次获取令牌的最早时间</span></span><br><span class="line">  <span class="built_in">this</span>.nextFreeTicketMicros = LongMath.saturatedAdd(nextFreeTicketMicros, waitMicros);</span><br><span class="line">  <span class="comment">// 所存储的令牌数需要减去本次消耗的令牌</span></span><br><span class="line">  <span class="built_in">this</span>.storedPermits -= storedPermitsToSpend;</span><br><span class="line">  <span class="comment">// 这里返回旧的获取令牌最早的时间，即本次请求无需为预支付的令牌付出额外的等待时间</span></span><br><span class="line">  <span class="keyword">return</span> returnValue;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> <span class="title function_">resync</span><span class="params">(<span class="type">long</span> nowMicros)</span> &#123;</span><br><span class="line">  <span class="comment">// if nextFreeTicket is in the past, resync to now</span></span><br><span class="line">  <span class="keyword">if</span> (nowMicros &gt; nextFreeTicketMicros) &#123;</span><br><span class="line">    <span class="comment">// 在 SmoothBursty 类中 coolDownIntervalMicros() 方法返回的是成员变量 stableIntervalMicros，在我们的例子中，该值即为 200000 微妙（即 200ms）</span></span><br><span class="line">    <span class="type">double</span> <span class="variable">newPermits</span> <span class="operator">=</span> (nowMicros - nextFreeTicketMicros) / coolDownIntervalMicros();</span><br><span class="line">    <span class="comment">// maxPermits 在 创建 RataLimiter 时的一个成员变量，表示所能存储的最大令牌数，在我们的例子中，该值为 5</span></span><br><span class="line">    <span class="comment">// 更新当前所存储的令牌数，取值为 原存储的令牌数与新产生的令牌数之和 与 最大存储令牌数 两个数值中较小的值，保证可用的令牌数不会大于所允许存储的最大令牌数</span></span><br><span class="line">    storedPermits = min(maxPermits, storedPermits + newPermits);</span><br><span class="line">    nextFreeTicketMicros = nowMicros;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在 reserveEarliestAvailable 方法结尾处打断点，来观察一下修改配置之后，第一次调用该方法时对 nextFreeTicketMicros 成员变量的设值情况。如下图:</p>
<img src="/2021/06/26/%E4%BD%BF%E7%94%A8Guava%E4%B8%ADRateLimiter%E9%81%87%E5%88%B0%E7%9A%84%E4%B8%80%E4%B8%AA%E9%97%AE%E9%A2%98/debug.png" class="" title="运行中各个变量的赋值情况">

<p>通过上图中的执行情况可知，在限流配置修改为 5TPS 的情况下，第一次请求进来之后，令牌桶内所存储的令牌桶还不足 1，发生了预支付令牌的情况，此时 nextFreeTicketMicros 成员变量会被赋值为约 200000 微妙（即 200ms），如果第二个请求是在第一个请求之后 200ms 内请求进来的，一定会触发限流逻辑，如下图：</p>
<img src="/2021/06/26/%E4%BD%BF%E7%94%A8Guava%E4%B8%ADRateLimiter%E9%81%87%E5%88%B0%E7%9A%84%E4%B8%80%E4%B8%AA%E9%97%AE%E9%A2%98/limit1.png" class="" title="第二次调用该方法时触发的限流的情况">

<p>同样的我们提上面的操作中 1 秒内的连续 6 次请求，第 6 次仍未触发限流，这种现象也是由于预支付令牌产生的。在一段时间内没有请求进来的情况下，令牌桶内累计了 5 个令牌，此时前 5 个令牌会被前 5 个请求消费掉，当第 6 个请求进来的时候，时间仍然在向前推进，只是此时桶内累计的令牌数不足 1 个，会触发预先支付令牌，故这种情况下第 6 个请求，仍然是能够通过的。而当第 7 个请求进来的时候，由于下一次请求获得令牌的最早时间已经大于当前时间了，故取令牌失败，触发限流，如下图：</p>
<img src="/2021/06/26/%E4%BD%BF%E7%94%A8Guava%E4%B8%ADRateLimiter%E9%81%87%E5%88%B0%E7%9A%84%E4%B8%80%E4%B8%AA%E9%97%AE%E9%A2%98/limit1.png" class="" title="Docker 第七次调用该方法时触发的限流的情况">

<h2 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h2><p>通过以上源码的分析，Gauava 中 RateLimiter 的平滑突发限流（SmoothBursty）是通过以固定的速率向桶内放置令牌，从而达到平滑输出的效果。由于桶内会进行令牌的积累，可以应对一定程度的流量突发。</p>
<p>对于我们线上遇到的修改配置之后第二个请求就触发了限流的问题，我们在切换配置时应该使用 RateLimiter 的 setRate 方法直接修改其限流配置，这样可以避免重新创建一个新的 RateLimiter 时会创建一个新的跑表（Stopwatch），同时也不会将 nextFreeTicketMicros 重新置为 0，使得在取令牌时对桶内令牌数的计算不会像重新创建一个那么极端。</p>
<h2 id="发散"><a href="#发散" class="headerlink" title="发散"></a>发散</h2><p>大家也可以看一下 Guava 中平滑预热限流（SmoothWarmingUp）在实现上与的平滑突发限流（SmoothBursty）有什么区别，此外常见的其他限流算法如漏桶算法、计数法分别是怎么实现的。</p>
]]></content>
      <tags>
        <tag>源码</tag>
        <tag>Guava</tag>
        <tag>限流</tag>
      </tags>
  </entry>
</search>
